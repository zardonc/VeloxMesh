---
milestone: v7.5
name: Scheduler Enhancements
audited: 2026-07-05T15:52:19-07:00
status: tech_debt
scores:
  requirements: 12/12
  phases: 3/3
  integration: 5/5
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
  process: []
tech_debt:
  - phase: 18-anomaly-and-ood-conservative-scoring
    items:
      - "18-VERIFICATION.md frontmatter uses status: complete instead of the workflow matrix value passed; content and evidence still show all ANOM requirements complete."
nyquist:
  compliant_phases:
    - 17-semantic-neighbor-feature-aggregates
    - 18-anomaly-and-ood-conservative-scoring
    - 19-sla-waiting-time-promotion
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# v7.5 Milestone Audit: Scheduler Enhancements

## Verdict

Functional milestone work is complete: all 12 v7.5 requirements are implemented, phase-verified, summary-backed, Nyquist-validated, and exercised through production-shape scheduler paths.

The only remaining item is documentation hygiene: `18-VERIFICATION.md` uses `status: complete` instead of the workflow matrix value `passed`.

## Scope

| Phase | Status | Evidence |
| --- | --- | --- |
| 17-semantic-neighbor-feature-aggregates | Passed | ROADMAP marks complete; `17-VERIFICATION.md` status is `passed`; summaries cover QDR-01..04. |
| 18-anomaly-and-ood-conservative-scoring | Complete | ROADMAP marks complete; `18-VERIFICATION.md` says complete and covers ANOM-01..04; `18-04` adds the final real ONNX Runtime path. |
| 19-sla-waiting-time-promotion | Passed | ROADMAP marks complete; `19-VERIFICATION.md` status is `passed`; summaries cover SLA-01..04. |

## Requirements Coverage

| Requirement | REQUIREMENTS.md | SUMMARY.md | VERIFICATION.md | Final |
| --- | --- | --- | --- | --- |
| QDR-01 | Complete | Listed | Passed | Satisfied |
| QDR-02 | Complete | Listed | Passed | Satisfied |
| QDR-03 | Complete | Listed | Passed | Satisfied |
| QDR-04 | Complete | Listed | Passed | Satisfied |
| ANOM-01 | Complete | Listed | Complete | Satisfied |
| ANOM-02 | Complete | Listed | Complete | Satisfied |
| ANOM-03 | Complete | Listed | Complete | Satisfied |
| ANOM-04 | Complete | Listed | Complete | Satisfied |
| SLA-01 | Complete | Listed | Passed | Satisfied |
| SLA-02 | Complete | Listed | Passed | Satisfied |
| SLA-03 | Complete | Listed | Passed | Satisfied |
| SLA-04 | Complete | Listed | Passed | Satisfied |

No orphaned v7.5 requirements found.

## Integration Check

| Area | Result | Evidence |
| --- | --- | --- |
| Semantic neighbor intake | Passed | `TaskIntake.Submit` extracts safe features, calls `SemanticNeighbors.Enrich` under `SemanticNeighborTaskTimeout`, and falls back to original safe features on timeout/error. |
| Training and ONNX feature path | Passed | Summary extraction shows QDR coverage across 17-01..03; tests pass for scheduler training artifact/export paths. |
| Real ONNX predictor path | Passed | `cmd/scheduler` ONNX mode uses `predictive.BatchScoreService`; smoke publishes a runtime artifact, starts `scheduler_training.onnx_worker`, waits for gRPC health, calls `BatchScoreTasks`, and requires a non-fallback score. |
| SLA queue promotion | Passed | `Executor.RunOne` calls `SLAPromoter.PromoteBeforePop` before `PopMin`; memory, Redis, and fallback queues expose non-mutating `PeekMin`; promotion uses score replacement via `Push`. |
| App wiring | Passed | `newSchedulerRunner` injects semantic neighbors, training recorder, quality recorder, and SLA promoter into the Gateway-owned scheduler runner. |

## Production-Shape Verification

| Check | Result |
| --- | --- |
| Python ONNX Runtime worker | Passed: `uv run --project tools/scheduler_training pytest tools/scheduler_training/tests/test_onnx_worker.py tools/scheduler_training/tests/test_train_publish.py -q` returned `8 passed in 1.22s`. |
| Scheduler RPC with real worker/model | Passed: `go test -count=1 -timeout 60s ./cmd/scheduler ./internal/scheduler ./internal/config ./internal/app ./internal/observability` executed the real worker smoke and passed. |
| Full backend test suite | Passed: `go test -count=1 -timeout 60s ./...` completed, including `tests/integration`. |
| Build | Passed: `go build ./...`. |

## Temporary vs Final Assessment

| Concern | Finding |
| --- | --- |
| Phase 18 temporary ONNX parser | Closed. `18-DISCUSSION-LOG.md` records that the temporary patch was rejected; `18-04-PLAN.md` and `18-04-SUMMARY.md` replace it with a model-neutral predictor boundary plus real Python ONNX Runtime worker. |
| Mock-only model evidence | Not found. Current smoke publishes a production-shape artifact, loads it through `onnxruntime.InferenceSession`, serves predictor gRPC, and calls the Scheduler RPC. |
| Scheduler-owned queue/vector boundaries | Not found. Gateway owns semantic lookup, queueing, execution, and promotion; Scheduler receives bounded safe feature fields. |
| Raw prompt/secret leakage | Not found in audited paths. Semantic aggregates, predictor RPC, promotion metrics, logs, and audit evidence use bounded scalar/enum/config dimensions. |

## Cross-Phase Flows

| Flow | Result | Evidence |
| --- | --- | --- |
| Gateway request to semantic-enriched scheduler task | Passed | Optional collector is disabled by default, injected only when dependencies exist, and fail-open under timeout. |
| Completed task to training sample to semantic aggregate | Passed | `SynchronousRunner` records completion evidence and indexes completed samples only when recorder/indexer are configured. |
| Training artifact to real ONNX worker to Scheduler score | Passed | Artifact publisher writes `model.onnx` and `manifest.json`; worker loads one `InferenceSession`; Scheduler ONNX mode routes through predictor client and `predictive.Scorer`. |
| Queued task waiting threshold to promotion evidence | Passed | Promotion checks tenant/class, model class, request kind, priority ordering, and emits sanitized metric/audit/log evidence for promoted/blocked/error outcomes. |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
| --- | --- | --- | --- |
| 17-semantic-neighbor-feature-aggregates | Exists | Yes | None. |
| 18-anomaly-and-ood-conservative-scoring | Exists | Yes | None. |
| 19-sla-waiting-time-promotion | Exists | Yes | None. |

## Open Items

- Optionally normalize `18-VERIFICATION.md` frontmatter from `status: complete` to `status: passed` so future tooling does not need to special-case it.
