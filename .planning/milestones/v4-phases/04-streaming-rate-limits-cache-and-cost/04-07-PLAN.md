---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 07
type: execute
wave: 7
depends_on:
  - 04-06
files_modified:
  - internal/http/middleware/auth.go
  - internal/http/middleware/auth_test.go
  - internal/admission/controller.go
  - internal/admission/controller_test.go
  - internal/app/app.go
  - internal/app/app_test.go
  - internal/gateway/service_test.go
  - tests/integration/chat_test.go
autonomous: true
requirements:
  - RATE-01
user_setup: []
must_haves:
  truths:
    - "D-29/D-30: requests with no usable credits return 429 before provider calls."
    - "D-32: existing admission boundary enforces positive-credit checks."
    - "D-34/D-38: admission does not deduct, reserve, or charge failed requests."
  artifacts:
    - path: "internal/http/middleware/auth.go"
      provides: "safe API-key identity in request context"
    - path: "internal/admission/controller.go"
      provides: "positive-credit admission controller"
    - path: "internal/app/app.go"
      provides: "mode-aware admission wiring"
  key_links:
    - from: "internal/http/middleware/auth.go"
      to: "internal/admission/controller.go"
      via: "safe API key hash or ID in context"
      pattern: "APIKey|Context|Hash"
---

<objective>
Finish the credit admission theme by enforcing positive-credit admission before provider calls.

Purpose: protect providers from zero-credit requests without implementing reservation or settlement early.
Output: safe auth context identity, repository-backed admission, 429 error contract, and tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-06-SUMMARY.md
@internal/http/middleware/auth.go
@internal/admission/controller.go
@internal/app/app.go
@internal/gateway/service.go
</context>

## Decision Coverage
D-02 D-24 D-25 D-26 D-29 D-30 D-31 D-32 D-33 D-34 D-38 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 RATE-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass Safe API-Key Identity to Admission</name>
  <files>internal/http/middleware/auth.go, internal/http/middleware/auth_test.go, internal/app/app.go, internal/app/app_test.go</files>
  <behavior>
    - Auth middleware stores safe API-key hash or ID in context after successful auth.
    - Raw bearer token is not stored in context or logs.
    - Durable mode authenticates by API-key repository hash and enabled status.
    - Disabled mode keeps `DevAPIKey` compatibility.
  </behavior>
  <action>Add minimal context helpers for authenticated API key identity using the existing SHA-256 token hash. In durable mode, authenticate data-plane keys by repository hash and enabled status; in disabled mode keep static dev auth per D-06. Auth-cache may cache validity but must not bypass credit admission per D-29.</action>
  <verify>
    <automated>go test ./internal/http/middleware -run "Auth|APIKey|Context|Cache"</automated>
    <automated>go test ./internal/app -run "Auth|Admission|Mode"</automated>
  </verify>
  <done>Admission receives safe API-key identity without raw token exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce Positive-Credit Admission</name>
  <files>internal/admission/controller.go, internal/admission/controller_test.go, internal/gateway/service_test.go, tests/integration/chat_test.go</files>
  <behavior>
    - Positive-credit key is admitted and provider fake is called.
    - Missing, disabled, zero-credit, and negative-credit keys return 429 before provider fake is called.
    - 429 response includes safe quota-style headers and OpenAI-compatible error JSON.
    - Admission release is no-op because this phase has no reservation.
  </behavior>
  <action>Implement a repository-backed `CreditAdmissionController` at the existing admission boundary per D-32. It only checks `CreditBalance > 0` before provider execution per D-29. Do not deduct, reserve, fixed-window count, provider-level throttle, or enqueue requests per D-31 and D-38. Return structured `quota_exhausted` or `insufficient_credits` with HTTP 429 and safe quota headers per D-30.</action>
  <verify>
    <automated>go test ./internal/admission -run "Credit|Quota|Mode"</automated>
    <automated>go test ./internal/gateway -run "Admission|Credit|Quota"</automated>
    <automated>go test ./tests/integration -run "ChatCompletions|Quota|Admission"</automated>
    <automated>go test ./...</automated>
  </verify>
  <done>Zero/no-credit requests are rejected before upstream provider calls.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: SQLite-backed API-key auth and positive-credit admission work locally.
Full mode: PostgreSQL-backed API-key auth and positive-credit admission are required; Redis may cache auth validity only.
Disabled mode: static `DevAPIKey` plus pass-through admission remain local compatibility only.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authorization header -> auth middleware | Raw token becomes safe identity. |
| auth context -> admission | Authenticated identity controls provider admission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | auth context/logs | mitigate | Store only token hash/API key ID; tests reject raw bearer exposure per D-42. |
| T-04-07-02 | Elevation of Privilege | auth cache | mitigate | Auth cache cannot bypass durable credit check. |
| T-04-07-03 | Denial of Service | zero-credit traffic | mitigate | Reject before provider call with 429 per D-29/D-30. |
| T-04-07-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/http/middleware ./internal/admission ./internal/app ./internal/gateway`.
Run `go test ./tests/integration -run "ChatCompletions|Quota|Admission"`.
Run `go test ./...`.
</verification>

<success_criteria>
RATE-01 is satisfied as positive-credit admission: no usable credits means no provider call.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-07-SUMMARY.md` when done.
</output>
