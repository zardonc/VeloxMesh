---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 09
type: execute
wave: 9
depends_on:
  - 04-08
files_modified:
  - internal/controlstate/admin_service.go
  - internal/controlstate/admin_service_test.go
  - internal/http/handlers/admin_providers.go
  - internal/http/handlers/admin_providers_test.go
  - internal/http/router.go
  - tests/integration/admin_providers_test.go
autonomous: true
requirements:
  - COST-01
user_setup: []
must_haves:
  truths:
    - "D-27/D-28: Admin can manage provider/model integer credit rates."
    - "D-42: rate APIs remain secret-safe."
    - "D-44..D-49: rate management is available in durable lite/full modes and disabled in static mode."
  artifacts:
    - path: "internal/controlstate/admin_service.go"
      provides: "rate service validation and repository calls"
    - path: "internal/http/handlers/admin_providers.go"
      provides: "rate management HTTP endpoints"
  key_links:
    - from: "internal/http/handlers/admin_providers.go"
      to: "internal/controlstate/admin_service.go"
      via: "provider/model rate DTOs"
      pattern: "Rate|ProviderModel"
---

<objective>
Expose Admin-managed provider/model credit rates as durable configuration.

Purpose: let cost settlement use configured integer rates instead of code defaults.
Output: narrow Admin rate endpoints, validation, and integration tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-08-SUMMARY.md
@internal/controlstate/admin_service.go
@internal/http/handlers/admin_providers.go
@internal/http/router.go
</context>

## Decision Coverage
D-02 D-27 D-28 D-31 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 COST-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Admin Rate Service Methods</name>
  <files>internal/controlstate/admin_service.go, internal/controlstate/admin_service_test.go</files>
  <behavior>
    - Upsert/read/delete validates provider exists and model belongs to provider.
    - Negative rates, empty provider ID, and empty model are rejected.
    - Integer zero rates are allowed and distinct from missing rate.
    - Disabled control-state mode reports rate management unavailable.
  </behavior>
  <action>Add the smallest Admin service methods for provider/model rate management. Validate against durable provider records and model catalog where available per D-27. Do not add bulk pricing, Admin Console UI, budget enforcement, cost-aware routing, or model degradation.</action>
  <verify>
    <automated>go test ./internal/controlstate -run "Rate|Admin"</automated>
  </verify>
  <done>Admin service can validate and manage provider/model rates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Rate HTTP Endpoints</name>
  <files>internal/http/handlers/admin_providers.go, internal/http/handlers/admin_providers_test.go, internal/http/router.go, tests/integration/admin_providers_test.go</files>
  <behavior>
    - `PUT` upserts a provider/model rate with integer input/output per-1K credits.
    - `GET` reads the current rate.
    - `DELETE` removes the rate or marks it missing.
    - Admin auth, idempotency/audit style, and secret-safe response patterns match existing Admin provider routes.
  </behavior>
  <action>Add narrow routes under existing Admin provider paths, for example `/admin/v1/providers/{id}/models/{model}/rate`. Use existing Admin bearer auth and handler response style. Return only safe provider/model/rate metadata. Keep live local checks opt-in per D-43.</action>
  <verify>
    <automated>go test ./internal/http/handlers -run "Rate|Admin"</automated>
    <automated>go test ./tests/integration -run "AdminProviders|Rates"</automated>
    <automated>go test ./...</automated>
  </verify>
  <done>Provider/model credit rates are Admin-managed durable config.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: Admin rate APIs work against SQLite durable state.
Full mode: Admin rate APIs work against PostgreSQL durable state and can be propagated by existing Redis config-change notification behavior.
Disabled mode: Admin rate APIs are unavailable because durable control state is off.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> durable rates | Admin input changes accounting rates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | rate endpoints | mitigate | Existing Admin auth plus provider/model and non-negative integer validation. |
| T-04-09-02 | Information Disclosure | rate responses | mitigate | Return rates and safe IDs only; no provider secrets or API keys per D-42. |
| T-04-09-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/controlstate ./internal/http/handlers`.
Run `go test ./tests/integration -run "AdminProviders|Rates"`.
Run `go test ./...`.
</verification>

<success_criteria>
Admin can manage durable provider/model credit rates in lite and full modes.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-09-SUMMARY.md` when done.
</output>
