---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/controlstate/types.go
  - internal/controlstate/repository.go
  - internal/controlstate/migrations/sqlite/0001_control_state.sql
  - internal/controlstate/migrations/postgres/0001_control_state.sql
  - internal/controlstate/sqlite/repository.go
  - internal/controlstate/sqlite/repository_test.go
  - internal/controlstate/postgres/repository.go
  - internal/controlstate/postgres/repository_test.go
autonomous: true
requirements:
  - CB-01
user_setup: []
must_haves:
  truths:
    - "D-04/D-05/D-08: durable control-state backends never silently fall back to hard-coded provider defaults."
    - "D-10/D-13: durable routing config is stored, loaded, and validated before runtime activation."
    - "D-44..D-49: lite mode uses SQLite; full mode requires PostgreSQL plus Redis capability checks."
  artifacts:
    - path: "internal/controlstate/repository.go"
      provides: "real RoutingRepository contract for durable fallback/routing config"
    - path: "internal/controlstate/sqlite/repository.go"
      provides: "SQLite routing config persistence for lite mode"
    - path: "internal/controlstate/postgres/repository.go"
      provides: "PostgreSQL routing config persistence for full mode"
  key_links:
    - from: "internal/controlstate/runtime.go"
      to: "internal/controlstate/repository.go"
      via: "RoutingRepository.Get before activation"
      pattern: "RoutingRepository|GetRouting|RoutingConfig"
---

<objective>
Create the durable routing repository foundation for the first D-02 theme: durable provider hardening.

Purpose: remove routing/fallback no-op storage before runtime, circuit breaker, quota, and settlement depend on durable state.
Output: SQLite/PostgreSQL routing config persistence, validation helpers, and focused repository tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@internal/controlstate/types.go
@internal/controlstate/repository.go
@internal/controlstate/sqlite/repository.go
@internal/controlstate/postgres/repository.go
</context>

## Decision Coverage
D-01 D-02 D-04 D-05 D-06 D-08 D-10 D-13 D-44 D-45 D-46 D-47 D-48 D-49

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist Durable Routing Config</name>
  <files>internal/controlstate/types.go, internal/controlstate/repository.go, internal/controlstate/migrations/sqlite/0001_control_state.sql, internal/controlstate/migrations/postgres/0001_control_state.sql, internal/controlstate/sqlite/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/postgres/repository.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - SQLite `SaveRoutingConfig` upserts `strategy`, `default_provider`, `fallback_enabled`, `max_attempts`, and `revision`.
    - SQLite `GetRoutingConfig` returns a typed not-found result only when no routing row exists.
    - PostgreSQL implements equivalent SQL and keeps opt-in `POSTGRES_TEST_DSN` integration coverage.
    - Stubbed routing repository methods no longer pretend success.
  </behavior>
  <action>Implement durable routing config persistence per D-10 and D-13. Use the existing migrations and repository style; add the smallest missing schema columns or table needed for routing config. Preserve static/env provider config only for `ControlStateBackend=disabled` per D-06. Do not add API-key, usage, rate, cache, or Admin API behavior in this plan.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "Routing|Repository"</automated>
    <automated>go test ./internal/controlstate/postgres -run "Routing|Repository|SQLShape|Integration"</automated>
  </verify>
  <done>Routing config is durable in SQLite and PostgreSQL with no no-op success path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate Routing Config Against Active Providers</name>
  <files>internal/controlstate/types.go, internal/controlstate/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - Unknown strategy is rejected.
    - Missing active default provider is rejected.
    - Fallback disabled forces one attempt.
    - Fallback enabled caps `max_attempts` at active eligible provider count.
  </behavior>
  <action>Add a narrow validation helper for durable routing config per D-08 and D-10. Keep validation free of hard-coded provider/model defaults such as `openai-primary` or `gpt-4o-mini` per D-04. Include mode assertions: SQLite is valid for lite mode per D-45; full-mode distributed capability is only true when PostgreSQL and Redis prerequisites are configured per D-47 and D-48.</action>
  <verify>
    <automated>go test ./internal/controlstate/... -run "Routing|Validate|Mode"</automated>
  </verify>
  <done>Invalid durable routing config fails closed before any runtime activation.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: SQLite routing config works locally without Redis.
Full mode: PostgreSQL routing config is required; full distributed capability also requires Redis.
Disabled mode: static/env routing remains compatibility-only and is not used when durable backend is selected.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable storage -> routing config | Database records become provider selection and fallback state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | routing config | mitigate | Validate strategy, default provider, fallback flag, and max_attempts against active providers before activation per D-10. |
| T-04-01-02 | Denial of Service | missing durable config | mitigate | Return typed missing-config errors instead of silently using hard-coded defaults per D-08. |
| T-04-01-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/controlstate/...`.
Run `rg -n "openai-primary|gpt-4o-mini" internal/controlstate` and confirm matches are negative assertions or disabled-mode compatibility only.
</verification>

<success_criteria>
Durable routing config is real, validated, and mode-aware before runtime hardening continues.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-01-SUMMARY.md` when done.
</output>
