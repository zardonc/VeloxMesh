---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - internal/config/config.go
  - internal/app/app.go
  - internal/app/app_test.go
  - internal/controlstate/runtime.go
  - internal/controlstate/runtime_test.go
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/health/prober.go
  - internal/health/prober_test.go
  - internal/http/handlers/health.go
  - tests/integration/durable_runtime_test.go
autonomous: true
requirements:
  - CB-01
user_setup: []
must_haves:
  truths:
    - "D-07/D-11/D-12: startup and reload publish one runtime snapshot used by routing, models, readiness, probing, and fallback."
    - "D-14/D-15: durable startup and request-time fallback settings no longer depend on constructor-time static config."
    - "D-16/D-17: readiness and prober settings come from the active runtime snapshot under durable control state."
  artifacts:
    - path: "internal/controlstate/runtime.go"
      provides: "runtime snapshot containing provider registry, router, routing config, model catalog, prober config, and mode capability"
    - path: "internal/app/app.go"
      provides: "normal durable startup path for sqlite/postgres control state"
    - path: "internal/http/handlers/health.go"
      provides: "readyz values from runtime snapshot"
  key_links:
    - from: "internal/app/app.go"
      to: "internal/controlstate/runtime.go"
      via: "initial ReloadProviders before serving data-plane traffic"
      pattern: "ReloadProviders|ActivateProviderSet"
    - from: "internal/gateway/service.go"
      to: "internal/controlstate/runtime.go"
      via: "request-time routing/fallback snapshot"
      pattern: "RuntimeSnapshot|RoutingConfig|Fallback"
---

<objective>
Finish the durable provider hardening theme by making the runtime snapshot the source of truth.

Purpose: ensure data-plane behavior sees durable provider/routing/prober changes without process restart.
Output: durable app startup wiring, runtime snapshot accessors, readyz/prober updates, and regression tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-01-SUMMARY.md
@.planning/phases/03-durable-control-state/03-03-PLAN.md
@.planning/phases/03-durable-control-state/03-07-PLAN.md
@internal/config/config.go
@internal/app/app.go
@internal/controlstate/runtime.go
@internal/gateway/service.go
@internal/health/prober.go
@internal/http/handlers/health.go
</context>

## Decision Coverage
D-01 D-02 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-14 D-15 D-16 D-17 D-18 D-44 D-45 D-46 D-47 D-48 D-49

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate Durable Runtime Before Serving</name>
  <files>internal/config/config.go, internal/app/app.go, internal/app/app_test.go, internal/controlstate/runtime.go, internal/controlstate/runtime_test.go, internal/gateway/service.go, internal/gateway/service_test.go</files>
  <behavior>
    - Durable `sqlite` and `postgres` startup load active providers and durable routing config before serving.
    - Missing durable provider config returns actionable no-active-provider/missing-config errors.
    - Disabled control-state mode still supports static/env providers for local compatibility.
    - Updating durable routing config changes fallback attempts without rebuilding `gateway.Service`.
  </behavior>
  <action>Update `app.New` and `RuntimeProviderManager` so durable backends perform normal repository setup, optional explicit seed, and initial reload before data-plane traffic per D-07 and D-14. Move provider runtime state out of `config.go` for durable mode per D-09. Update `gateway.Service` to read fallback enabled and max attempts from the current runtime/routing snapshot during request execution per D-15. Do not introduce streaming, quota, cost, cache, or circuit-breaker state in this plan.</action>
  <verify>
    <automated>go test ./internal/app -run "Durable|Startup|Reload|Mode"</automated>
    <automated>go test ./internal/controlstate -run "Runtime|Snapshot|Routing|Mode"</automated>
    <automated>go test ./internal/gateway -run "Fallback|Runtime|Durable"</automated>
  </verify>
  <done>Durable runtime activation is the normal app path for sqlite/postgres backends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Readiness and Probing Use Runtime Snapshot</name>
  <files>internal/health/prober.go, internal/health/prober_test.go, internal/http/handlers/health.go, internal/app/app_test.go, tests/integration/durable_runtime_test.go</files>
  <behavior>
    - `Readyz` reports configured providers, routing strategy, mode, and probe settings from runtime snapshot in durable mode.
    - `health.Prober` uses durable provider health config from the active snapshot.
    - Anthropic/Gemini health semantics are explicit when live upstream probing is unsupported.
    - `/v1/models`, readyz, chat routing, fallback, and prober state change after durable reload.
  </behavior>
  <action>Replace durable-mode reads of `cfg.Providers`, `cfg.RoutingStrategy`, static fallback settings, and static health overrides in readiness/probing paths with runtime snapshot reads per D-12, D-16, and D-17. For native adapter health checks, keep semantics secret-safe and honest: report configured/probe-unsupported or use a cheap SDK metadata check if one already exists; do not add prompt-generating health calls per D-18.</action>
  <verify>
    <automated>go test ./internal/health -run "Prober|Durable|Native|Mode"</automated>
    <automated>go test ./internal/app -run "Readyz|Durable|Mode"</automated>
    <automated>go test ./tests/integration -run "DurableRuntime|Health|Models"</automated>
  </verify>
  <done>Readiness, models, routing, fallback, and probing reflect the active durable runtime snapshot.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: SQLite durable provider/routing config, readiness, models, local fallback, and probing work without Redis.
Full mode: PostgreSQL plus Redis are required for production-capable distributed runtime reload/config-change behavior.
Disabled mode: static/env providers remain local compatibility only.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable repositories -> runtime snapshot | Database state becomes live data-plane provider state. |
| Admin reload -> data-plane routing | Reload changes active provider, routing, fallback, and probing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | runtime activation | mitigate | Publish snapshots only after durable provider and routing validation succeeds per D-10 and D-11. |
| T-04-02-02 | Information Disclosure | readyz/prober errors | mitigate | Expose provider IDs and safe statuses only; never API keys, auth headers, raw prompts, or upstream payloads per D-42. |
| T-04-02-03 | Denial of Service | missing config | mitigate | Durable mode fails closed with actionable not-ready/no-provider errors per D-08. |
| T-04-02-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/app ./internal/controlstate ./internal/gateway ./internal/health`.
Run `go test ./tests/integration -run "DurableRuntime|Health|Models"`.
Run `go test ./...`.
</verification>

<success_criteria>
Durable provider configuration is the source of truth for runtime data-plane behavior in lite and full modes.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-02-SUMMARY.md` when done.
</output>
