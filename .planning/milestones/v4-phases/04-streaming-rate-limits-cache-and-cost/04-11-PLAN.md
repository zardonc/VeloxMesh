---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 11
type: execute
wave: 11
depends_on:
  - 04-10
files_modified:
  - internal/llm/types.go
  - internal/providers/adapter.go
  - internal/providers/capabilities.go
  - internal/providers/openai/adapter.go
  - internal/providers/openai/adapter_test.go
  - internal/controlstate/types.go
  - internal/controlstate/repository.go
  - internal/controlstate/migrations/sqlite/0001_control_state.sql
  - internal/controlstate/migrations/postgres/0001_control_state.sql
  - internal/controlstate/sqlite/repository.go
  - internal/controlstate/sqlite/repository_test.go
  - internal/controlstate/postgres/repository.go
autonomous: true
requirements:
  - CACHE-01
user_setup: []
must_haves:
  truths:
    - "CACHE-01: semantic cache has real embedding-backed storage, not a disabled-only interface."
    - "D-03: semantic cache is planned only after usage/cost recording exists."
    - "D-42: cache storage does not persist raw prompts or provider secrets."
  artifacts:
    - path: "internal/providers/adapter.go"
      provides: "optional provider-neutral embeddings interface"
    - path: "internal/providers/openai/adapter.go"
      provides: "OpenAI-compatible embeddings implementation using existing HTTP adapter patterns"
    - path: "internal/controlstate/repository.go"
      provides: "semantic cache repository contract"
  key_links:
    - from: "internal/providers/openai/adapter.go"
      to: "internal/llm/types.go"
      via: "embedding request/response"
      pattern: "Embed|Embedding"
    - from: "internal/gateway/service.go"
      to: "internal/controlstate/repository.go"
      via: "semantic cache lookup/store in next plan"
      pattern: "SemanticCache"
---

<objective>
Add the storage and embedding foundations needed for real opt-in semantic cache behavior.

Purpose: satisfy CACHE-01 with an actual semantic cache path after settlement exists, while avoiding vector database dependencies.
Output: provider-neutral embeddings operation, OpenAI-compatible embedding support, durable semantic cache entries, and repository tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-10-SUMMARY.md
@internal/llm/types.go
@internal/providers/adapter.go
@internal/providers/openai/adapter.go
@internal/controlstate/types.go
@internal/controlstate/repository.go
</context>

## Decision Coverage
D-01 D-02 D-03 D-39 D-40 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 CACHE-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Provider-Neutral Embeddings Operation</name>
  <files>internal/llm/types.go, internal/providers/adapter.go, internal/providers/capabilities.go, internal/providers/openai/adapter.go, internal/providers/openai/adapter_test.go</files>
  <behavior>
    - Embedding request contains model and normalized text input without logging raw prompts.
    - Embedding response contains numeric vector and usage metadata when provider supplies it.
    - OpenAI-compatible adapter can call embeddings endpoint using existing API-key/base-URL configuration.
    - Capability metadata reports embeddings support only for implemented adapters.
  </behavior>
  <action>Add a narrow optional embeddings interface for adapters, implemented first in `internal/providers/openai` with existing stdlib HTTP patterns. This uses already-present provider configuration and dependencies; do not add a vector database or new package. Native adapters may return unsupported unless an embedding operation already exists safely. Keep raw prompt/input out of logs and errors per D-42.</action>
  <verify>
    <automated>go test ./internal/providers/openai -run "Embedding|Capability|SecretSafe"</automated>
    <automated>go test ./internal/providers -run "Capability|Embedding"</automated>
  </verify>
  <done>Semantic cache can obtain real embeddings from an implemented adapter path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store Durable Semantic Cache Entries</name>
  <files>internal/controlstate/types.go, internal/controlstate/repository.go, internal/controlstate/migrations/sqlite/0001_control_state.sql, internal/controlstate/migrations/postgres/0001_control_state.sql, internal/controlstate/sqlite/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/postgres/repository.go</files>
  <behavior>
    - Cache entries store tenant/API-key scope, model, embedding vector, response JSON, created timestamp, expiry, hit count, and source usage ID when available.
    - Raw prompt text and bearer tokens are not stored.
    - SQLite and PostgreSQL can insert/list candidates by scope/model for bounded in-application cosine similarity.
    - Entries can be disabled or expired without deleting usage records.
  </behavior>
  <action>Add a minimal `SemanticCacheEntry` and repository contract. Store embeddings as JSON/text or byte-encoded numeric arrays using stdlib encoding; similarity calculation will happen in gateway code in 04-12 to avoid a vector database dependency. Scope entries by API key hash or tenant-equivalent safe identity plus model to avoid cross-key leakage. Add TTL/expiry fields and enabled flag. Do not store raw prompts; if a hash of normalized input is needed, use a hash only.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "SemanticCache|Embedding|SecretSafe"</automated>
    <automated>go test ./internal/controlstate/postgres -run "SemanticCache|Embedding|SQLShape|Integration"</automated>
  </verify>
  <done>Durable semantic cache entries exist with privacy-safe metadata and embeddings.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: SQLite semantic cache storage and embedding lookup can work locally when explicitly enabled and a configured provider supports embeddings.
Full mode: PostgreSQL semantic cache storage works with the same repository contract; Redis is not required for semantic similarity and does not store prompts.
Disabled mode: semantic cache is unavailable because durable storage and safe API-key scoping are absent.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt-derived text -> embeddings provider | User input is sent for embedding only when semantic cache is explicitly enabled. |
| cache storage -> future responses | Stored response JSON may be returned on semantic match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Information Disclosure | semantic cache storage | mitigate | Store embeddings, safe scope, and response only; never raw prompts, tokens, or provider secrets. |
| T-04-11-02 | Spoofing | cache scope | mitigate | Scope entries by safe API-key identity and model to prevent cross-key hits. |
| T-04-11-03 | Tampering | embedding vectors | mitigate | Repository tests verify encoded vector round-trip and expiry/enabled flags. |
| T-04-11-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/providers/openai ./internal/providers`.
Run `go test ./internal/controlstate/sqlite ./internal/controlstate/postgres`.
Run `rg -n "raw prompt|Authorization|Bearer|sk-|api_key" internal/controlstate internal/providers/openai` and confirm matches are redaction, fixtures, or negative assertions only.
</verification>

<success_criteria>
CACHE-01 has real embedding and durable semantic cache primitives after usage/cost recording exists.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-11-SUMMARY.md` when done.
</output>
