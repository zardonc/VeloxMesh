---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - internal/gateway/circuitbreaker.go
  - internal/gateway/circuitbreaker_test.go
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/controlstate/runtime.go
  - internal/controlstate/runtime_test.go
autonomous: true
requirements:
  - CB-01
user_setup: []
must_haves:
  truths:
    - "CB-01: gateway has tested closed, open, and half-open circuit-breaker states."
    - "CB-01: fallback chain skips open providers and can recover through half-open success."
    - "D-10/D-15: breaker and fallback decisions use the active runtime routing snapshot."
  artifacts:
    - path: "internal/gateway/circuitbreaker.go"
      provides: "provider circuit breaker state machine"
    - path: "internal/gateway/service.go"
      provides: "fallback-chain integration that skips open circuits"
  key_links:
    - from: "internal/gateway/service.go"
      to: "internal/gateway/circuitbreaker.go"
      via: "allow/record success/record failure around provider attempts"
      pattern: "CircuitBreaker|Allow|Record"
---

<objective>
Deliver CB-01 as its own small durable-hardening slice before streaming begins.

Purpose: make fallback-chain behavior explicit and tested instead of hiding it inside durable config work.
Output: provider circuit breaker state machine, gateway fallback-chain integration, and closed/open/half-open tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-02-SUMMARY.md
@.planning/phases/02-health-aware-routing/02-05-SUMMARY.md
@internal/gateway/service.go
@internal/controlstate/runtime.go
</context>

## Decision Coverage
D-01 D-02 D-10 D-11 D-15 D-20 D-21 D-34 D-44 D-45 D-46 D-47 D-48 D-49 CB-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Circuit Breaker State Machine</name>
  <files>internal/gateway/circuitbreaker.go, internal/gateway/circuitbreaker_test.go</files>
  <behavior>
    - Closed state allows requests and counts consecutive retryable provider failures.
    - Failure threshold opens the circuit and blocks selection.
    - Recovery timeout moves open to half-open.
    - Half-open success closes the circuit; half-open failure opens it again.
  </behavior>
  <action>Create a small provider-keyed circuit breaker using Go stdlib time and a testable clock hook. Use retryable provider errors and provider health failure outcomes already present in gateway code. Keep thresholds in runtime routing snapshot defaults or existing config-derived values; do not add Admin UI or distributed breaker storage. This is actual CB-01 behavior, not a config placeholder.</action>
  <verify>
    <automated>go test ./internal/gateway -run "CircuitBreaker"</automated>
  </verify>
  <done>Circuit breaker states and transitions are deterministic and covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Breaker with Fallback Chain</name>
  <files>internal/gateway/service.go, internal/gateway/service_test.go, internal/controlstate/runtime.go, internal/controlstate/runtime_test.go</files>
  <behavior>
    - Open providers are skipped during provider selection/fallback.
    - Fallback chain tries the next eligible closed or half-open provider.
    - Strict `X-Route-To` override returns structured unavailable when that provider circuit is open.
    - Breaker records success/failure once per provider attempt and does not charge failed requests.
  </behavior>
  <action>Wire the breaker into `gateway.Service` around provider attempts. Consult the active runtime snapshot for fallback settings per D-15. Preserve D-20/D-21 streaming constraints by exposing an allow/record boundary that streaming can use before first bytes only. In lite mode breaker state is per process; in full mode it is still per gateway instance, while durable routing and PostgreSQL/Redis mode checks remain the source for provider/fallback config.</action>
  <verify>
    <automated>go test ./internal/gateway -run "Fallback|CircuitBreaker|StrictOverride"</automated>
    <automated>go test ./internal/controlstate -run "Runtime|CircuitBreaker|Fallback"</automated>
  </verify>
  <done>Fallback-chain behavior is tested with closed/open/half-open breaker states.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: circuit breaker state is in-process and works with SQLite durable routing config.
Full mode: circuit breaker state is in-process per gateway instance; durable provider/routing config and Redis config-change propagation still coordinate which providers are eligible.
Disabled mode: breaker can protect static local providers, but no production durable semantics are claimed.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider failures -> breaker state | Upstream outcomes change which providers can receive traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Denial of Service | circuit breaker | mitigate | Threshold and half-open tests prevent permanently routing to repeatedly failing providers. |
| T-04-03-02 | Tampering | strict override | mitigate | `X-Route-To` cannot bypass an open circuit; it receives structured unavailable instead. |
| T-04-03-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/gateway -run "CircuitBreaker|Fallback|StrictOverride"`.
Run `go test ./...`.
</verification>

<success_criteria>
CB-01 is covered by real circuit-breaker and fallback-chain behavior with closed/open/half-open tests.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-03-SUMMARY.md` when done.
</output>
