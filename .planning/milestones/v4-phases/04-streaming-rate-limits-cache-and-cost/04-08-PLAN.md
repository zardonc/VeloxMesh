---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 08
type: execute
wave: 8
depends_on:
  - 04-07
files_modified:
  - internal/controlstate/types.go
  - internal/controlstate/repository.go
  - internal/controlstate/migrations/sqlite/0001_control_state.sql
  - internal/controlstate/migrations/postgres/0001_control_state.sql
  - internal/controlstate/sqlite/repository.go
  - internal/controlstate/sqlite/repository_test.go
  - internal/controlstate/postgres/repository.go
  - internal/controlstate/postgres/repository_test.go
autonomous: true
requirements:
  - COST-01
user_setup: []
must_haves:
  truths:
    - "D-27/D-28: provider/model credit rates are durable integer credits per 1K input/output tokens."
    - "D-35/D-37: usage records can represent settled and missing_usage/unsettled outcomes."
    - "Full-mode cost safety uses PostgreSQL transactions and row locks for balance mutation in later settlement."
  artifacts:
    - path: "internal/controlstate/types.go"
      provides: "ProviderModelRate and settlement-aware UsageRecord fields"
    - path: "internal/controlstate/repository.go"
      provides: "rate and usage repository contracts"
    - path: "internal/controlstate/postgres/repository.go"
      provides: "PostgreSQL storage APIs prepared for transactional settlement"
  key_links:
    - from: "internal/gateway/service.go"
      to: "internal/controlstate/repository.go"
      via: "rate lookup and usage logging in settlement plan"
      pattern: "ProviderModelRate|UsageRecord"
---

<objective>
Start the fourth D-02 theme: usage and rates, beginning with durable storage contracts.

Purpose: create real rate and usage persistence before Admin APIs and settlement rely on it.
Output: provider/model rate table, settlement-aware usage records, SQLite/PostgreSQL repository methods, and tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-07-SUMMARY.md
@internal/controlstate/types.go
@internal/controlstate/repository.go
@internal/controlstate/sqlite/repository.go
@internal/controlstate/postgres/repository.go
</context>

## Decision Coverage
D-02 D-13 D-27 D-28 D-33 D-34 D-35 D-36 D-37 D-38 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 COST-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist Provider-Model Credit Rates</name>
  <files>internal/controlstate/types.go, internal/controlstate/repository.go, internal/controlstate/migrations/sqlite/0001_control_state.sql, internal/controlstate/migrations/postgres/0001_control_state.sql, internal/controlstate/sqlite/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/postgres/repository.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - SQLite can save/get/delete rate keyed by provider ID and model.
    - PostgreSQL has equivalent SQL and opt-in integration coverage.
    - Rates are non-negative integers: input credits per 1K and output credits per 1K.
    - Missing rate is distinguishable from zero-rate.
  </behavior>
  <action>Add a narrow `ProviderModelRate` type and repository methods for durable rate lookup/save/delete per D-27. Use integer columns only per D-28. Do not add currency, floating-point prices, provider-level quotas, model degradation, budget enforcement, or pricing defaults.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "Rate|ProviderModel"</automated>
    <automated>go test ./internal/controlstate/postgres -run "Rate|ProviderModel|SQLShape|Integration"</automated>
  </verify>
  <done>Provider/model credit rates persist durably as integer per-1K token rates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist Settlement-Aware Usage Records</name>
  <files>internal/controlstate/types.go, internal/controlstate/repository.go, internal/controlstate/migrations/sqlite/0001_control_state.sql, internal/controlstate/migrations/postgres/0001_control_state.sql, internal/controlstate/sqlite/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/postgres/repository.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - Usage records include API key reference/hash, provider, model, prompt tokens, response tokens, total tokens, duration, timestamp, input/output rates, credits consumed, and settlement status.
    - Missing usage can be recorded as `missing_usage` or `unsettled` without guessed token counts.
    - PostgreSQL repository exposes or documents transactional settlement helper shape for 04-10 using `SELECT ... FOR UPDATE`.
    - Usage records never store raw prompts, upstream payloads, bearer tokens, or provider secrets.
  </behavior>
  <action>Extend existing `UsageRecord` and repository methods into real persistence per D-13 and D-35. Add settlement status values for `settled`, `missing_usage`, `missing_rate`, and `unsettled`. For full mode, specify PostgreSQL settlement will run in one transaction that locks the API-key balance row with `SELECT ... FOR UPDATE`, writes the usage record, updates balance, and commits; SQLite can use a write transaction for local single-process lite mode. Do not implement gateway settlement yet.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "Usage|Settlement"</automated>
    <automated>go test ./internal/controlstate/postgres -run "Usage|Settlement|Transaction|SQLShape|Integration"</automated>
  </verify>
  <done>Durable storage can represent settled and unsettled usage safely.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: SQLite stores rates and usage locally with single-process transaction semantics.
Full mode: PostgreSQL stores rates, balances, and usage; balance mutation must use transaction plus row lock in 04-10.
Disabled mode: rates and usage settlement are unavailable and fail closed.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin rate config -> storage | Admin-provided rate values become billing inputs. |
| provider usage -> durable records | Provider usage metadata becomes accounting data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | rates | mitigate | Non-negative integer validation and provider/model keys per D-27/D-28. |
| T-04-08-02 | Repudiation | usage records | mitigate | Persist timestamp, provider, model, tokens, rates, credits, and status per D-35. |
| T-04-08-03 | Information Disclosure | usage records | mitigate | Do not store prompts, provider payloads, bearer tokens, or secrets per D-42. |
| T-04-08-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/controlstate/sqlite ./internal/controlstate/postgres`.
Run `rg -n "prompt|raw|Authorization|Bearer|secret|api_key" internal/controlstate` and confirm matches are field names, redaction, or negative assertions only.
</verification>

<success_criteria>
COST-01 has durable rate and usage storage ready for Admin management and gateway settlement.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-08-SUMMARY.md` when done.
</output>
