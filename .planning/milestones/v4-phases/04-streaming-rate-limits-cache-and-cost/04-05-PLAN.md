---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/http/handlers/chat.go
  - internal/http/handlers/chat_test.go
  - tests/integration/chat_test.go
autonomous: true
requirements:
  - STRM-01
user_setup: []
must_haves:
  truths:
    - "D-19: `stream:true` returns OpenAI-compatible SSE through the gateway."
    - "D-20/D-21: fallback is allowed only before first stream bytes are written."
    - "D-22: client disconnect cancels upstream work promptly."
  artifacts:
    - path: "internal/gateway/service.go"
      provides: "stream orchestration and pre-first-event fallback"
    - path: "internal/http/handlers/chat.go"
      provides: "OpenAI-compatible SSE writer"
  key_links:
    - from: "internal/http/handlers/chat.go"
      to: "internal/gateway/service.go"
      via: "HandleChatCompletionStream"
      pattern: "text/event-stream|Flush|Stream"
---

<objective>
Finish the streaming theme by wiring gateway orchestration and HTTP SSE responses.

Purpose: make `stream:true` observable to clients while preserving fallback and cancellation safety.
Output: gateway stream execution, SSE handler behavior, and integration tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-04-SUMMARY.md
@internal/gateway/service.go
@internal/http/handlers/chat.go
</context>

## Decision Coverage
D-02 D-19 D-20 D-21 D-22 D-23 D-40 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 STRM-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate Streaming with Safe Fallback</name>
  <files>internal/gateway/service.go, internal/gateway/service_test.go</files>
  <behavior>
    - Gateway retries/falls back only when a stream-capable provider fails before first event.
    - After first event, stream errors do not switch providers or replay partial content.
    - Circuit breaker and health accounting record the pinned provider attempt once.
    - Client cancellation releases admission/health accounting once.
  </behavior>
  <action>Add `HandleChatCompletionStream` or equivalent to `gateway.Service`. Reuse routing, admission, circuit breaker, and fallback-chain behavior from non-streaming execution, but track first event delivery per D-20 and D-21. Unsupported streaming adapters fail before body bytes. Do not add settlement here; carry stream completion metadata so 04-10 can settle after stream completion.</action>
  <verify>
    <automated>go test ./internal/gateway -run "Stream|Fallback|CircuitBreaker|Cancel"</automated>
  </verify>
  <done>Gateway stream orchestration respects fallback, breaker, and cancellation rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write OpenAI-Compatible SSE Response</name>
  <files>internal/http/handlers/chat.go, internal/http/handlers/chat_test.go, tests/integration/chat_test.go</files>
  <behavior>
    - `stream:true` returns `Content-Type: text/event-stream`, `Cache-Control: no-cache`, and OpenAI-compatible `chat.completion.chunk` events.
    - Handler emits `[DONE]` after completion.
    - `X-Cache-Hit:false` and `X-Cache-Level:none` remain on streaming and non-streaming responses before semantic cache is introduced.
    - If `http.Flusher` is unavailable, handler returns structured error before writing bytes.
  </behavior>
  <action>Update `ChatHandler.ChatCompletions` so streaming requests use the service stream path per D-19. Write secret-safe SSE chunks only; do not include raw provider events or prompts. Preserve non-streaming JSON behavior. Keep cache as explicit miss headers per D-40 until 04-11/04-12.</action>
  <verify>
    <automated>go test ./internal/http/handlers -run "Chat|Streaming|SSE"</automated>
    <automated>go test ./tests/integration -run "ChatCompletions|Streaming|Cancel"</automated>
    <automated>go test ./...</automated>
  </verify>
  <done>`stream:true` works over SSE and non-streaming chat remains compatible.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: streaming works with SQLite durable provider config when the adapter supports streaming.
Full mode: streaming works with PostgreSQL durable provider config and Redis runtime coordination; Redis does not carry stream payloads.
Disabled mode: static local provider streaming works only if the adapter supports streaming.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway stream -> client | Incremental output is sent before full completion. |
| client disconnect -> upstream request | Cancellation must stop upstream work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | fallback after bytes | mitigate | Track first write and disallow provider switch after bytes per D-20/D-21. |
| T-04-05-02 | Denial of Service | abandoned streams | mitigate | Propagate request context cancellation and release accounting once per D-22. |
| T-04-05-03 | Information Disclosure | SSE payloads | mitigate | Emit normalized chunks only, no raw provider payloads or secrets per D-42. |
| T-04-05-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/gateway ./internal/http/handlers`.
Run `go test ./tests/integration -run "ChatCompletions|Streaming|Cancel"`.
Run `go test ./...`.
</verification>

<success_criteria>
SSE streaming proxy works through the gateway with safe pre-first-event fallback and cancellation.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-05-SUMMARY.md` when done.
</output>
