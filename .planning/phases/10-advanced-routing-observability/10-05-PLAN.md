---
phase: 10-advanced-routing-observability
plan: 05
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-02
  - 10-03
  - 10-04
files_modified:
  - internal/controlstate/runtime.go
  - internal/controlstate/runtime_test.go
  - internal/app/app.go
  - internal/app/app_test.go
  - internal/hotstate/hotstate.go
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/routing/router_test.go
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - OBS-01
  - OBS-02
requirements_addressed:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - "D-06: Below-threshold composite-score selection triggers the existing fallback chain before client failure."
    - "D-08/D-14: Runtime hot reload applies valid SQLite composite config and keeps the last known good config after invalid reloads."
    - "D-02: Provider/model cost rates feed the router only as a near-tie tie-breaker."
    - "D-05: Successful live provider/model requests warm composite-score selection."
    - "D-09/D-10/D-12: Gateway request outcomes update traces and Prometheus metrics without sensitive data."
  artifacts:
    - path: "internal/controlstate/runtime.go"
      provides: "RuntimeProviderManager passes validated composite config and costs into HealthAwareRouter"
      contains: "NewHealthAwareRouter"
    - path: "internal/app/app.go"
      provides: "Routing config reload validates and preserves last known good runtime snapshot"
      contains: "EventRouting"
    - path: "internal/gateway/service.go"
      provides: "Fallback, warm-up recording, metrics, and trace lifecycle integration"
      contains: "ErrCompositeScoreBelowThreshold"
    - path: "internal/hotstate/hotstate.go"
      provides: "Typed routing config change event"
      contains: "EventRouting"
  key_links:
    - from: "internal/app/app.go"
      to: "internal/controlstate/validation.go"
      via: "ValidateRoutingConfig before runtime activation"
      pattern: "ValidateRoutingConfig"
    - from: "internal/controlstate/runtime.go"
      to: "internal/routing/router.go"
      via: "validated composite config and provider/model costs"
      pattern: "Composite"
    - from: "internal/gateway/service.go"
      to: "internal/health/store.go"
      via: "RecordModelOutcome on live provider result"
      pattern: "RecordModel"
    - from: "internal/gateway/service.go"
      to: "internal/observability"
      via: "request outcome metrics and trace events"
      pattern: "cache_result"
---

<objective>
Integrate composite routing config, fallback safeguards, and observability into the live gateway path.

Purpose: Close the cross-package behavior that cannot be proven inside isolated router/config/observability packages: valid hot reload, last-known-good retention, score-threshold fallback, provider/model warm-up, cost tie-break inputs, and sanitized request lifecycle reporting.
Output: Runtime wiring and integration tests covering all Phase 10 requirements.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing-observability/10-CONTEXT.md
@.planning/phases/10-advanced-routing-observability/10-RESEARCH.md
@.planning/phases/09-redis-stack-qdrant-fallback-integration/09-04-SUMMARY.md
@internal/controlstate/runtime.go
@internal/app/app.go
@internal/hotstate/hotstate.go
@internal/gateway/service.go
@internal/gateway/service_test.go
@internal/routing/router.go
@internal/observability/metrics.go
@internal/observability/tracing.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire validated SQLite routing config into runtime</name>
  <files>internal/controlstate/runtime.go, internal/controlstate/runtime_test.go, internal/app/app.go, internal/app/app_test.go, internal/hotstate/hotstate.go</files>
  <action>Add a typed hotstate.EventRouting config-change event and handle it in App.StartConfigChangeSubscriber by reloading routing config without requiring a provider rebuild unless the existing runtime activation path requires one. In App.ReloadProviders, validate loaded RoutingConfig with active provider records, backend, and Redis state before activation. If validation or composite JSON loading fails, log the validation error and keep the current RuntimeProviderManager snapshot unchanged per D-16. Load provider/model rates from repo.Rates for active provider models and pass them into the routing.CompositeConfig conversion so D-02 cost tie-breaking uses existing billing rates. Convert controlstate.CompositeRoutingConfig plus optional cost overrides into routing.CompositeConfig in runtime wiring, and keep nil config equivalent to DefaultCompositeConfig for composite-score only. Do not add BFF/Admin UI endpoints.</action>
  <verify>
    <automated>timeout 60s go test ./internal/app ./internal/controlstate</automated>
  </verify>
  <done>Valid composite config hot reloads into RuntimeProviderManager, invalid/risky config leaves the prior snapshot active, provider/model cost rates reach the router, and existing provider/combo/semantic-rule reload behavior still passes tests.</done>
</task>

<task type="auto">
  <name>Task 2: Connect threshold fallback, warm-up recording, and sanitized outcomes</name>
  <files>internal/gateway/service.go, internal/gateway/service_test.go, internal/routing/router_test.go</files>
  <action>Teach the gateway attempt loop to treat ErrCompositeScoreBelowThreshold as a fallback-triggering routing miss when fallback is enabled: mark the selected low-score provider unavailable only if the router exposes one safely, otherwise continue through the existing SelectExcluding loop without returning to the client until attempts are exhausted. Record provider/model live outcomes through the health.Store method from Plan 01 after each adapter result so D-05 and D-08 warm-up is based on successful live requests. Ensure request outcome metrics include provider, model, strategy, status, cache_result, and error_category only, and trace events include fallback reason without raw error messages or sensitive payloads per D-09, D-10, and D-12.</action>
  <verify>
    <automated>timeout 60s go test ./internal/gateway ./internal/routing ./internal/observability</automated>
  </verify>
  <done>Low composite scores exercise the existing fallback chain, successful live requests warm provider/model routing, and cache/provider/fallback outcomes are reflected in metrics/traces with only approved labels and safe attributes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 10 integration gate</name>
  <files>internal/app/app_test.go, internal/gateway/service_test.go, internal/routing/router_test.go</files>
  <action>Add or extend focused integration-style tests that prove the full Phase 10 flow: default strategy behavior remains unchanged per D-13, opt-in composite-score selects via normalized multi-signal scoring, cost is only a tie-breaker, stale metrics become neutral while health still applies, invalid config is rejected with last known good retained, below-threshold scoring falls back, Prometheus labels stay allowlisted, and OTel traces do not contain prompts, auth headers, API keys, provider payloads, user IDs, or API-key IDs. Keep tests deterministic and under the 60-second backend unit-test limit.</action>
  <verify>
    <automated>timeout 60s go test ./internal/routing ./internal/health ./internal/controlstate ./internal/controlstate/sqlite ./internal/observability ./internal/gateway ./internal/app ./internal/http</automated>
  </verify>
  <done>All ROUT-01, ROUT-02, ROUT-03, OBS-01, and OBS-02 acceptance behaviors pass in targeted tests without exceeding the 60-second backend unit-test timeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite routing config -> live runtime | Operator-controlled durable config can change routing behavior. |
| gateway request -> metrics/traces | Untrusted request data is near observability instrumentation. |
| billing rates -> router | Cost data affects tie-break decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | hot reload routing config | mitigate | Validate before activation and retain last known good runtime snapshot on errors per D-16. |
| T-10-14 | Denial of Service | fallback loop | mitigate | Bound attempts with existing fallback config; low-score fallback cannot loop beyond MaxAttempts. |
| T-10-15 | Information Disclosure | gateway service observability | mitigate | Record only D-10 labels and D-12 safe trace attributes through centralized helpers. |
| T-10-16 | Tampering | cost tie-break inputs | mitigate | Use existing provider/model rates and validated overrides; cost only applies within near-tie threshold per D-02. |
| T-10-SC | Tampering | package supply chain | accept | No new external package install in this plan. |
</threat_model>

<verification>
Run `timeout 60s go test ./internal/routing ./internal/health ./internal/controlstate ./internal/controlstate/sqlite ./internal/observability ./internal/gateway ./internal/app ./internal/http`.
</verification>

<success_criteria>
The phase is complete when composite-score is opt-in, validated SQLite routing config hot-reloads safely, invalid config preserves last known good behavior, low scores trigger fallback, provider/model warm-up is based on successful live requests, Prometheus exposes only D-10 labels, OpenTelemetry exposes only D-12-safe request lifecycle data, and all Phase 10 requirement tests pass under the 60-second backend unit-test timeout.
</success_criteria>

<output>
Create `.planning/phases/10-advanced-routing-observability/10-05-SUMMARY.md` when done.
</output>
