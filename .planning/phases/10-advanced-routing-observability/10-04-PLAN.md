---
phase: 10-advanced-routing-observability
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - go.mod
  - go.sum
  - internal/observability/tracing.go
  - internal/observability/tracing_test.go
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/app/app.go
autonomous: true
requirements:
  - OBS-01
requirements_addressed:
  - OBS-01
user_setup:
  - service: opentelemetry-collector
    why: "Receives exported traces outside local unit tests."
    env_vars:
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        source: "Local or deployed OpenTelemetry collector endpoint."
must_haves:
  truths:
    - "OBS-01: Operators can inspect OpenTelemetry traces for TTFT, TPOT, E2E latency, and cache-hit behavior."
    - "D-09: Traces capture routing decision, selected provider, score summary, TTFT, TPOT, E2E, cache hit/miss, provider outcome, and fallback reason."
    - "D-11: Selected-provider score summary is trace-only and not per-candidate metrics."
    - "D-12: Trace attributes are strictly sanitized and contain only safe generated request IDs when present."
  artifacts:
    - path: "internal/observability/tracing.go"
      provides: "OpenTelemetry setup and sanitized lifecycle span helpers"
      exports: ["SetupTracing", "StartRequestTrace"]
    - path: "internal/gateway/service.go"
      provides: "Request lifecycle span events for non-streaming and streaming flows"
      contains: "StartRequestTrace"
    - path: "internal/app/app.go"
      provides: "OTel setup at gateway startup when configured through environment"
      contains: "SetupTracing"
  key_links:
    - from: "internal/gateway/service.go"
      to: "internal/observability/tracing.go"
      via: "sanitized span helper"
      pattern: "routing.decision"
    - from: "internal/routing/router.go"
      to: "internal/observability/tracing.go"
      via: "RoutingDecision.CompositeScoreSummary attributes"
      pattern: "score"
---

<objective>
Add sanitized OpenTelemetry request lifecycle traces.

Purpose: Satisfy OBS-01 and D-09 through D-12 using the existing gateway service path and the selected-provider RoutingDecision from Plan 01.
Output: OTel setup, lifecycle span helpers, gateway service instrumentation, and sanitization tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing-observability/10-CONTEXT.md
@.planning/phases/10-advanced-routing-observability/10-RESEARCH.md
@.planning/phases/08-semantic-pipeline/08-01-SUMMARY.md
@internal/observability/metrics.go
@internal/gateway/service.go
@internal/gateway/service_test.go
@internal/app/app.go
@internal/http/middleware/requestid.go
@go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenTelemetry setup and safe span helpers</name>
  <files>go.mod, go.sum, internal/observability/tracing.go, internal/observability/tracing_test.go, internal/app/app.go</files>
  <action>Add the researched OpenTelemetry Go modules. Implement observability setup that uses the global no-op tracer when OTEL_EXPORTER_OTLP_ENDPOINT is absent, and configures an OTLP trace exporter when that environment variable is present. Use environment variables, not source-coded deployment endpoints or secrets. Add helper functions that accept a narrow safe event struct for request lifecycle traces: generated request ID, provider, model, strategy, status, cache_result, error_category, fallback_reason, selected-provider score summary, and timing values. Attribute builders must reject or omit forbidden fields from D-12: prompts, messages, auth headers, API keys, provider payloads, user IDs, API-key IDs, and arbitrary error strings. Wire SetupTracing into App.New and close/flush it on Run context cancellation without changing startup behavior when tracing is not configured.</action>
  <verify>
    <automated>timeout 60s go test ./internal/observability ./internal/app</automated>
  </verify>
  <done>Tracing setup is no-op by default, OTLP export is opt-in through environment, app startup still works without a collector, and tests verify forbidden attribute names and values are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument gateway request lifecycle</name>
  <files>internal/gateway/service.go, internal/gateway/service_test.go, internal/observability/tracing_test.go</files>
  <action>Instrument HandleChatCompletion and the streaming path with one lifecycle span per gateway request. Record events or attributes for routing decision, selected provider, strategy, selected score summary from Plan 01, cache hit/miss, provider outcome, fallback reason, TTFT, TPOT, and E2E latency per D-09. For streaming, record TTFT on the first successful streamed event and E2E/TPOT when the stream finishes; do not treat setup latency as TTFT. Use generated safe request IDs from existing middleware/request conversion when available; never copy user identifiers or API-key IDs into trace attributes per D-12. Keep provider-specific payloads behind adapters and do not serialize request/response bodies into spans.</action>
  <verify>
    <automated>timeout 60s go test ./internal/gateway ./internal/observability</automated>
  </verify>
  <done>Gateway tests observe sanitized lifecycle span data for cache hit/miss, routing, provider outcome, fallback reason, TTFT, TPOT, and E2E timing, with no raw prompt, identity, auth, API key, or provider payload leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> OTel collector | Request lifecycle metadata crosses into external observability infrastructure. |
| HTTP request -> trace attributes | Untrusted request data is near trace creation code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure | OTel attributes/events | mitigate | Centralized allowlisted span helpers and tests for D-12 forbidden keys/values. |
| T-10-11 | Denial of Service | trace event volume | mitigate | Emit one request span and bounded lifecycle events; no per-candidate spans or payload serialization per D-11. |
| T-10-12 | Spoofing | request ID attribute | mitigate | Use middleware-generated or internally generated safe request IDs only; do not trust user identity labels. |
| T-10-SC | Tampering | Go module install | mitigate | Install only researched official OpenTelemetry modules; run go test after go.mod/go.sum update. |
</threat_model>

<verification>
Run `timeout 60s go test ./internal/observability ./internal/gateway ./internal/app`.
</verification>

<success_criteria>
OBS-01 is covered for non-streaming and streaming requests. D-09 lifecycle fields are present, D-11 score visibility stays trace-only for the selected provider, and D-12 sanitization has automated coverage.
</success_criteria>

<output>
Create `.planning/phases/10-advanced-routing-observability/10-04-SUMMARY.md` when done.
</output>
