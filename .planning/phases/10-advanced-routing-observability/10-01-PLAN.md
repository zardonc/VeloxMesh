---
phase: 10-advanced-routing-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/health/store.go
  - internal/health/redis_store.go
  - internal/health/store_test.go
  - internal/health/redis_store_test.go
  - internal/routing/router.go
  - internal/routing/composite.go
  - internal/routing/composite_test.go
  - internal/routing/router_test.go
  - internal/errors/errors.go
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
requirements_addressed:
  - ROUT-01
  - ROUT-02
  - ROUT-03
must_haves:
  truths:
    - "D-13: Existing default routing behavior remains unchanged unless strategy is exactly composite-score."
    - "D-01/D-02: composite-score balances latency, pending requests, error rate, health, and uses cost only as a tie-breaker."
    - "D-03: Unhealthy providers are excluded and degraded providers remain eligible with a penalty."
    - "D-04/D-07: guarded z-score uses neutral stale metrics and falls back for tiny or flat pools."
    - "D-05/D-08: provider/model pairs use round-robin until warm-up success count is met."
    - "D-06: below-threshold composite score returns a distinct routing error for the gateway fallback loop."
  artifacts:
    - path: "internal/routing/composite.go"
      provides: "Pure composite scoring helper and selected-provider score summary"
      exports: ["CompositeConfig", "CompositeScoreSummary", "DefaultCompositeConfig", "SelectComposite"]
    - path: "internal/routing/router.go"
      provides: "Opt-in composite-score strategy branch in HealthAwareRouter"
      contains: "composite-score"
    - path: "internal/health/store.go"
      provides: "Provider/model warm-up counters for live successful requests"
      contains: "ModelSnapshot"
    - path: "internal/errors/errors.go"
      provides: "Below-threshold routing error for fallback handling"
      contains: "composite_score_below_threshold"
  key_links:
    - from: "internal/routing/router.go"
      to: "internal/routing/composite.go"
      via: "HealthAwareRouter composite-score strategy branch"
      pattern: "SelectComposite"
    - from: "internal/routing/composite.go"
      to: "internal/health/store.go"
      via: "provider/model warm-up snapshot"
      pattern: "ModelSnapshot"
---

<objective>
Create the opt-in Composite Score Router core.

Purpose: Satisfy ROUT-01, ROUT-02, and ROUT-03 with a pure scoring helper behind the existing HealthAwareRouter seam, preserving default round-robin and least-latency behavior per D-13.
Output: Composite scoring code, provider/model warm-up state, routing decision score summary, and focused tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing-observability/10-CONTEXT.md
@.planning/phases/10-advanced-routing-observability/10-RESEARCH.md
@internal/routing/router.go
@internal/routing/router_test.go
@internal/health/store.go
@internal/health/redis_store.go
@internal/errors/errors.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider/model warm-up snapshots</name>
  <files>internal/health/store.go, internal/health/redis_store.go, internal/health/store_test.go, internal/health/redis_store_test.go</files>
  <action>Add provider/model live request counters to the existing health.Store without changing EndRequest semantics. Introduce a small ModelSnapshot value containing provider ID, model, total successes, total failures, and last updated timestamp. Add Store methods for recording a provider/model outcome and reading a provider/model snapshot. In in-memory and Redis stores, update these counters from a new explicit method, not from probe results, so D-05 and D-08 warm-up is based on successful live requests only. Keep unknown provider/model snapshots at zero successes, and preserve existing provider-level Snapshot and Snapshots behavior.</action>
  <verify>
    <automated>timeout 60s go test ./internal/health</automated>
  </verify>
  <done>Provider/model counters are available through the health.Store interface, Redis and in-memory stores agree on behavior, probe-only activity does not warm a model, and existing health tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement composite-score selection</name>
  <files>internal/routing/composite.go, internal/routing/composite_test.go, internal/routing/router.go, internal/routing/router_test.go, internal/errors/errors.go</files>
  <action>Create a pure composite scoring helper and wire it as an opt-in HealthAwareRouter strategy named composite-score per D-13. The helper must score latency, pending requests, error rate, and health per D-01, apply cost only after the non-cost score is within the configured near-tie threshold per D-02, hard-exclude unhealthy providers through the existing getHealthyProviders path and penalize degraded providers per D-03, use guarded z-score only when count and variance are sufficient per D-04, treat stale latency/error/load metrics as neutral while health still applies per D-07, round-robin until every eligible provider/model reaches the configured warm-up success count per D-05 and D-08, and return ErrCompositeScoreBelowThreshold when the best score is below threshold per D-06. Extend RoutingDecision with a selected-provider CompositeScoreSummary only; do not expose per-candidate metrics. Keep existing constructor behavior by defaulting to conservative composite config when no explicit config is passed.</action>
  <verify>
    <automated>timeout 60s go test ./internal/routing ./internal/health</automated>
  </verify>
  <done>composite-score selects through HealthAwareRouter only when configured, round-robin and least-latency tests still pass, cost cannot make a materially worse candidate win, stale signals are neutral, tiny/flat pools use fallback normalization, and low scores return the distinct fallback-trigger error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routing config -> router | Operator-controlled strategy/config changes influence provider selection. |
| health store -> router | Hot health data influences routing but may be stale or incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | composite config | mitigate | Plan 02 validates bounds; this plan treats invalid zero values through DefaultCompositeConfig and tests conservative behavior. |
| T-10-02 | Denial of Service | z-score math | mitigate | Guard candidate count and variance; tiny or flat pools use min/max or neutral scoring per D-04. |
| T-10-03 | Information Disclosure | RoutingDecision | mitigate | Store only selected-provider score summary; no prompts, auth headers, API keys, user IDs, or provider payloads per D-11/D-12. |
| T-10-SC | Tampering | package supply chain | accept | No new external package install in this plan. |
</threat_model>

<verification>
Run `timeout 60s go test ./internal/routing ./internal/health`.
</verification>

<success_criteria>
ROUT-01, ROUT-02, and ROUT-03 have runnable unit coverage. The default routing strategy remains whatever current config supplies; composite-score is reachable only by explicit strategy selection. All D-01 through D-08 and D-13 scoring decisions are represented in tests.
</success_criteria>

<output>
Create `.planning/phases/10-advanced-routing-observability/10-01-SUMMARY.md` when done.
</output>
