---
phase: 10-advanced-routing-observability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/observability/metrics.go
  - internal/observability/prometheus.go
  - internal/observability/prometheus_test.go
  - internal/http/router.go
  - internal/http/router_observability_test.go
autonomous: true
requirements:
  - OBS-02
requirements_addressed:
  - OBS-02
must_haves:
  truths:
    - "OBS-02: Operators can scrape Prometheus histograms for routing and request timing."
    - "D-10: Prometheus labels are limited to provider, model, strategy, status, cache_result, and error_category."
    - "D-11: Score breakdowns are not emitted as per-candidate Prometheus metrics."
    - "D-12: Metrics contain no raw prompts, auth headers, API keys, provider payloads, user IDs, or request IDs."
  artifacts:
    - path: "internal/observability/prometheus.go"
      provides: "Prometheus Metrics implementation and scrape handler"
      exports: ["NewPrometheusMetrics"]
    - path: "internal/observability/metrics.go"
      provides: "Low-cardinality metrics interface for request outcome, routing, health, and timing"
      contains: "cache_result"
    - path: "internal/http/router.go"
      provides: "Prometheus scrape route"
      contains: "/metrics"
  key_links:
    - from: "internal/http/router.go"
      to: "internal/observability/prometheus.go"
      via: "promhttp handler"
      pattern: "metrics"
    - from: "internal/gateway/service.go"
      to: "internal/observability/metrics.go"
      via: "existing DefaultMetrics calls"
      pattern: "RecordRequestOutcome"
---

<objective>
Replace stub request metrics with a real Prometheus implementation.

Purpose: Satisfy OBS-02 and D-10 through a scrapeable metrics endpoint that keeps labels bounded and sanitized.
Output: Prometheus metrics implementation, /metrics route, and tests enforcing the label allowlist.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing-observability/10-CONTEXT.md
@.planning/phases/10-advanced-routing-observability/10-RESEARCH.md
@internal/observability/metrics.go
@internal/http/router.go
@internal/gateway/service.go
@go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prometheus metrics implementation</name>
  <files>go.mod, go.sum, internal/observability/metrics.go, internal/observability/prometheus.go, internal/observability/prometheus_test.go</files>
  <action>Add github.com/prometheus/client_golang at the researched version. Extend the Metrics interface only as needed to record request duration, provider latency, routing decisions, health status, and request outcome with cache_result and error_category. Implement NewPrometheusMetrics using a caller-provided prometheus.Registerer so tests can use an isolated registry. Define histograms for end-to-end request duration and provider timing, plus counters/gauges where existing methods need them. Every metric vector must use only the D-10 labels: provider, model, strategy, status, cache_result, and error_category. Do not include request IDs, user IDs, API-key IDs, route overrides, raw prompts, headers, or provider payload fields per D-12. Keep StubMetrics behavior for tests that do not install Prometheus metrics.</action>
  <verify>
    <automated>timeout 60s go test ./internal/observability</automated>
  </verify>
  <done>Prometheus metrics register cleanly in an isolated registry, histograms observe request/provider timings, and tests fail if any metric exposes a label outside the D-10 allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Expose /metrics through the existing router</name>
  <files>internal/http/router.go, internal/http/router_observability_test.go</files>
  <action>Wire a GET /metrics endpoint on the existing Chi router using the observability Prometheus handler. Keep data-plane auth unchanged for /v1 routes and do not require admin auth for scraping unless an existing metrics auth pattern already exists. The endpoint must expose only Prometheus text output and must not include application config, raw requests, auth headers, or provider payloads. Add a router test that installs an isolated Prometheus registry, hits /metrics, and asserts a Prometheus response without requiring a running Prometheus server.</action>
  <verify>
    <automated>timeout 60s go test ./internal/observability ./internal/http</automated>
  </verify>
  <done>/metrics is scrapeable from the gateway router, does not disturb existing authenticated routes, and the scrape output uses only approved low-cardinality labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Prometheus scraper | Runtime request metadata crosses into an unauthenticated scrape surface. |
| metric labels -> Prometheus TSDB | Label values influence series cardinality and storage load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | metrics labels | mitigate | Enforce D-10 label allowlist and D-12 forbidden fields in tests. |
| T-10-08 | Denial of Service | Prometheus cardinality | mitigate | Do not label by request, user, API key, prompt, route override, or error message; use bounded error_category only. |
| T-10-09 | Spoofing | status labels | mitigate | Derive status from gateway outcome categories, not raw upstream response bodies. |
| T-10-SC | Tampering | Go module install | mitigate | Install only researched official Prometheus Go client; run go test after go.mod/go.sum update. |
</threat_model>

<verification>
Run `timeout 60s go test ./internal/observability ./internal/http`.
</verification>

<success_criteria>
OBS-02 is covered by a scrapeable /metrics endpoint and Prometheus histograms. D-10, D-11, and D-12 are enforced by automated tests.
</success_criteria>

<output>
Create `.planning/phases/10-advanced-routing-observability/10-03-SUMMARY.md` when done.
</output>
