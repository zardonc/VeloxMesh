---
phase: 10-advanced-routing-observability
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - internal/controlstate/types.go
  - internal/controlstate/validation.go
  - internal/controlstate/validation_test.go
  - internal/controlstate/sqlite/repository.go
  - internal/controlstate/sqlite/repository_test.go
  - internal/controlstate/sqlite/migrations.go
  - internal/controlstate/migrations/sqlite/0006_routing_composite.sql
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
requirements_addressed:
  - ROUT-01
  - ROUT-02
  - ROUT-03
must_haves:
  truths:
    - "D-14: SQLite routing config stores strategy, weights, thresholds, warm-up count, stale window, preset name, and optional overrides."
    - "D-15: A conservative preset is available and explicit overrides can replace preset values."
    - "D-16: Invalid or risky composite routing config is rejected instead of silently clamped."
    - "D-13: composite-score is accepted as an opt-in strategy beside existing round-robin and least-latency."
  artifacts:
    - path: "internal/controlstate/types.go"
      provides: "Composite routing config structs on RoutingConfig"
      contains: "CompositeRoutingConfig"
    - path: "internal/controlstate/validation.go"
      provides: "Structured validation for routing and composite config"
      contains: "ValidateRoutingConfig"
    - path: "internal/controlstate/migrations/sqlite/0006_routing_composite.sql"
      provides: "SQLite columns for composite routing config"
    - path: "internal/controlstate/sqlite/repository.go"
      provides: "SQLite read/write of composite routing config"
      contains: "routingRepo"
  key_links:
    - from: "internal/controlstate/sqlite/repository.go"
      to: "internal/controlstate/types.go"
      via: "RoutingConfig.Composite JSON scan/save"
      pattern: "Composite"
    - from: "internal/controlstate/validation.go"
      to: "internal/routing/composite.go"
      via: "matching bounds and conservative preset semantics"
      pattern: "composite-score"
---

<objective>
Persist and validate Composite Score Router config in SQLite.

Purpose: Implement D-14, D-15, D-16, and the opt-in strategy portion of D-13 without adding an admin UI surface.
Output: RoutingConfig schema, SQLite migration/repository support, and validation tests for safe composite routing activation.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-advanced-routing-observability/10-CONTEXT.md
@.planning/phases/10-advanced-routing-observability/10-RESEARCH.md
@.planning/phases/07-adapter-interfaces-sqlite-foundation/07-SUMMARY.md
@.planning/phases/08-semantic-pipeline/08-01-SUMMARY.md
@internal/controlstate/types.go
@internal/controlstate/validation.go
@internal/controlstate/sqlite/repository.go
@internal/controlstate/sqlite/migrations.go
@internal/controlstate/sqlite/repository_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and validate composite routing config</name>
  <files>internal/controlstate/types.go, internal/controlstate/validation.go, internal/controlstate/validation_test.go</files>
  <action>Extend RoutingConfig with a Composite field containing preset name, signal weights, score threshold, near-tie threshold, degraded penalty, warm-up success count, stale window duration, minimum z-score candidates, minimum variance, and optional provider/model cost overrides. Accept strategy composite-score in ValidateRoutingConfig per D-13. Add validation that rejects negative or zero unsafe durations/counts, weights outside documented bounds, all-zero weights, score thresholds outside [0,1], near-tie thresholds outside [0,1], invalid preset names, malformed provider/model override keys, and cost overrides with negative rates. Return FieldError entries with stable field names; do not silently rewrite risky input per D-16. Keep existing priority validation behavior unchanged unless an existing test proves it is dead code.</action>
  <verify>
    <automated>timeout 60s go test ./internal/controlstate -run TestValidateRoutingConfig</automated>
  </verify>
  <done>Validation accepts round-robin, least-latency, and composite-score, rejects invalid composite configs with FieldError details, and existing routing validation tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Store composite config in SQLite routing_config</name>
  <files>internal/controlstate/sqlite/repository.go, internal/controlstate/sqlite/repository_test.go, internal/controlstate/sqlite/migrations.go, internal/controlstate/migrations/sqlite/0006_routing_composite.sql</files>
  <action>Add a SQLite migration that extends the global routing config with a nullable composite config JSON column. Register the migration in internal/controlstate/sqlite/migrations.go after 0005. Update routingRepo.Get and routingRepo.Save to round-trip RoutingConfig.Composite using encoding/json and parameterized SQL only. Missing composite JSON must load as nil, preserving existing configs. Invalid stored JSON must return an error so runtime activation can retain the last known good config in Plan 05. Add repository tests for nil composite config, full conservative preset config, and malformed persisted JSON.</action>
  <verify>
    <automated>timeout 60s go test ./internal/controlstate ./internal/controlstate/sqlite</automated>
  </verify>
  <done>SQLite migrations create the composite routing config storage, repository save/get round-trips all fields, nil remains backward-compatible, and invalid persisted JSON is not accepted as a live config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> runtime | Durable routing config crosses from operator-controlled storage into live request routing. |
| Admin/control-plane -> SQLite | Any future routing config writer must rely on structured validation before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | RoutingConfig.Composite | mitigate | Validate strategy, weights, thresholds, warm-up count, stale window, preset, and overrides before activation/save per D-16. |
| T-10-05 | Denial of Service | SQLite migration/repository | mitigate | Use nullable JSON column and parameterized SQL; malformed JSON returns error instead of activating. |
| T-10-06 | Repudiation | validation errors | mitigate | Return stable FieldError codes and fields so callers can audit rejection reason without logging sensitive data. |
| T-10-SC | Tampering | package supply chain | accept | No new external package install in this plan. |
</threat_model>

<verification>
Run `timeout 60s go test ./internal/controlstate ./internal/controlstate/sqlite`.
</verification>

<success_criteria>
Routing config persistence covers D-14, D-15, D-16, and the D-13 composite-score strategy name. Existing SQLite configs continue to load, and invalid composite config cannot become the active runtime config.
</success_criteria>

<output>
Create `.planning/phases/10-advanced-routing-observability/10-02-SUMMARY.md` when done.
</output>
