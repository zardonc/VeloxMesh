---
phase: 26-scheduler-scoring-backpressure-hardening
plan: "03"
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - internal/scheduler/intake.go
  - internal/scheduler/intake_test.go
  - internal/scheduler/executor.go
  - internal/scheduler/admin_scheduler_service.go
  - internal/http/handlers/admin_scheduler_test.go
  - cmd/scheduler/main.go
  - cmd/scheduler/main_test.go
  - internal/scheduler/features.go
  - internal/scheduler/features_test.go
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: ["SSBH-08"]
must_haves:
  truths:
    - "Scheduler queue admission limit checks and queue writes are serialized in the gateway process."
    - "Task handlers use the executor-provided context."
    - "Rollout PATCH reports successful memory changes even when rollup lookup is degraded."
    - "Predictor breaker tuning is wired from existing scheduler breaker env vars."
---

<objective>
Fix the six scheduler review findings from the 26-03 audit pass, with focused regression tests and real-component verification.
</objective>

<scope_review>

## Inputs

- Review findings from the current audit: bugs 1 through 6.
- External reference: `C:\Users\inthe\.gemini\antigravity-ide\brain\447463d1-daf7-400f-a3be-423a44717fad\fix_plan.md`.
- Existing architecture: Scheduler is optional, gateway-owned, low-latency, and single-node memory queueing is the default.

## Chosen Design

- Bug 1: add one `sync.Mutex` to `TaskIntake` and lock only `checkAdmission + Queue.Push`. This fixes the current single-process memory/Redis-node-scoped gateway path without changing every queue backend. Add a `ponytail:` comment noting Redis Lua/per-backend atomic push if cross-process queue limits become required.
- Bug 2: use the `runCtx` passed into task handlers for chat and stream execution.
- Bug 3: make rollout `Update` return the updated controller snapshot after validation; quality rollup lookup failure becomes a response warning, not an error after mutation.
- Bug 4: pass existing scheduler breaker env vars into `PythonClientConfig`.
- Bug 5: sync Phase 26 planning state docs.
- Bug 6: fix sentence delimiter handling for Chinese punctuation.

## Out of Scope

- New queue interface methods.
- Redis Lua admission script.
- Cross-node runtime rollout propagation.
- Automatic ONNX rollback.

</scope_review>

<tasks>

<task type="auto">
  <name>Task 1: Serialize scheduler admission and queue push</name>
  <files>internal/scheduler/intake.go, internal/scheduler/intake_test.go</files>
  <action>Add a narrow mutex around admission limit check and queue push. Add a concurrent Submit regression proving final queue length does not exceed the hard limit.</action>
  <verify>go test -timeout 60s ./internal/scheduler -run "TestTaskIntakeConcurrent|TestTaskIntakeSoftLimit|TestTaskIntakeHighPriority"</verify>
  <acceptance_criteria>
    - Concurrent Submit cannot exceed hard limit in one gateway process.
    - Existing soft-limit and high-priority behavior remains unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Use executor-provided task context</name>
  <files>internal/scheduler/executor.go, internal/scheduler/intake_test.go</files>
  <action>Pass handler `runCtx` through to `execute` for chat and streaming paths. Add focused tests that prove the executor context reaches both execute callbacks.</action>
  <verify>go test -timeout 60s ./internal/scheduler -run "TestSynchronousRunner.*Context"</verify>
  <acceptance_criteria>
    - Chat execution receives the handler context.
    - Stream execution receives the handler context.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Make rollout PATCH mutation/reporting consistent</name>
  <files>internal/scheduler/admin_scheduler_service.go, internal/http/handlers/admin_scheduler_test.go</files>
  <action>Add optional rollout response warnings. Validation failures still return 400 before mutation; rollup query failures after mutation return 200 with warning and audit as success_with_warning.</action>
  <verify>go test -timeout 60s ./internal/http/handlers -run TestAdminSchedulerRollout</verify>
  <acceptance_criteria>
    - Invalid PATCH does not mutate controller state.
    - Rollup query failure after a valid PATCH returns 200 and includes `quality_rollup_query_failed`.
    - Audit outcome is not `validation_failed` for post-mutation rollup lookup failure.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire predictor breaker env and fix sentence punctuation</name>
  <files>cmd/scheduler/main.go, cmd/scheduler/main_test.go, internal/scheduler/features.go, internal/scheduler/features_test.go</files>
  <action>Pass breaker failure threshold and recovery timeout env values into the Python predictor client config. Fix Chinese sentence punctuation detection.</action>
  <verify>go test -timeout 60s ./cmd/scheduler ./internal/scheduler -run "TestSchedulerServiceUsesPythonONNXWorkerSmoke|TestExtractSafeFeatures"</verify>
  <acceptance_criteria>
    - Real Python predictor smoke still passes.
    - Breaker env values affect predictor fallback behavior.
    - Chinese punctuation closes sentence buckets correctly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Sync planning state and run full verification</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>Record Phase 26/v7.8 as current completed scheduler hardening work, then run focused and full verification.</action>
  <verify>go test -timeout 60s ./internal/scheduler ./internal/http/handlers ./cmd/scheduler -count=1; go test -timeout 60s ./...; go build ./...; git diff --check</verify>
  <acceptance_criteria>
    - Tests use real package components, real SQLite admin repository, and real local Python predictor worker smoke.
    - No mock-only substitute is used for required real-component paths.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `go test -timeout 60s ./internal/scheduler -run "TestTaskIntakeConcurrent|TestSynchronousRunner.*Context|TestExtractSafeFeatures" -count=1`
- `go test -timeout 60s ./internal/http/handlers -run TestAdminSchedulerRollout -count=1`
- `go test -timeout 60s ./cmd/scheduler -run "TestSchedulerServiceUsesPythonONNXWorkerSmoke|TestSchedulerServicePredictorBreakerEnv" -count=1`
- `go test -timeout 60s ./internal/scheduler ./internal/http/handlers ./cmd/scheduler -count=1`
- `go test -timeout 60s ./...`
- `go build ./...`
- `git diff --check`
</verification>

<success_criteria>
- Bugs 1 through 6 are fixed.
- Scheduler 1.0 single-node queue backpressure is reliable under concurrent Submit.
- Rollout admin PATCH cannot falsely report failed validation after successful mutation.
- Predictor breaker settings are operator-tunable through existing env names.
- Phase 26 docs reflect completed v7.8 hardening.
</success_criteria>
