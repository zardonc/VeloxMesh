---
phase: 26-scheduler-scoring-backpressure-hardening
plan: "01"
type: execute
wave: 1
depends_on: ["20-01", "23-01", "25-01"]
files_modified:
  - internal/scheduler/client.go
  - internal/scheduler/client_test.go
  - internal/scheduler/predictor/breaker.go
  - internal/scheduler/predictor/proto.go
  - internal/scheduler/predictor/python_client.go
  - internal/scheduler/predictor/python_client_test.go
  - internal/config/config_types.go
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
  - cmd/scheduler/main.go
  - .env.example
  - config.json.example
  - config.scheduler.example.json
  - docs/scheduler-1.0-runbook.md
autonomous: true
requirements: ["SSBH-01", "SSBH-02", "SSBH-03", "SSBH-04"]
must_haves:
  truths:
    - "External scheduler and predictor scoring calls are bounded by non-blocking max concurrency."
    - "Slow successful scorer calls are recorded as degraded and fall back instead of blocking intake."
    - "Breaker logic uses a small window so one success does not erase recent failures."
    - "Operator docs state predictive scoring is an optimization path and should quick-fail to heuristic/FIFO."
  artifacts:
    - path: "internal/scheduler/client.go"
      provides: "GRPCScorer backpressure, slow-call fallback, and window breaker"
    - path: "internal/scheduler/predictor/python_client.go"
      provides: "Python ONNX predictor backpressure and slow-call fallback"
    - path: "docs/scheduler-1.0-runbook.md"
      provides: "operator guidance for scorer timeout and fallback policy"
---

<objective>
Protect Gateway intake throughput from slow or unhealthy external Scheduler/Python predictor scoring while preserving the existing synchronous score-before-enqueue model.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add scorer backpressure config</name>
  <files>internal/config/config_types.go, internal/config/config.go, internal/config/config_validation.go, internal/config/config_test.go, .env.example, config.json.example, config.scheduler.example.json</files>
  <read_first>
    - internal/config/config_types.go
    - internal/config/config.go
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - .env.example
    - config.scheduler.example.json
  </read_first>
  <action>Add `scorer_max_concurrency` and `scorer_slow_threshold` config fields with env support, defaults, validation, and examples.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - Env and JSON config can set max concurrency and slow threshold.
    - Default max concurrency is conservative.
    - Default slow threshold is no higher than scheduler timeout.
  </acceptance_criteria>
  <done>Operators can bound scorer concurrency and slow-call sensitivity.</done>
</task>

<task type="auto">
  <name>Task 2: Harden GRPCScorer</name>
  <files>internal/scheduler/client.go, internal/scheduler/client_test.go</files>
  <read_first>
    - internal/scheduler/client.go
    - internal/scheduler/client_test.go
    - internal/scheduler/intake.go
  </read_first>
  <action>Add non-blocking scorer slots, slow-success fallback, and sliding-window breaker behavior to the gateway-side gRPC scorer.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler -run "TestGRPCScorer|TestWeightedScorer"</automated>
  </verify>
  <acceptance_criteria>
    - Requests beyond max scorer concurrency return fallback immediately.
    - Slow successful responses return `scorer_slow` fallback and count against the breaker.
    - Alternating failure/success does not let one success clear recent failures.
    - ONNX scorer busy/slow fallbacks are treated as unusable and can fall back to heuristic.
  </acceptance_criteria>
  <done>Gateway scorer calls cannot pile up indefinitely behind a slow external scheduler.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Python ONNX predictor client</name>
  <files>internal/scheduler/predictor/breaker.go, internal/scheduler/predictor/proto.go, internal/scheduler/predictor/python_client.go, internal/scheduler/predictor/python_client_test.go, cmd/scheduler/main.go</files>
  <read_first>
    - internal/scheduler/predictor/python_client.go
    - internal/scheduler/predictor/breaker.go
    - internal/scheduler/predictor/noop.go
    - cmd/scheduler/main.go
  </read_first>
  <action>Add predictor max concurrency, slow-success fallback, window breaker behavior, and scheduler env wiring.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/predictor ./cmd/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - Busy predictor calls return per-task `ErrPredictorBusy` fallback.
    - Slow successful predictor calls return per-task `ErrPredictorSlow` fallback.
    - Breaker opens on failure-rate window and half-opens after recovery.
    - Tests use real local gRPC predictor server behavior.
  </acceptance_criteria>
  <done>Python predictor failures degrade through existing NoopPredictor path.</done>
</task>

<task type="auto">
  <name>Task 4: Document operator constraints and verify full build</name>
  <files>docs/scheduler-1.0-runbook.md</files>
  <read_first>
    - docs/scheduler-1.0-runbook.md
  </read_first>
  <action>Document predictive scoring as an optimization path, new backpressure config, short timeout guidance, and the async-scoring deferral.</action>
  <verify>
    <automated>go test -timeout 60s ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - Runbook includes new config fields and quick-fallback guidance.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Phase 26 is operator-visible and regression-checked.</done>
</task>

</tasks>

<verification>
Run:
- `go test -timeout 60s ./internal/config ./internal/scheduler ./internal/scheduler/predictor -count=1`
- `go test -timeout 60s ./...`
- `go build ./...`
- `git diff --check`
</verification>

<success_criteria>
- SSBH-01 through SSBH-04 are covered by code, tests, and docs.
- Slow or overloaded scorer calls use fallback instead of blocking Gateway intake.
- Breaker behavior resists intermittent-success masking.
- No new dependency is introduced.
</success_criteria>

## Artifacts this phase produces

- Scheduler scorer max concurrency config
- Scheduler scorer slow threshold config
- GRPCScorer busy/slow fallback behavior
- PythonONNXPredictorClient busy/slow fallback behavior
- Window-based scorer breaker tests
- Real gRPC slow/busy predictor validation tests
