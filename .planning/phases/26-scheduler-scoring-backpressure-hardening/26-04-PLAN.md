---
phase: 26-scheduler-scoring-backpressure-hardening
plan: "04"
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - internal/scheduler/result_registry.go
  - internal/scheduler/executor.go
  - internal/scheduler/intake.go
  - internal/scheduler/executor_test.go
  - internal/scheduler/features.go
  - internal/scheduler/features_test.go
  - internal/scheduler/client.go
  - internal/scheduler/client_test.go
  - internal/app/app.go
  - internal/app/app_test.go
autonomous: true
requirements: ["SSBH-08"]
must_haves:
  truths:
    - "A waiting request context must not cancel a different queued task it happens to execute."
    - "Explicit Redis scheduler queue mode uses the existing FallbackQueue for runtime Redis failures."
    - "Scheduler feature extraction must not undercount unspaced CJK text as a single token."
    - "WeightedScorer fallback results keep FIFO scheduler metadata."
    - "App background workers stop through the App lifecycle context."
---

<objective>
Fix the five scheduler/app risks confirmed in `C:\Users\inthe\.gemini\antigravity-ide\brain\fb90e712-c44f-4699-86c0-8f569dc1f44e\risk_verification.md` with existing components only.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Isolate task execution context from waiter context</name>
  <files>internal/scheduler/result_registry.go, internal/scheduler/executor.go, internal/scheduler/intake.go, internal/scheduler/executor_test.go</files>
  <action>Store a per-task execution context at submit/register time and make Executor.RunOne use it instead of deriving handler context from the caller that popped the queue item.</action>
  <verify>go test -timeout 60s ./internal/scheduler -run TestExecutorRunOneUsesRegisteredTaskContext -count=1</verify>
  <acceptance_criteria>
    - Canceling the RunOne caller context after a handler starts does not cancel the task handler context.
    - Task context cancellation is still available through registry unregister/cancel cleanup.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Use FallbackQueue in Redis scheduler queue mode</name>
  <files>internal/app/app.go, internal/app/app_test.go</files>
  <action>Wrap the Redis scheduler queue with the existing scheduler.NewFallbackQueue(redisQueue, memoryQueue) and report the backend as redis+fallback.</action>
  <verify>go test -timeout 60s ./internal/app -run TestNewSchedulerQueueExplicitRedisIsNodeScoped -count=1</verify>
  <acceptance_criteria>
    - Explicit Redis queue startup returns a FallbackQueue backed by the real RedisQueue and MemoryQueue.
    - Startup failure before Redis ping still degrades to plain memory queue.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix unspaced CJK feature extraction</name>
  <files>internal/scheduler/features.go, internal/scheduler/features_test.go</files>
  <action>Replace direct strings.Fields use with a minimal tokenizer that keeps whitespace-delimited text unchanged and treats contiguous CJK runes as token-like units when no spaces exist.</action>
  <verify>go test -timeout 60s ./internal/scheduler -run TestExtractSafeFeaturesUnspacedCJK -count=1</verify>
  <acceptance_criteria>
    - A real Chinese sentence without spaces produces more than one estimated input token.
    - Sentence bucket and vocabulary bucket still use the same token slice.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Preserve FIFO metadata for weighted scorer fallbacks</name>
  <files>internal/scheduler/client.go, internal/scheduler/client_test.go</files>
  <action>Only overwrite SchedulerType in scoreIndexed when the returned result has no FallbackReason.</action>
  <verify>go test -timeout 60s ./internal/scheduler -run TestWeightedScorerONNXFailureFallsBackToHeuristicThenFIFO -count=1</verify>
  <acceptance_criteria>
    - Scorer error fallback results stay SchedulerTypeFIFO.
    - Healthy heuristic and ONNX results keep their expected SchedulerType labels.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Stop app background workers through App lifecycle</name>
  <files>internal/app/app.go, internal/app/app_test.go</files>
  <action>Add an App-owned context cancel function, pass that context to replication workers and config subscriber startup, and call it from Run shutdown and a Close method.</action>
  <verify>go test -timeout 60s ./internal/app -run TestAppCloseCancelsLifecycle -count=1</verify>
  <acceptance_criteria>
    - App.Close cancels the lifecycle context.
    - Run cancellation also cancels lifecycle context before returning.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `go test -timeout 60s ./internal/scheduler -run "TestExecutorRunOneUsesRegisteredTaskContext|TestExtractSafeFeaturesUnspacedCJK|TestWeightedScorerONNXFailureFallsBackToHeuristicThenFIFO" -count=1`
- `go test -timeout 60s ./internal/app -run "TestNewSchedulerQueueExplicitRedisIsNodeScoped|TestAppCloseCancelsLifecycle" -count=1`
- `go test -timeout 60s ./internal/scheduler ./internal/app -count=1`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- All five confirmed risks are fixed.
- Verification uses the repository's real scheduler queue, app wiring, miniredis Redis component, and package tests.
- No mock-only replacement is introduced for the risk paths.
</success_criteria>
