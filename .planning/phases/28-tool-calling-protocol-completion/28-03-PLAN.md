---
phase: 28-tool-calling-protocol-completion
plan: "03"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - internal/providers/toolstream/state.go
  - internal/providers/toolstream/state_test.go
  - internal/providers/adaptertest/tool_contract.go
  - internal/providers/adaptertest/tool_contract_test.go
autonomous: true
requirements:
  - TOOL-F01
user_setup: []
artifacts:
  - internal/providers/toolstream/state.go
  - internal/providers/toolstream/state_test.go
  - internal/providers/adaptertest/tool_contract.go
  - internal/providers/adaptertest/tool_contract_test.go
must_haves:
  truths:
    - "D-09 and D-10: upstream tool-call IDs are preserved, and missing IDs receive one stable opaque request-domain ID that is reused in stream fragments, the final response, and later tool results."
    - "D-11 and D-12: generated IDs contain no secrets, indexes are deterministic zero-based identities, and interleaved calls never drift in identity or name."
    - "D-13 and D-14: provider-specific association is isolated behind the normalizer; duplicate IDs, unknown IDs, name conflicts, and unrecoverable drift are protocol errors rather than guesses."
    - "D-22 and D-23: normalized fragments are emitted immediately, with identity on the first fragment and stable order and interleaving thereafter."
    - "D-24 and D-25: arguments remain opaque strings until completion, concatenated JSON is validated at completion, and a valid tool-call turn ends with public finish_reason tool_calls."
    - "D-26 and D-27: malformed upstream streams use the existing provider_bad_response path, emitted events are not retracted, and Phase 27 finalizer remains the sole terminal, health, breaker, release, and usage owner."
    - "D-29 through D-33: deterministic local tests precede implementation, one shared contract matrix is reusable by all three adapters, Go commands use a hard 60 second timeout, state has no network I/O or unbounded per-fragment logging, and D-33 is rechecked before implementation."
    - "Spec-less edge probe (adjacency, encoding, ordering, idempotency, concurrency): same-name distinct calls retain separate ID/index state, opaque argument fragments preserve exact byte order until final JSON validation, interleaving never reassigns an established index, and concurrent or repeated completion cannot settle twice."
  artifacts:
    - path: internal/providers/toolstream/state.go
      provides: "Provider-neutral copy-on-write stream shadow state with injected ID generation, stable index identity, fragment normalization, and completion validation."
    - path: internal/providers/toolstream/state_test.go
      provides: "Red-first and passing state-machine tests for interleaving, IDs, drift, opaque arguments, completion, finish reason, and protocol errors."
    - path: internal/providers/adaptertest/tool_contract.go
      provides: "Reusable provider-neutral tool contract matrix and callback harness for definitions, choices, history, results, non-streaming, streaming, and errors."
    - path: internal/providers/adaptertest/tool_contract_test.go
      provides: "Harness contract tests proving deterministic execution, secret-safe failures, no network dependency, and readiness for OpenAI, Anthropic, and Gemini fixtures."
  key_links:
    - from: internal/providers/toolstream/state.go
      to: internal/llm/types.go
      via: "Consumes and emits normalized ToolCallChunk and finish-reason data without changing public stream framing."
    - from: internal/providers/adaptertest/tool_contract.go
      to: internal/providers/toolstream/state.go
      via: "Runs the shared stream shadow contract through an injected adapter/fixture callback."
    - from: internal/providers/toolstream/state.go
      to: internal/gateway/stream_terminal.go
      via: "Defines an integration seam and assertions for reuse of the existing Phase 27 finalizer; the finalizer file is read-only in this plan."
---

<objective>
Build the shared provider-neutral stream shadow state and reusable contract harness for tool-call deltas. The state machine must normalize identity, indexes, names, and opaque argument fragments immediately while validating completion and contradictions without owning terminal lifecycle. Later adapters will use this state beside the existing Phase 27 finalizer, which remains the sole owner of terminal callbacks, health, breaker, release, cancellation, and usage settlement.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
@.planning/PROJECT.md
@.planning/phases/28-tool-calling-protocol-completion/28-CONTEXT.md
@.planning/phases/28-tool-calling-protocol-completion/28-RESEARCH.md
@.planning/phases/28-tool-calling-protocol-completion/28-PATTERNS.md
@.planning/phases/28-tool-calling-protocol-completion/28-COVERAGE.md
</execution_context>

<context>
This plan depends only on Plan 01's normalized types and runs in wave 2 in parallel with routing preflight. The exact new state path is `internal/providers/toolstream/state.go`; the exact reusable harness path is `internal/providers/adaptertest/tool_contract.go`. Existing harness patterns live in `internal/providers/adaptertest/harness.go`, and the Phase 27 terminal owner is `internal/gateway/stream_terminal.go` under `.planning/phases/27-stream-terminal-settlement-consistency`. Read that finalizer and its verification before implementation.

The state machine is protocol shadow validation only. It emits normalized fragments and returns protocol errors, but it does not emit terminal events, settle usage, mark health, trip breakers, release admission, cancel streams, or retry providers. Do not edit `internal/gateway/stream_terminal.go` in this plan. The harness must be usable by all three provider adapters and their fixtures later, but this plan does not implement provider request mapping or provider-specific network behavior.
</context>

<tasks>
  <task type="auto" tdd="true">
    <name>Write the red stream and shared-contract failure matrix first</name>
    <files>internal/providers/toolstream/state_test.go, internal/providers/adaptertest/tool_contract_test.go</files>
    <read_first>
      Read `28-CONTEXT.md` decisions D-09 through D-14, D-22 through D-27, and D-29 through D-33; `28-RESEARCH.md`; `28-PATTERNS.md`; `internal/llm/types.go`; existing provider stream normalization code; `internal/providers/adaptertest/harness.go`; and the Phase 27 `internal/gateway/stream_terminal.go` plus its tests and verification artifact. Recheck the official provider streaming documentation and pinned SDK types required by D-33 before choosing fixture shapes.
    </read_first>
    <action>
      Enumerate stream failure modes before production edits and write deterministic red tests. Cover preserved upstream IDs, deterministic generated IDs for missing IDs, repeated ID reuse across final and tool-result contexts, distinct calls to the same function with independent ID/index state, interleaved zero-based indexes, missing index recovery only when unambiguous, first-fragment identity, later identity or name drift, duplicate and unknown IDs, empty or malformed function names, malformed delta shape, opaque argument fragments retaining exact byte order that may be invalid until concatenated, invalid final JSON, finish before all calls settle, contradictory finish reason, valid content plus tool calls, repeated/concurrent completion, and emitted-event non-retraction after a later error.

      Add harness tests for all five choice modes, definitions, assistant call history, tool results, non-streaming and streaming cases, provider-neutral error classification, secret-safe error assertions, and no network or credential dependency. Add an explicit finalizer ownership assertion or test seam that proves the shadow state never invokes terminal callbacks and that later adapter integration must call the existing Phase 27 finalizer exactly once.
    </action>
    <verify>
      <automated>go test -timeout 60s ./internal/providers/toolstream ./internal/providers/adaptertest -run 'TestToolStream|TestToolContract'</automated>
      <manual>Confirm the red matrix is deterministic and offline, checks immediate emission and invalid completion, and proves Phase 27 finalizer ownership is an integration contract.</manual>
    </verify>
    <fails_when>
      The automated command is green before implementation, buffers a whole stream before checking emission, assumes provider-specific IDs are always present, permits identity drift or unknown result IDs, asserts a terminal callback from the shadow state, includes raw arguments in failures, or requires network credentials.
    </fails_when>
    <acceptance_criteria>
      The red matrix covers D-09 through D-14, D-22 through D-27, and D-29 through D-33. It explicitly distinguishes immediate fragment emission from shadow assembly, checks invalid completion and finish behavior, and proves Phase 27 finalizer ownership is an integration contract rather than a new state-machine responsibility.
    </acceptance_criteria>
    <done>The red stream and contract matrices cover identity, interleaving, completion, privacy, and finalizer ownership without network dependencies.</done>
  </task>

  <task type="auto" tdd="true">
    <name>Implement copy-on-write stream shadow state and completion checks</name>
    <files>internal/providers/toolstream/state.go, internal/providers/toolstream/state_test.go</files>
    <read_first>
      Read the red state tests from Task 1, the current `llm.ToolCallChunk` and `StreamEvent` types, provider stream delta patterns, and the Phase 27 finalizer implementation. Confirm the state API does not overlap terminal ownership and follow the copy-on-write and injected ID generator patterns recorded in `28-PATTERNS.md`.
    </read_first>
    <action>
      Implement a provider-neutral shadow state with value-returning or copy-on-write transitions. Inject the ID generator so tests can prove stable deterministic reuse; generated IDs must be opaque, request-domain scoped, and independent of tool names, arguments, result content, credentials, or provider secrets. Preserve upstream IDs exactly when present. Maintain deterministic zero-based index identity and support interleaving; fill a missing index only in the explicitly unambiguous single-active-call case, otherwise return a generic provider_bad_response protocol error.

      Normalize and return each accepted fragment immediately. The first fragment for an index establishes ID, function type, and name; later fragments may append opaque argument strings but cannot change established identity or name. Keep content buffers only for unfinished calls, retain compact completion markers as needed to reject duplicate or unknown transitions, and never log each fragment. Validate concatenated arguments with JSON validity only when a call completes or the tool-call turn closes. A tool-call finish is valid only when all active calls are complete and valid, then the public finish reason is tool_calls. On malformed deltas, drift, invalid final JSON, or illegal transitions, return provider_bad_response without retracting already emitted events.

      Expose an integration seam that reports protocol completion or failure to the caller but does not perform terminal callbacks, health updates, breaker updates, usage settlement, admission release, cancellation, or retry. Document in code comments only where needed that `internal/gateway/stream_terminal.go` remains the sole Phase 27 finalizer owner.
    </action>
    <verify>
      <automated>go test -timeout 60s ./internal/providers/toolstream</automated>
      <manual>Inspect the tests for stable IDs across emitted fragments and completion, bounded shadow state, no raw-payload error text, and no finalizer invocation.</manual>
    </verify>
    <fails_when>
      The automated command exits non-zero because the state mutates caller-owned data, buffers all fragments, loses interleaved identity, changes a supplied ID, guesses through ambiguous drift, validates partial JSON as complete, emits a finish before all calls settle, logs raw payloads, or owns any Phase 27 terminal callback.
    </fails_when>
    <acceptance_criteria>
      D-09 through D-14 and D-22 through D-27 pass in the state tests. Fragments are immediately available to the adapter, identity is stable by index, arguments are opaque until completion, errors use the existing provider_bad_response classification without payload disclosure, and the state machine has no lifecycle side effects.
    </acceptance_criteria>
    <done>The stream shadow state emits validated fragments immediately, preserves stable identities, validates only at completion, and leaves lifecycle ownership to Phase 27.</done>
  </task>

  <task type="auto" tdd="true">
    <name>Build the reusable three-provider tool contract harness</name>
    <files>internal/providers/adaptertest/tool_contract.go, internal/providers/adaptertest/tool_contract_test.go</files>
    <read_first>
      Read the state API from Task 2, existing `internal/providers/adaptertest/harness.go`, provider adapter test conventions, `28-RESEARCH.md` provider fixture guidance, and the exact adapter paths listed in `28-PATTERNS.md`. Ensure the new harness composes with `RunConformance`, `AssertGatewayError`, `AssertSecretSafeError`, and `AssertCapabilitiesEqual` rather than replacing them.
    </read_first>
    <action>
      Add a provider-neutral contract matrix and callback-based runner that later OpenAI, Anthropic, and Gemini adapter fixtures can reuse. The matrix must cover tool definitions with opaque parameters, omitted and explicit auto, none, required, and named choices, assistant calls and tool results, content coexistence, non-streaming responses, streaming fragments, interleaving, preserved or generated IDs, final `tool_calls` finish, malformed upstream response errors, and secret-safe error projection. Keep provider-specific association and mapping behind fixture callbacks so the harness tests the shared contract without hard-coding an adapter's internal wire format.

      Make the harness deterministic and offline. It must not read credentials, call the network, execute tools, call MCP, or create a second terminal finalizer. Provide an explicit completion observer or injected finalizer callback contract showing that the adapter or gateway caller hands terminal work to `internal/gateway/stream_terminal.go` and that the shadow state only reports protocol status. Keep the public helper surface small so later provider plans can add fixtures without duplicating the matrix.
    </action>
    <verify>
      <automated>go test -timeout 60s ./internal/providers/adaptertest ./internal/providers/toolstream</automated>
      <manual>Review the harness for accidental network imports, credential reads, raw payload logging, or lifecycle callbacks. Do not run provider integration or full backend tests during this plan-authoring turn.</manual>
    </verify>
    <fails_when>
      The automated command exits non-zero because each provider needs a separate copy of the matrix, the harness assumes one provider's wire format, tests require live credentials or network access, the harness executes a tool, the shadow state becomes the terminal owner, or secret-safe assertions are omitted.
    </fails_when>
    <acceptance_criteria>
      D-30 is satisfied by one shared matrix that all three adapters can reuse, and D-29, D-31, D-32, and D-33 remain explicit in the harness tests and execution notes. The harness proves the state machine and Phase 27 finalizer have separate responsibilities and is ready for later provider-specific fixtures.
    </acceptance_criteria>
    <done>A single deterministic provider-neutral harness covers the shared tool contract and clearly delegates terminal work to the Phase 27 finalizer.</done>
  </task>
</tasks>

<threat_model>
  <scope>Untrusted provider stream deltas enter shared normalization state and eventually influence public tool-call fragments and terminal classification.</scope>
  <assets>Tool-call IDs, indexes, names, opaque argument strings, finish reason, emitted stream events, error classification, and terminal settlement ownership.</assets>
  <controls>
    <control severity="high" asvs="V5,V8">Treat every provider delta as untrusted. Reject malformed structure, duplicate or unknown IDs, identity drift, name conflicts, ambiguous indexes, invalid completion JSON, and illegal transitions without guessing.</control>
    <control severity="high" asvs="V7,V8">Keep arguments and tool results opaque and out of errors and logs. Validate concatenated JSON only at the defined completion boundary and use provider_bad_response without raw payload disclosure.</control>
    <control severity="high" asvs="V8,V13">Keep the shadow state side-effect free. The Phase 27 finalizer at `internal/gateway/stream_terminal.go` remains the sole owner of cancel, done, health, breaker, admission release, and usage settlement; any second owner blocks the plan.</control>
    <control severity="medium" asvs="V4,V10">Use injected deterministic ID generation, copy-on-write transitions, bounded unfinished-call buffers, no per-fragment high-cardinality logs, and no network or credential dependency in the harness.</control>
    <control severity="medium" asvs="V5,V8">Emit accepted fragments immediately and never retract them after a later protocol error; surface the error through the existing stream error path.</control>
  </controls>
  <blockers>Any guessed identity, unbounded argument buffering, raw payload leak, premature tool_calls finish, terminal lifecycle callback from shadow state, network-dependent harness, or duplicate finalizer ownership blocks the plan.</blockers>
</threat_model>

<verification>
  <command>`go test -timeout 60s ./internal/providers/toolstream -run 'TestToolStream'`</command>
  <command>`go test -timeout 60s ./internal/providers/adaptertest -run 'TestToolContract'`</command>
  <command>`go test -timeout 60s ./internal/providers/adaptertest ./internal/providers/toolstream`</command>
  <command>`git diff --check -- .planning/phases/28-tool-calling-protocol-completion/28-03-PLAN.md`</command>
  <manual>Confirm the state machine is only a protocol shadow validator and that all terminal callbacks remain delegated to the Phase 27 finalizer path.</manual>
</verification>

<success_criteria>
  <criterion>The shared state emits normalized fragments immediately, preserves or deterministically generates stable identities, supports interleaving, and rejects unrecoverable drift.</criterion>
  <criterion>Arguments remain opaque until completion, valid tool-call turns finish as tool_calls, and malformed upstream data follows provider_bad_response without raw payload disclosure.</criterion>
  <criterion>The reusable contract harness covers the common matrix for OpenAI, Anthropic, and Gemini without network, credentials, execution, or provider-specific duplication.</criterion>
  <criterion>Phase 27 finalizer ownership is explicit and unchanged; the new state machine performs no terminal, health, breaker, release, cancellation, usage, or retry work.</criterion>
</success_criteria>

<output>
  Produce a concise execution summary identifying the state API, ID and index invariants, error and finish behavior, shared harness matrix, and the Phase 27 finalizer ownership proof. Record any D-33 official documentation or pinned SDK discrepancy. Do not report product test execution from this plan-authoring turn.
</output>
