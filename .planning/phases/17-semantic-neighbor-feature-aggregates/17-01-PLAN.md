---
phase: 17-semantic-neighbor-feature-aggregates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/scheduler/v1/scheduler.proto
  - internal/scheduler/schedulerv1/scheduler.pb.go
  - internal/scheduler/types.go
  - internal/scheduler/features.go
  - internal/scheduler/types_test.go
  - internal/scheduler/features_test.go
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
autonomous: true
requirements: ["QDR-01", "QDR-02", "QDR-03", "QDR-04"]
must_haves:
  truths:
    - "D-05/D-06/D-07 semantic-neighbor aggregates are first-class TaskFeature and scheduler proto fields."
    - "D-08 semantic-neighbor fields have bounded defaults whenever enrichment is disabled, unavailable, timed out, or unsupported."
    - "D-10 scheduler semantic-neighbor config is disabled by default with min_count 20, task timeout 5ms, and batch timeout 15ms."
  artifacts:
    - path: "proto/scheduler/v1/scheduler.proto"
      provides: "TaskFeature wire fields neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, coverage_ratio"
    - path: "internal/scheduler/types.go"
      provides: "Go TaskFeature fields and proto mapping for semantic-neighbor aggregates"
    - path: "internal/config/config.go"
      provides: "Scheduler semantic-neighbor config defaults and env/json loading"
  key_links:
    - from: "internal/scheduler/features.go"
      to: "internal/scheduler/types.go"
      via: "ExtractSafeFeatures returns neutral aggregate defaults before optional enrichment"
    - from: "internal/config/config.go"
      to: "internal/config/config_validation.go"
      via: "SchedulerConfig defaults are validated before app wiring consumes them"
---

<objective>
Create the Phase 17 semantic-neighbor feature contract and configuration surface.

Purpose: D-05, D-06, and D-07 require bounded aggregate fields to be part of the normal scheduler feature contract, and D-08/D-10 require safe disabled-by-default behavior.
Output: TaskFeature/proto/config changes that let later plans enrich, persist, and score semantic-neighbor aggregates without raw prompts, embeddings, semantic-cache payloads, auth headers, API keys, or provider secrets crossing into Scheduler.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
@.planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
@.planning/phases/15-training-feedback-and-onnx-path/15-CONTEXT.md
@.planning/phases/14-scheduler-queue-foundation/14-CONTEXT.md
@proto/scheduler/v1/scheduler.proto
@internal/scheduler/types.go
@internal/scheduler/features.go
@internal/config/config.go
@internal/config/config_validation.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add first-class semantic aggregate fields to scheduler features</name>
  <files>proto/scheduler/v1/scheduler.proto, internal/scheduler/schedulerv1/scheduler.pb.go, internal/scheduler/types.go, internal/scheduler/features.go, internal/scheduler/types_test.go, internal/scheduler/features_test.go</files>
  <read_first>
    - proto/scheduler/v1/scheduler.proto
    - internal/scheduler/types.go
    - internal/scheduler/features.go
    - internal/scheduler/client_test.go
    - internal/scheduler/features_test.go
    - .planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
  </read_first>
  <behavior>
    - ExtractSafeFeatures returns neighbor_count=0, latency fields=0, output_tokens_p70=0, success_rate=0, timeout_rate=0, coverage_level="none", coverage_ratio=0 when no enrichment has run.
    - TaskFeature.proto maps every new Go field to the matching protobuf field and does not use a metadata map.
    - Generated scheduler.pb.go contains the new TaskFeature accessors after proto generation.
  </behavior>
  <action>Add TaskFeature fields named NeighborCount, LatencyP50Ms, LatencyP90Ms, LatencyStddevMs, OutputTokensP70, SuccessRate, TimeoutRate, CoverageLevel, and CoverageRatio per D-05/D-06/D-07. Add matching protobuf fields neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, and coverage_ratio after existing field numbers. Define coverage level constants "none", "tenant", and "fallback" in Go so D-03 fallback can be represented later. Update ExtractSafeFeatures and TaskFeature.proto mapping so D-08 neutral defaults are emitted before enrichment. Regenerate internal/scheduler/schedulerv1/scheduler.pb.go using the project protoc setup from Phase 14.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/types.go contains the nine first-class aggregate fields on TaskFeature.
    - proto/scheduler/v1/scheduler.proto contains the nine first-class TaskFeature fields without a metadata map.
    - internal/scheduler/features_test.go asserts the D-08 neutral defaults from ExtractSafeFeatures.
    - internal/scheduler/types_test.go asserts Go-to-proto mapping for every aggregate field.
  </acceptance_criteria>
  <done>Scheduler feature payloads can carry the D-05/D-06 semantic aggregate shape with D-08 safe defaults and no raw text or embedding fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add disabled-by-default semantic-neighbor scheduler config</name>
  <files>internal/config/config.go, internal/config/config_validation.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - .planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
  </read_first>
  <behavior>
    - Default SchedulerConfig has SemanticNeighborsEnabled=false, SemanticNeighborsMinCount=20, SemanticNeighborsTaskTimeout=5ms, and SemanticNeighborsBatchTimeout=15ms.
    - Env loading accepts SCHEDULER_SEMANTIC_NEIGHBORS_ENABLED, SCHEDULER_SEMANTIC_NEIGHBORS_MIN_COUNT, SCHEDULER_SEMANTIC_NEIGHBORS_TASK_TIMEOUT, and SCHEDULER_SEMANTIC_NEIGHBORS_BATCH_TIMEOUT.
    - JSON config merge accepts semantic_neighbors_enabled, semantic_neighbors_min_count, semantic_neighbors_task_timeout, and semantic_neighbors_batch_timeout under scheduler.
  </behavior>
  <action>Extend SchedulerConfig with SemanticNeighborsEnabled, SemanticNeighborsMinCount, SemanticNeighborsTaskTimeout, and SemanticNeighborsBatchTimeout per D-10. Apply defaults false, 20, 5ms, and 15ms. Validate min_count is greater than zero, both timeouts are positive, and batch timeout is greater than or equal to task timeout. Keep examples and source-controlled defaults secret-free per AGENTS.md. Do not make semantic-neighbor enablement depend only on vector store presence.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - internal/config/config_test.go covers defaults, env overrides, JSON merge, invalid min_count, invalid timeout, and batch timeout smaller than task timeout.
    - No source-controlled config contains provider secrets, API keys, auth headers, or concrete deployment credentials.
  </acceptance_criteria>
  <done>Operators have the D-10 config surface and disabled default needed before Gateway wiring consumes semantic-neighbor enrichment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway -> Scheduler RPC | Only bounded numeric or enum aggregate fields cross this boundary. |
| Env/file config -> runtime config | Admin-controlled values enter SchedulerConfig and must be validated before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Information Disclosure | proto/scheduler/v1/scheduler.proto | mitigate | Add only numeric and enum aggregate fields from D-05/D-06; do not add raw text, embeddings, semantic-cache payloads, headers, API keys, or provider secrets per QDR-02. |
| T-17-01-02 | Tampering | internal/config/config_validation.go | mitigate | Reject non-positive min_count/timeouts and batch timeout smaller than task timeout before app wiring consumes D-10 config. |
| T-17-01-03 | Denial of Service | internal/config/config.go | mitigate | Keep enrichment disabled by default and bounded by 5ms/15ms defaults per D-10. |
| T-17-01-SC | Tampering | generated protobuf tooling | mitigate | Use the existing local protoc and Go generator path from Phase 14; do not add package installs in this plan. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/scheduler ./internal/config`
- `go build ./...`
</verification>

<success_criteria>
- QDR-02 is satisfied at the contract layer: scheduler payloads contain only bounded numeric or enum semantic aggregate fields.
- QDR-03 is satisfied at the config layer: semantic enrichment is disabled by default and timeout-bounded.
- QDR-04 has the safe-default feature contract required by later persistence and ONNX work.
- D-05, D-06, D-07, D-08, and D-10 are explicitly represented in code and tests.
</success_criteria>

## Artifacts this phase produces

- TaskFeature fields: NeighborCount, LatencyP50Ms, LatencyP90Ms, LatencyStddevMs, OutputTokensP70, SuccessRate, TimeoutRate, CoverageLevel, CoverageRatio.
- Protobuf fields: neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, coverage_ratio.
- Config fields: SchedulerConfig.SemanticNeighborsEnabled, SchedulerConfig.SemanticNeighborsMinCount, SchedulerConfig.SemanticNeighborsTaskTimeout, SchedulerConfig.SemanticNeighborsBatchTimeout.
- Env/config keys: SCHEDULER_SEMANTIC_NEIGHBORS_ENABLED, SCHEDULER_SEMANTIC_NEIGHBORS_MIN_COUNT, SCHEDULER_SEMANTIC_NEIGHBORS_TASK_TIMEOUT, SCHEDULER_SEMANTIC_NEIGHBORS_BATCH_TIMEOUT, scheduler.semantic_neighbors_enabled, scheduler.semantic_neighbors_min_count, scheduler.semantic_neighbors_task_timeout, scheduler.semantic_neighbors_batch_timeout.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP goal | Optional semantic-neighbor aggregate features without moving raw text/embeddings into Scheduler | Covered by Task 1 contract and Task 2 disabled config. |
| QDR-01 | Optional collection requires configured vector/embedding path | Covered by config prerequisite in Task 2; runtime collection is completed by 17-02. |
| QDR-02 | Only bounded numeric/enum aggregates reach Scheduler/training | Covered by Task 1 proto/TaskFeature shape. |
| QDR-03 | Disabled default, timeout budget, fail-open path | Covered by Task 2 config defaults; runtime fallback is completed by 17-02. |
| QDR-04 | Training export/ONNX default field support | Contract defaults covered here; persistence/export/ONNX completed by 17-03. |
| CONTEXT D-05/D-06/D-07/D-08/D-10 | Aggregate shape, first-class fields, safe defaults, disabled config | Covered by Tasks 1 and 2. |
| CONTEXT deferred block | Semantic-cache metadata source, heuristic score adjustment, anomaly/OOD, SLA promotion | Excluded by phase boundary; no task writes those behaviors. |

<output>
Create `.planning/phases/17-semantic-neighbor-feature-aggregates/17-01-SUMMARY.md` when done.
</output>
