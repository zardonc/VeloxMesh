---
phase: 17-semantic-neighbor-feature-aggregates
plan: 17-01
subsystem: scheduler
tags: [scheduler, protobuf, config, semantic-neighbors]
requires:
  - phase: 14-scheduler-queue-foundation
    provides: Scheduler feature extraction and RPC contract
provides:
  - First-class semantic-neighbor aggregate fields on TaskFeature and scheduler protobuf payloads
  - Safe D-08 neutral aggregate defaults before enrichment runs
  - Disabled-by-default semantic-neighbor scheduler configuration
affects: [scheduler, config, training, onnx]
tech-stack:
  added: []
  patterns: [first-class bounded scheduler feature fields, disabled optional scheduler config]
key-files:
  created:
    - internal/scheduler/types_test.go
  modified:
    - proto/scheduler/v1/scheduler.proto
    - internal/scheduler/schedulerv1/scheduler.pb.go
    - internal/scheduler/types.go
    - internal/scheduler/features.go
    - internal/scheduler/features_test.go
    - internal/config/config.go
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - .env.example
key-decisions:
  - "Represented semantic-neighbor coverage with explicit string constants instead of a metadata map."
  - "Kept semantic-neighbor enrichment disabled by default with 20 sample minimum and 5ms/15ms timeout defaults."
patterns-established:
  - "TaskFeature semantic aggregates default to zero values plus coverage_level=none before optional enrichment."
requirements-completed: [QDR-01, QDR-02, QDR-03, QDR-04]
duration: 35 min
completed: 2026-07-04
---

# Phase 17 Plan 01: Semantic Feature Contract and Config Summary

**Scheduler TaskFeature payloads now carry bounded semantic-neighbor aggregate fields with disabled-by-default config controls**

## Performance

- **Duration:** 35 min
- **Started:** 2026-07-04T19:25:00Z
- **Completed:** 2026-07-04T20:00:00Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Added nine first-class semantic aggregate fields to the scheduler proto, generated Go type, and `TaskFeature` mapping.
- Added D-08 neutral defaults from `ExtractSafeFeatures`, including `coverage_level=none`.
- Added scheduler semantic-neighbor config defaults, env keys, JSON merge, validation, and `.env.example` placeholders.

## Task Commits

1. **Task 1: Add first-class semantic aggregate fields to scheduler features** - `3c6017a` (feat)
2. **Task 2: Add disabled-by-default semantic-neighbor scheduler config** - `c1cb9e2` (feat)
3. **Verification fix: Preserve semantic aggregates on protobuf wire** - `bbb67a2` (fix)

## Files Created/Modified

- `proto/scheduler/v1/scheduler.proto` - Added the nine semantic aggregate fields with stable field numbers.
- `internal/scheduler/schedulerv1/scheduler.pb.go` - Added generated Go fields/getters and updated runtime descriptor bytes.
- `internal/scheduler/types.go` - Added aggregate fields, coverage constants, and protobuf mapping.
- `internal/scheduler/features.go` - Added safe `coverage_level=none` default.
- `internal/scheduler/features_test.go` - Covered D-08 neutral defaults.
- `internal/scheduler/types_test.go` - Covered Go-to-proto mapping and protobuf wire round trip.
- `internal/config/config.go` - Added config fields, defaults, env loading, and JSON merge.
- `internal/config/config_validation.go` - Added min-count and timeout validation.
- `internal/config/config_test.go` - Covered defaults, env overrides, JSON merge, and invalid values.
- `.env.example` - Documented secret-free semantic-neighbor scheduler toggles.

## Decisions Made

- Used explicit `TaskFeature` fields rather than a metadata map so scheduler, training, export, and ONNX paths can share one bounded contract.
- Left enrichment disabled by default and independent of vector-store presence; 17-02 owns optional dependency wiring.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Local protobuf generator unavailable**
- **Found during:** Task 1 verification
- **Issue:** `protoc` was not on PATH, so the generated Go file could not be regenerated by the usual toolchain.
- **Fix:** Added the generated Go fields/getters and rebuilt the raw protobuf descriptor bytes from `descriptor_pb2` in the existing uv scheduler-training environment.
- **Files modified:** `internal/scheduler/schedulerv1/scheduler.pb.go`, `internal/scheduler/types_test.go`
- **Verification:** Added and passed a `proto.Marshal`/`proto.Unmarshal` round-trip test proving the new fields survive the wire format.
- **Committed in:** `bbb67a2`

---

**Total deviations:** 1 auto-fixed (blocking)
**Impact on plan:** The protobuf contract is functional and covered by a wire test. Installing `protoc` later can replace the manual descriptor update with normal regeneration.

## Issues Encountered

- Initial accessor-only test passed while protobuf marshal dropped the new fields; fixed by adding descriptor coverage and a wire round-trip test.

## User Setup Required

None - no external service configuration required.

## Verification

- `go test -timeout 60s ./internal/scheduler ./internal/config`
- `go build ./...`

## Self-Check: PASSED

All task acceptance criteria passed.

## Next Phase Readiness

17-02 can now enrich safe `TaskFeature` snapshots with semantic-neighbor aggregate values and rely on disabled defaults when enrichment is unavailable.

---
*Phase: 17-semantic-neighbor-feature-aggregates*
*Completed: 2026-07-04*
