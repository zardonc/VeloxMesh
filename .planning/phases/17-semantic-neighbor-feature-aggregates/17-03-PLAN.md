---
phase: 17-semantic-neighbor-feature-aggregates
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - internal/controlstate/types.go
  - internal/scheduler/training.go
  - internal/scheduler/training_test.go
  - internal/controlstate/sqlite/scheduler_training_samples.go
  - internal/controlstate/sqlite/scheduler_training_samples_test.go
  - internal/controlstate/postgres/scheduler_training_samples.go
  - internal/controlstate/postgres/scheduler_training_samples_test.go
  - internal/controlstate/migrations/sqlite/0009_scheduler_training_semantic_aggregates.sql
  - internal/controlstate/migrations/postgres/0008_scheduler_training_semantic_aggregates.sql
  - tools/scheduler_training/scheduler_training/export.py
  - tools/scheduler_training/scheduler_training/train.py
  - tools/scheduler_training/scheduler_training/artifacts.py
  - tools/scheduler_training/tests/test_export_schema.py
  - tools/scheduler_training/tests/test_train_publish.py
  - tools/scheduler_training/tests/test_artifacts.py
  - internal/scheduler/onnx/artifact.go
  - internal/scheduler/onnx/scorer.go
  - internal/scheduler/onnx/artifact_test.go
  - internal/scheduler/onnx/scorer_test.go
  - internal/scheduler/heuristic/score_test.go
autonomous: true
requirements: ["QDR-02", "QDR-04"]
must_haves:
  truths:
    - "D-07 semantic aggregate fields persist as first-class training sample/export/ONNX feature fields."
    - "D-08/D-14 unsupported or unavailable semantic aggregate fields use neutral defaults at runtime."
    - "D-13 ONNX/training can consume semantic aggregates when the artifact supports them."
    - "D-15 heuristic and FIFO scores are unchanged by semantic aggregates in Phase 17."
  artifacts:
    - path: "internal/controlstate/types.go"
      provides: "SchedulerTrainingSample semantic aggregate fields"
    - path: "tools/scheduler_training/scheduler_training/export.py"
      provides: "Exported semantic aggregate feature columns with defaults"
    - path: "internal/scheduler/onnx/artifact.go"
      provides: "Artifact feature compatibility policy for semantic aggregate support"
  key_links:
    - from: "internal/scheduler/training.go"
      to: "internal/controlstate/sqlite/scheduler_training_samples.go"
      via: "TrainingRecorder copies TaskFeature aggregate fields into durable samples"
    - from: "tools/scheduler_training/scheduler_training/export.py"
      to: "internal/scheduler/onnx/scorer.go"
      via: "Export/training/artifact metadata and runtime coverage agree on semantic aggregate defaults"
---

<objective>
Carry semantic-neighbor aggregate fields through training persistence, offline export, and ONNX runtime compatibility.

Purpose: QDR-04, D-07, D-08, D-13, D-14, and D-15 require semantic aggregates to be available to training and ONNX without breaking existing artifacts or changing heuristic/FIFO score behavior.
Output: Durable sample columns, SQLite/PostgreSQL parity, export/training schema support, ONNX artifact compatibility, and tests that prove neutral defaults and heuristic score stability.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
@.planning/phases/15-training-feedback-and-onnx-path/15-CONTEXT.md
@.planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
@internal/controlstate/types.go
@internal/scheduler/training.go
@internal/controlstate/sqlite/scheduler_training_samples.go
@internal/controlstate/postgres/scheduler_training_samples.go
@tools/scheduler_training/scheduler_training/export.py
@tools/scheduler_training/scheduler_training/train.py
@tools/scheduler_training/scheduler_training/artifacts.py
@internal/scheduler/onnx/artifact.go
@internal/scheduler/onnx/scorer.go
@internal/scheduler/heuristic/score.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist aggregate fields in scheduler training samples</name>
  <files>internal/controlstate/types.go, internal/scheduler/training.go, internal/scheduler/training_test.go, internal/controlstate/sqlite/scheduler_training_samples.go, internal/controlstate/sqlite/scheduler_training_samples_test.go, internal/controlstate/postgres/scheduler_training_samples.go, internal/controlstate/postgres/scheduler_training_samples_test.go, internal/controlstate/migrations/sqlite/0009_scheduler_training_semantic_aggregates.sql, internal/controlstate/migrations/postgres/0008_scheduler_training_semantic_aggregates.sql</files>
  <read_first>
    - internal/controlstate/types.go
    - internal/scheduler/training.go
    - internal/controlstate/sqlite/scheduler_training_samples.go
    - internal/controlstate/postgres/scheduler_training_samples.go
    - internal/controlstate/migrations/sqlite/0007_scheduler_training_samples.sql
    - internal/controlstate/migrations/postgres/0006_scheduler_training_samples.sql
    - internal/scheduler/training_test.go
    - internal/controlstate/sqlite/scheduler_training_samples_test.go
    - internal/controlstate/postgres/scheduler_training_samples_test.go
  </read_first>
  <behavior>
    - TrainingRecorder copies all nine TaskFeature aggregate fields into SchedulerTrainingSample.
    - SQLite and PostgreSQL inserts/selects round-trip the nine fields with zero/"none" defaults.
    - Existing samples migrated without semantic aggregate data read back with safe D-08 defaults.
  </behavior>
  <action>Add first-class SchedulerTrainingSample fields for neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, and coverage_ratio per D-07. Add SQLite migration 0009 and PostgreSQL migration 0008 with non-null defaults matching D-08. Update sampleValues, schedulerTrainingSampleColumns, scan functions, and repository tests for both backends. Update TrainingRecorder so all completed outcomes recorded by Phase 15/16 carry the semantic aggregate snapshot from Task.Feature.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler ./internal/controlstate/sqlite ./internal/controlstate/postgres</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/training_test.go asserts all nine aggregate fields are copied from Task.Feature to SchedulerTrainingSample.
    - SQLite and PostgreSQL tests insert and list a sample with non-default aggregate values and a sample that relies on migration defaults.
    - New migrations add only bounded numeric/text enum columns and do not add raw prompt, embedding, semantic-cache payload, auth header, API key, or provider secret columns.
  </acceptance_criteria>
  <done>Durable training samples preserve D-07 aggregate fields with D-08 safe defaults across SQLite and PostgreSQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend offline export and training feature schema</name>
  <files>tools/scheduler_training/scheduler_training/export.py, tools/scheduler_training/scheduler_training/train.py, tools/scheduler_training/scheduler_training/artifacts.py, tools/scheduler_training/tests/test_export_schema.py, tools/scheduler_training/tests/test_train_publish.py, tools/scheduler_training/tests/test_artifacts.py</files>
  <read_first>
    - tools/scheduler_training/scheduler_training/export.py
    - tools/scheduler_training/scheduler_training/train.py
    - tools/scheduler_training/scheduler_training/artifacts.py
    - tools/scheduler_training/tests/test_export_schema.py
    - tools/scheduler_training/tests/test_train_publish.py
    - tools/scheduler_training/tests/test_artifacts.py
    - .planning/phases/15-training-feedback-and-onnx-path/15-CONTEXT.md
  </read_first>
  <behavior>
    - Exported datasets include the nine semantic aggregate columns with safe defaults when source rows do not contain values.
    - Training feature preparation includes semantic aggregate columns in a deterministic order and encodes coverage_level as a bounded enum.
    - Artifact metadata identifies whether semantic aggregate features are supported.
  </behavior>
  <action>Update scheduler_training export schema to include neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, and coverage_ratio per D-07/QDR-04. Ensure missing or null values become D-08 defaults before model input. Update train.py feature preparation so semantic fields may feed the model per D-13 when present in the artifact feature list. Update artifacts.py and tests so published manifest metadata records semantic aggregate support as a boolean or feature list without embedding raw exported datasets in the runtime artifact directory.</action>
  <verify>
    <automated>uv run pytest tools/scheduler_training/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - test_export_schema.py asserts exported semantic aggregate columns and default filling.
    - test_train_publish.py asserts feature preparation includes semantic aggregate fields when present and still trains with default-filled rows.
    - test_artifacts.py asserts runtime artifacts contain manifest metadata and not raw datasets or bulky training logs.
  </acceptance_criteria>
  <done>Offline training can consume semantic aggregate fields per D-13 while preserving QDR-04 default behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ONNX compatibility defaults and heuristic score invariance</name>
  <files>internal/scheduler/onnx/artifact.go, internal/scheduler/onnx/scorer.go, internal/scheduler/onnx/artifact_test.go, internal/scheduler/onnx/scorer_test.go, internal/scheduler/heuristic/score_test.go</files>
  <read_first>
    - internal/scheduler/onnx/artifact.go
    - internal/scheduler/onnx/scorer.go
    - internal/scheduler/onnx/artifact_test.go
    - internal/scheduler/onnx/scorer_test.go
    - internal/scheduler/heuristic/score.go
    - internal/scheduler/heuristic/score_test.go
    - .planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
  </read_first>
  <behavior>
    - ONNX artifacts that declare semantic aggregate feature support can include the new fields in runtime coverage/confidence calculations.
    - ONNX artifacts that do not declare semantic aggregate support still load under the existing supported schema and score with neutral/default aggregate values.
    - Heuristic ScoreCalculator produces identical score, predicted latency, confidence, and fallback reason for features that differ only by semantic aggregate fields.
  </behavior>
  <action>Update ONNX manifest parsing so runtime can distinguish artifacts that support semantic aggregate features from artifacts that do not, without requiring retraining before runtime starts per D-14. Update featureCoverage to count semantic aggregate coverage only when manifest metadata declares support; otherwise keep current coverage behavior and neutral D-08 defaults. Add tests for supported and unsupported artifact manifests. Add a heuristic score invariance test proving D-15: changing neighbor_count, latency stats, output_tokens_p70, success_rate, timeout_rate, coverage_level, or coverage_ratio does not alter heuristic/FIFO scoring in Phase 17.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/onnx ./internal/scheduler/heuristic</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/onnx/artifact_test.go covers manifests with and without semantic aggregate support metadata.
    - internal/scheduler/onnx/scorer_test.go covers D-14 neutral/default scoring for unsupported artifacts.
    - internal/scheduler/heuristic/score_test.go proves D-15 heuristic score invariance for semantic aggregate-only changes.
  </acceptance_criteria>
  <done>ONNX/training can use semantic aggregates when supported, while unsupported ONNX artifacts and heuristic/FIFO behavior remain stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway training evidence -> durable control state | Only safe TaskFeature aggregates and completion labels are stored. |
| Durable samples -> offline Python tooling | Export receives bounded sample fields and no raw request content. |
| Artifact metadata -> ONNX runtime | Manifest controls feature compatibility without exposing model internals to data-plane callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Information Disclosure | training sample migrations and export.py | mitigate | Add only bounded aggregate columns; tests assert no raw prompt, embedding, semantic-cache payload, auth header, API key, provider secret, or original payload columns are introduced. |
| T-17-03-02 | Tampering | ONNX manifest metadata | mitigate | Validate semantic aggregate support metadata and use D-08 neutral defaults when unsupported or absent per D-14. |
| T-17-03-03 | Denial of Service | ONNX runtime startup | mitigate | Preserve existing artifact load behavior for current schema and do not require retraining before startup. |
| T-17-03-04 | Elevation of Privilege | heuristic/FIFO scoring | mitigate | Tests prove semantic aggregate fields do not change heuristic/FIFO ordering in Phase 17 per D-15. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/scheduler ./internal/scheduler/onnx ./internal/scheduler/heuristic ./internal/controlstate/sqlite ./internal/controlstate/postgres`
- `uv run pytest tools/scheduler_training/tests -q`
- `go build ./...`
</verification>

<success_criteria>
- QDR-02 remains true across training/export: no raw prompts, embeddings, semantic-cache payloads, auth headers, API keys, provider secrets, or original payloads are stored.
- QDR-04 is satisfied: training export and ONNX feature preparation include semantic aggregate fields with safe defaults.
- D-07, D-08, D-13, D-14, and D-15 are covered by persistence, tooling, runtime, and invariance tests.
- SQLite and PostgreSQL scheduler training sample paths stay at parity.
</success_criteria>

## Artifacts this phase produces

- SchedulerTrainingSample fields matching TaskFeature semantic aggregate fields.
- SQLite migration: internal/controlstate/migrations/sqlite/0009_scheduler_training_semantic_aggregates.sql.
- PostgreSQL migration: internal/controlstate/migrations/postgres/0008_scheduler_training_semantic_aggregates.sql.
- Offline export/training feature columns for neighbor_count, latency_p50_ms, latency_p90_ms, latency_stddev_ms, output_tokens_p70, success_rate, timeout_rate, coverage_level, coverage_ratio.
- ONNX manifest compatibility metadata for semantic aggregate support.
- Heuristic score invariance tests for Phase 17 semantic aggregate fields.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP goal | Scheduler feature payloads and training samples include numeric/enum aggregate fields with defaults | Covered by Tasks 1 and 2. |
| QDR-02 | No sensitive content reaches Scheduler/training/export | Covered by Tasks 1 and 2. |
| QDR-04 | Training export and ONNX preparation include semantic aggregate defaults | Covered by Tasks 2 and 3. |
| CONTEXT D-07/D-08 | First-class fields and bounded defaults in training/export/ONNX | Covered by Tasks 1, 2, and 3. |
| CONTEXT D-13/D-14 | ONNX/training consumption with unsupported artifact defaults | Covered by Tasks 2 and 3. |
| CONTEXT D-15 | Heuristic observes only and FIFO ignores semantic aggregates | Covered by Task 3. |
| CONTEXT deferred block | Semantic-cache metadata source, heuristic score adjustment, anomaly/OOD, SLA promotion | Excluded by phase boundary; no task writes those behaviors. |

<output>
Create `.planning/phases/17-semantic-neighbor-feature-aggregates/17-03-SUMMARY.md` when done.
</output>
