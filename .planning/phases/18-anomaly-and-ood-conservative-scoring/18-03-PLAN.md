---
phase: 18-anomaly-and-ood-conservative-scoring
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - internal/controlstate/types.go
  - internal/controlstate/scheduler_quality_rollup.go
  - internal/controlstate/migrations/sqlite/0010_scheduler_quality_anomaly.sql
  - internal/controlstate/migrations/postgres/0009_scheduler_quality_anomaly.sql
  - internal/controlstate/sqlite/scheduler_quality_rollups.go
  - internal/controlstate/postgres/scheduler_quality_rollups.go
  - internal/scheduler/quality.go
  - internal/scheduler/quality_test.go
  - internal/observability/metrics.go
  - internal/observability/prometheus.go
autonomous: true
requirements: ["ANOM-04"]
must_haves:
  truths:
    - "D-15 anomaly/OOD quality is compared by scheduler_version + task_type + coverage_level."
    - "D-16 durable quality rollups include anomaly_count, anomaly_rate, and anomaly_unavailable_count."
    - "D-17 live metrics may add only low-cardinality coverage_level and anomaly_status labels."
    - "D-18 anomaly unavailable/degraded counts stay separate from scheduler fallback rate."
  artifacts:
    - path: "internal/controlstate/types.go"
      provides: "SchedulerQualityRollup anomaly fields and coverage_level dimension"
    - path: "internal/controlstate/migrations/sqlite/0010_scheduler_quality_anomaly.sql"
      provides: "SQLite durable rollup schema extension"
    - path: "internal/controlstate/migrations/postgres/0009_scheduler_quality_anomaly.sql"
      provides: "PostgreSQL durable rollup schema extension"
    - path: "internal/scheduler/quality.go"
      provides: "prediction quality recording for anomaly counts and coverage level"
  key_links:
    - from: "internal/scheduler/quality.go"
      to: "internal/controlstate/scheduler_quality_rollup.go"
      via: "quality recorder writes anomaly fields and normalization derives anomaly_rate"
    - from: "internal/scheduler/quality.go"
      to: "internal/observability/metrics.go"
      via: "live MAPE/fallback/anomaly metrics use the same sanitized dimensions"
---

<objective>
Extend prediction-quality evidence so operators can compare anomaly behavior with MAPE and fallback signals.

Purpose: ANOM-04 and D-15 through D-18 require durable and live quality evidence for anomaly rate, unavailable/degraded anomaly state, MAPE, and fallback behavior by scheduler version, task type, and coverage level.
Output: SQLite/PostgreSQL rollup parity, recorder changes, and metrics that keep anomaly degradation distinct from scheduler fallback.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
@.planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
@internal/scheduler/quality.go
@internal/controlstate/types.go
@internal/controlstate/scheduler_quality_rollup.go
@internal/controlstate/sqlite/scheduler_quality_rollups.go
@internal/controlstate/postgres/scheduler_quality_rollups.go
@internal/observability/metrics.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anomaly fields and coverage dimension to durable quality rollups</name>
  <files>internal/controlstate/types.go, internal/controlstate/scheduler_quality_rollup.go, internal/controlstate/migrations/sqlite/0010_scheduler_quality_anomaly.sql, internal/controlstate/migrations/postgres/0009_scheduler_quality_anomaly.sql, internal/controlstate/sqlite/scheduler_quality_rollups.go, internal/controlstate/postgres/scheduler_quality_rollups.go</files>
  <read_first>
    - internal/controlstate/types.go
    - internal/controlstate/scheduler_quality_rollup.go
    - internal/controlstate/sqlite/scheduler_quality_rollups.go
    - internal/controlstate/postgres/scheduler_quality_rollups.go
    - internal/controlstate/migrations/sqlite/0008_scheduler_quality_rollups.sql
    - internal/controlstate/migrations/postgres/0007_scheduler_quality_rollups.sql
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - SchedulerQualityRollup includes CoverageLevel, AnomalyCount, AnomalyRate, and AnomalyUnavailableCount.
    - NormalizeSchedulerQualityRollup computes anomaly_rate = anomaly_count / sample_count when sample_count > 0.
    - MergeSchedulerQualityRollups sums anomaly_count and anomaly_unavailable_count and recomputes anomaly_rate.
    - SQLite and PostgreSQL repositories persist and scan the new fields with migration parity.
  </behavior>
  <action>Add CoverageLevel string, AnomalyCount int64, AnomalyRate float64, and AnomalyUnavailableCount int64 to SchedulerQualityRollup. Extend NormalizeSchedulerQualityRollup, MergeSchedulerQualityRollups, sqlite/postgres column lists, scan functions, value builders, and conflict keys to include coverage_level. Add SQLite migration 0010 and PostgreSQL migration 0009 that add coverage_level with default 'none', anomaly_count default 0, anomaly_rate default 0, and anomaly_unavailable_count default 0. If an existing unique key prevents coverage-level grouping, migrate to a unique key including coverage_level for bucket_start, scheduler_type, scheduler_version, task_type, model_class, and coverage_level.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate/...</automated>
  </verify>
  <acceptance_criteria>
    - Repository tests or migration-backed tests prove SQLite and PostgreSQL rollups round-trip coverage_level and anomaly fields.
    - MergeSchedulerQualityRollups recomputes anomaly_rate after merging.
    - ListByWindow still filters by scheduler_type, scheduler_version, and task_type while returning coverage_level in results.
  </acceptance_criteria>
  <done>Durable quality evidence can group anomaly behavior by coverage level with SQLite/PostgreSQL parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record anomaly counts from scheduler task metadata</name>
  <files>internal/scheduler/quality.go, internal/scheduler/quality_test.go</files>
  <read_first>
    - internal/scheduler/quality.go
    - internal/scheduler/quality_test.go
    - internal/scheduler/training.go
    - internal/scheduler/types.go
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - qualityRollup writes CoverageLevel from task.Feature.CoverageLevel with missing value normalized to "none".
    - anomaly_status "ood" increments AnomalyCount.
    - anomaly_status "unavailable" or "degraded" increments AnomalyUnavailableCount.
    - anomaly unavailable/degraded does not increment ErrorCount or scheduler fallback counts.
  </behavior>
  <action>Extend qualityScoreEvidence to read anomaly_status from task metadata written by 18-02 and coverage_level from TaskFeature. Update qualityRollup so anomaly_status "ood" sets AnomalyCount=1, anomaly_status "unavailable" or "degraded" sets AnomalyUnavailableCount=1, and all other statuses leave both at 0. Keep existing MAPE, wait, scheduler call latency, error, confidence, and safe sample link behavior unchanged.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/quality_test.go proves an OOD task records AnomalyCount=1 and AnomalyRate=1 after normalization.
    - internal/scheduler/quality_test.go proves degraded/unavailable records AnomalyUnavailableCount=1 and does not increment ErrorCount.
    - Existing MAPE, wait, call latency, confidence, and safe sample ID behavior remains covered.
  </acceptance_criteria>
  <done>Prediction-quality recording carries anomaly evidence without conflating it with scheduler errors or fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add live comparison metrics with coverage and anomaly labels</name>
  <files>internal/observability/metrics.go, internal/observability/prometheus.go, internal/scheduler/quality.go, internal/scheduler/quality_test.go</files>
  <read_first>
    - internal/observability/metrics.go
    - internal/observability/prometheus.go
    - internal/scheduler/quality.go
    - .planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Live comparison metrics can include coverage_level and anomaly_status labels only when labels are bounded.
    - Scheduler fallback/error metrics remain separate from anomaly unavailable/degraded metrics.
    - Existing metric methods keep backward-compatible stub behavior for tests.
  </behavior>
  <action>Extend metrics interfaces and Prometheus collectors so scheduler prediction MAPE, comparison wait, comparison call latency, and anomaly count can be recorded by scheduler_type, scheduler_version, task_type, coverage_level, and anomaly_status where appropriate. Sanitize coverage_level to "none", "fallback", "tenant", or "all"; sanitize anomaly_status to "normal", "ood", "unavailable", or "degraded". Keep tenant IDs, API-key identity, task IDs, model_class, raw prompts, embeddings, semantic-cache payloads, authorization headers, provider secrets, threshold values, and validation errors out of live metric labels.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/observability ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove metrics use sanitized coverage_level and anomaly_status labels for OOD and unavailable paths.
    - Tests prove anomaly unavailable/degraded does not call IncSchedulerComparisonError or scheduler fallback metrics.
    - Existing observability tests still pass with StubMetrics.
  </acceptance_criteria>
  <done>Operators can compare anomaly rate, MAPE, and fallback behavior with low-cardinality live metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler task metadata -> quality recorder | Runtime anomaly status becomes durable evidence and must be sanitized. |
| Control-state migrations -> repositories | Durable schema changes must preserve SQLite/PostgreSQL parity. |
| Quality recorder -> live metrics | Only low-cardinality labels may leave the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | internal/observability/prometheus.go | mitigate | Restrict live labels to scheduler_type, scheduler_version, task_type, coverage_level, and anomaly_status; never emit tenant IDs, API-key identity, task IDs, prompts, embeddings, threshold values, or validation errors. |
| T-18-03-02 | Tampering | control-state migrations | mitigate | Add migration tests/round trips for SQLite and PostgreSQL parity before repositories write anomaly fields. |
| T-18-03-03 | Repudiation | quality rollups | mitigate | Store anomaly_count, anomaly_rate, and anomaly_unavailable_count separately so degraded anomaly metadata cannot be mistaken for scheduler fallback or hidden in MAPE alone. |
| T-18-03-04 | Denial of Service | metrics cardinality | mitigate | Sanitize coverage_level and anomaly_status to closed enum sets and keep model_class out of live metrics. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/controlstate/... ./internal/scheduler ./internal/observability`
- `go build ./...`
</verification>

<success_criteria>
- ANOM-04 is satisfied: quality rollups and metrics compare anomaly rate, MAPE, and fallback behavior by scheduler version, task type, and coverage level.
- D-15 through D-18 are represented in durable evidence, live metrics, and tests.
- SQLite and PostgreSQL migration/repository parity is preserved.
- Anomaly unavailable/degraded is not counted as scheduler backend fallback.
</success_criteria>

## Artifacts this phase produces

- SchedulerQualityRollup fields: CoverageLevel, AnomalyCount, AnomalyRate, AnomalyUnavailableCount.
- Migrations: SQLite 0010 scheduler quality anomaly migration, PostgreSQL 0009 scheduler quality anomaly migration.
- Metadata key: scheduler anomaly status for quality recording.
- Metrics labels: coverage_level and anomaly_status closed enum labels.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP success 4 | Quality rollups and metrics compare anomaly rate, MAPE, and fallback behavior | Covered by all tasks. |
| ANOM-04 | Rollups/metrics compare anomaly behavior by scheduler version and task type | Covered by all tasks, extended with coverage_level per D-15. |
| CONTEXT D-15 | Compare by scheduler_version + task_type + coverage_level | Covered by Tasks 1, 2, and 3. |
| CONTEXT D-16 | Durable anomaly_count, anomaly_rate, anomaly_unavailable_count | Covered by Tasks 1 and 2. |
| CONTEXT D-17 | Low-cardinality coverage_level and anomaly_status live labels | Covered by Task 3. |
| CONTEXT D-18 | Anomaly unavailable/degraded separate from scheduler fallback rate | Covered by Tasks 2 and 3. |
| Out of scope | Offline threshold computation and runtime conservative scoring | Covered by 18-01 and 18-02. |

<output>
Create `.planning/phases/18-anomaly-and-ood-conservative-scoring/18-03-SUMMARY.md` when done.
</output>

