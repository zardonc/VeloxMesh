---
phase: 18-anomaly-and-ood-conservative-scoring
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - internal/scheduler/onnx/artifact.go
  - internal/scheduler/onnx/artifact_test.go
  - internal/scheduler/onnx/scorer.go
  - internal/scheduler/onnx/scorer_test.go
  - internal/scheduler/types.go
  - internal/scheduler/heuristic/score.go
  - internal/scheduler/heuristic/score_test.go
  - internal/observability/metrics.go
  - internal/observability/prometheus.go
  - cmd/scheduler/main.go
autonomous: true
requirements: ["ANOM-02", "ANOM-03"]
must_haves:
  truths:
    - "D-07 missing anomaly metadata does not fail startup; ONNX scoring remains available with anomaly unavailable."
    - "D-08 structurally invalid anomaly metadata disables anomaly/OOD behavior only; main ONNX scoring remains available."
    - "D-09 degraded anomaly state is visible in metrics, logs, and admin/status output."
    - "D-10 metrics/status use low-cardinality enum reasons, with detailed validation errors only in logs."
    - "D-11/D-12 OOD hits lower confidence through severity-scaled clamp with a non-zero floor."
    - "D-13 OOD severity maps into TaskFeature.UncertaintyHint and reuses the heuristic uncertainty penalty path."
    - "D-14 OOD severity is relative exceedance: max((observed_distance - threshold) / threshold, 0)."
  artifacts:
    - path: "internal/scheduler/onnx/artifact.go"
      provides: "anomaly metadata validation and degradation state"
    - path: "internal/scheduler/onnx/scorer.go"
      provides: "confidence and uncertainty adjustment for OOD tasks"
    - path: "cmd/scheduler/main.go"
      provides: "startup log/status visibility for anomaly metadata state"
  key_links:
    - from: "internal/scheduler/onnx/scorer.go"
      to: "internal/scheduler/heuristic/score.go"
      via: "scorer adjusts ConfidenceHint and UncertaintyHint before ScoreCalculator.Score"
    - from: "internal/scheduler/onnx/artifact.go"
      to: "cmd/scheduler/main.go"
      via: "artifact anomaly status is surfaced without failing ONNX startup"
---

<objective>
Apply anomaly/OOD metadata at ONNX scheduler runtime without changing the scheduler RPC contract.

Purpose: ANOM-02 and ANOM-03 require ONNX Scheduler to degrade clearly when anomaly metadata is absent or invalid, and to score unfamiliar tasks more conservatively through confidence and uncertainty signals.
Output: Runtime anomaly validation/status and severity-based confidence/uncertainty adjustment that feeds the existing heuristic score calculator path.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
@.planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
@internal/scheduler/onnx/artifact.go
@internal/scheduler/onnx/scorer.go
@internal/scheduler/types.go
@internal/scheduler/heuristic/score.go
@cmd/scheduler/main.go
@internal/observability/metrics.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate anomaly metadata as degraded optional artifact state</name>
  <files>internal/scheduler/onnx/artifact.go, internal/scheduler/onnx/artifact_test.go, cmd/scheduler/main.go</files>
  <read_first>
    - internal/scheduler/onnx/artifact.go
    - internal/scheduler/onnx/artifact_test.go
    - cmd/scheduler/main.go
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Missing anomaly_thresholds yields AnomalyStatus "unavailable" and AnomalyReason "missing_metadata" while LoadArtifact succeeds.
    - Empty anomaly_thresholds yields AnomalyStatus "unavailable" and AnomalyReason "missing_metadata" while LoadArtifact succeeds.
    - Invalid threshold entries yield AnomalyStatus "degraded" and AnomalyReason "invalid_metadata" while LoadArtifact succeeds.
    - Unsupported coverage labels yield AnomalyStatus "degraded" and AnomalyReason "invalid_metadata" while LoadArtifact succeeds.
    - Unsupported feature schema, target, parity, checksum, and semantic feature errors still fail startup as before.
  </behavior>
  <action>Add low-cardinality anomaly status fields to Artifact or Manifest-derived runtime state: AnomalyStatus values "available", "unavailable", and "degraded"; AnomalyReason values "ok", "missing_metadata", "invalid_metadata", and "sparse_threshold". Validate anomaly_thresholds structurally after existing manifest validation. Treat missing/empty/invalid anomaly metadata as optional degradation only, returning a loadable Artifact with detailed validation errors retained for logs. A valid entry must have Threshold greater than 0, SampleCount greater than 0, Mean greater than or equal to 0, and Stddev greater than or equal to 0. Accepted coverage keys are "none", "fallback", "tenant", and "all". Update cmd/scheduler/main.go startup logging/status to expose only AnomalyStatus and AnomalyReason while logging detailed validation errors locally.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/onnx ./cmd/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/onnx/artifact_test.go proves missing and invalid anomaly metadata do not fail LoadArtifact.
    - internal/scheduler/onnx/artifact_test.go proves invalid core artifact metadata still fails LoadArtifact.
    - cmd/scheduler startup/status output contains only low-cardinality anomaly status/reason fields, not threshold values or task identifiers.
  </acceptance_criteria>
  <done>ONNX artifacts expose anomaly degradation state without breaking the main ONNX scoring path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lower confidence and raise uncertainty for OOD tasks</name>
  <files>internal/scheduler/onnx/scorer.go, internal/scheduler/onnx/scorer_test.go, internal/scheduler/heuristic/score.go, internal/scheduler/heuristic/score_test.go, internal/scheduler/types.go</files>
  <read_first>
    - internal/scheduler/onnx/scorer.go
    - internal/scheduler/onnx/scorer_test.go
    - internal/scheduler/heuristic/score.go
    - internal/scheduler/types.go
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Runtime chooses threshold by task.RequestKind + task.CoverageLevel, then falls back to task.RequestKind + "all".
    - If no threshold exists, scoring proceeds unchanged and anomaly status is unavailable for that task.
    - observed_distance uses the same formula as 18-01: max(latency_p90_ms - latency_p50_ms, 0) / max(latency_p50_ms, 1) + timeout_rate + max(1 - success_rate, 0) + max(1 - coverage_ratio, 0).
    - severity equals max((observed_distance - threshold) / threshold, 0).
    - severity 0 leaves confidence and uncertainty unchanged.
    - severity greater than 0 lowers confidence with a floor of 0.05 and increases UncertaintyHint before ScoreCalculator.Score runs.
  </behavior>
  <action>Add a small anomaly scoring helper in internal/scheduler/onnx/scorer.go. Before calling `s.calculator.Score(task)`, normalize semantic aggregates, select the anomaly threshold, compute observed_distance and severity, then set `task.ConfidenceHint` to the lower of the existing hint/base confidence and `max(0.05, current_confidence * (1 / (1 + severity)))`. Increase `task.UncertaintyHint` by `min(severity, 5.0)`. Keep ScoreResult and scheduler proto unchanged. After ScoreCalculator.Score returns, set score.Confidence from the adjusted confidence path so D-11/D-12 are visible in existing ScoreResult. Add heuristic score tests proving higher UncertaintyHint increases virtual-deadline score through the existing uncertainty penalty.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/onnx ./internal/scheduler/heuristic</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/onnx/scorer_test.go proves an OOD task has lower Confidence and higher Score than the same task under threshold.
    - internal/scheduler/onnx/scorer_test.go proves threshold fallback from coverage_level to "all".
    - internal/scheduler/onnx/scorer_test.go proves missing/degraded anomaly metadata leaves existing ONNX scoring behavior unchanged except status reporting.
    - internal/scheduler/heuristic/score_test.go proves UncertaintyHint contributes to the existing uncertainty penalty path.
  </acceptance_criteria>
  <done>OOD severity makes unfamiliar ONNX-scored tasks more conservative through confidence and uncertainty only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface anomaly status through low-cardinality metrics</name>
  <files>internal/observability/metrics.go, internal/observability/prometheus.go, internal/scheduler/onnx/scorer.go, internal/scheduler/onnx/scorer_test.go</files>
  <read_first>
    - internal/observability/metrics.go
    - internal/observability/prometheus.go
    - internal/scheduler/onnx/scorer.go
    - .planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Metrics count anomaly status using only scheduler_version, task_type, coverage_level, and anomaly_status labels.
    - Metrics count anomaly metadata degradation separately from scheduler fallback.
    - Detailed validation errors go to logs only and are not metric labels.
  </behavior>
  <action>Add metrics interface methods and Prometheus implementation for ONNX anomaly status, using bounded labels scheduler_version, task_type, coverage_level, and anomaly_status. Emit anomaly_status values "normal", "ood", "unavailable", and "degraded" from the ONNX scorer. Do not increment scheduler fallback/error counters for missing or invalid anomaly metadata. Ensure coverage_level is sanitized to "none", "fallback", "tenant", or "all".</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/onnx ./internal/observability</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove an OOD hit records anomaly_status="ood".
    - Tests prove missing anomaly metadata records anomaly_status="unavailable" without changing scheduler fallback rate.
    - Metrics labels never include tenant IDs, API-key identity, task IDs, raw prompts, embeddings, semantic-cache payloads, authorization headers, provider secrets, threshold values, or validation error text.
  </acceptance_criteria>
  <done>Anomaly runtime state is observable with sanitized labels and does not contaminate scheduler fallback metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact manifest -> runtime scorer | Offline metadata controls confidence/uncertainty adjustment and must be validated. |
| Runtime scorer -> metrics/status | Only low-cardinality status/reason labels may leave the scorer. |
| ONNX scorer -> existing scheduler contract | ScoreResult contract remains unchanged; conservative behavior uses existing fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | internal/scheduler/onnx/artifact.go | mitigate | Validate anomaly threshold structure and disable anomaly behavior on invalid metadata while keeping core ONNX validation strict. |
| T-18-02-02 | Denial of Service | internal/scheduler/onnx/scorer.go | mitigate | Clamp confidence floor to 0.05 and UncertaintyHint severity contribution to 5.0 so malformed-but-accepted metadata cannot create unbounded scores. |
| T-18-02-03 | Information Disclosure | metrics/status/logs | mitigate | Emit only bounded enum status/reason labels in metrics/status and keep detailed validation errors in local logs only. |
| T-18-02-04 | Spoofing | scheduler RPC callers | mitigate | Do not add request-controllable anomaly fields to the scheduler RPC; derive severity from existing bounded TaskFeature fields and artifact metadata. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/scheduler/onnx ./internal/scheduler/heuristic ./internal/observability ./cmd/scheduler`
- `go build ./...`
</verification>

<success_criteria>
- ANOM-02 is satisfied: ONNX Scheduler validates anomaly metadata and reports degraded/unavailable behavior without breaking valid ONNX startup.
- ANOM-03 is satisfied: OOD tasks lower confidence, raise uncertainty, and receive more conservative virtual-deadline scores.
- Scheduler RPC and public data-plane contracts remain unchanged.
- D-07 through D-14 are represented in code and tests.
</success_criteria>

## Artifacts this phase produces

- Runtime status fields: AnomalyStatus, AnomalyReason.
- Constants/enums: anomaly status values available/unavailable/degraded and normal/ood/unavailable/degraded metrics values.
- Functions/helpers: validateAnomalyThresholds, anomalyDistance, anomalySeverity, applyAnomalyAdjustment.
- Metrics methods: RecordSchedulerAnomalyStatus or equivalent low-cardinality anomaly status recorder.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP success 2 | ONNX Scheduler validates anomaly metadata and reports clear degraded/fallback behavior | Covered by Tasks 1 and 3. |
| ROADMAP success 3 | OOD tasks lower confidence, increase uncertainty, and score conservatively | Covered by Task 2. |
| ANOM-02 | Startup validation and degraded/missing behavior | Covered by Tasks 1 and 3. |
| ANOM-03 | Confidence/uncertainty/scoring conservatism | Covered by Task 2. |
| CONTEXT D-07/D-08/D-09/D-10 | Missing/invalid metadata degrades only anomaly behavior and is visible safely | Covered by Tasks 1 and 3. |
| CONTEXT D-11/D-12/D-13/D-14 | Severity-based confidence clamp and uncertainty penalty reuse | Covered by Task 2. |
| Out of scope | Durable quality rollups and migration parity | Deferred to 18-03. |

<output>
Create `.planning/phases/18-anomaly-and-ood-conservative-scoring/18-02-SUMMARY.md` when done.
</output>

