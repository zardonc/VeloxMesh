---
phase: 18-anomaly-and-ood-conservative-scoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/scheduler_training/scheduler_training/train.py
  - tools/scheduler_training/scheduler_training/artifacts.py
  - tools/scheduler_training/tests/test_train_publish.py
  - tools/scheduler_training/tests/test_export_schema.py
  - internal/scheduler/onnx/artifact.go
  - internal/scheduler/onnx/artifact_test.go
autonomous: true
requirements: ["ANOM-01"]
must_haves:
  truths:
    - "D-01/D-04 anomaly thresholds are published as anomaly_thresholds[task_type][coverage_level]."
    - "D-02 thresholds are computed from successful safe scheduler samples only; failure and timeout samples are comparison evidence only."
    - "D-03 sparse task_type+coverage_level buckets fall back to task_type-level thresholds, and sparse task_type buckets are marked unavailable."
    - "D-05 each threshold entry contains threshold, sample_count, mean, and stddev."
    - "D-06 tooling computes mean+k*stddev and percentile thresholds, then publishes the more conservative threshold."
  artifacts:
    - path: "tools/scheduler_training/scheduler_training/train.py"
      provides: "anomaly distance calculation and grouped threshold computation"
    - path: "tools/scheduler_training/scheduler_training/artifacts.py"
      provides: "manifest publish support for anomaly_thresholds"
    - path: "internal/scheduler/onnx/artifact.go"
      provides: "Manifest structs for anomaly threshold metadata"
  key_links:
    - from: "tools/scheduler_training/scheduler_training/train.py"
      to: "tools/scheduler_training/scheduler_training/artifacts.py"
      via: "training result carries anomaly_thresholds into manifest.json"
    - from: "tools/scheduler_training/scheduler_training/export.py"
      to: "tools/scheduler_training/scheduler_training/train.py"
      via: "exported safe scheduler samples provide request_kind, coverage_level, success_rate, timeout_rate, and coverage_ratio"
---

<objective>
Publish anomaly/OOD threshold metadata in versioned ONNX scheduler artifacts.

Purpose: ANOM-01 and D-01 through D-06 require offline tooling to derive conservative thresholds from safe samples and write them into the normal artifact manifest.
Output: Python training/publish changes plus Go manifest structs so later runtime plans can validate and consume anomaly_thresholds without adding sidecar files.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
@.planning/phases/17-semantic-neighbor-feature-aggregates/17-CONTEXT.md
@.planning/phases/15-training-feedback-and-onnx-path/15-CONTEXT.md
@tools/scheduler_training/scheduler_training/export.py
@tools/scheduler_training/scheduler_training/train.py
@tools/scheduler_training/scheduler_training/artifacts.py
@internal/scheduler/onnx/artifact.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute grouped anomaly thresholds from successful safe samples</name>
  <files>tools/scheduler_training/scheduler_training/train.py, tools/scheduler_training/tests/test_train_publish.py</files>
  <read_first>
    - tools/scheduler_training/scheduler_training/train.py
    - tools/scheduler_training/scheduler_training/export.py
    - tools/scheduler_training/tests/test_train_publish.py
    - tools/scheduler_training/tests/test_export_schema.py
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Successful rows are the only rows used to compute anomaly thresholds.
    - Failure and timeout rows are counted in comparison evidence but do not affect threshold, mean, stddev, or percentile values.
    - Threshold groups are keyed by request_kind as task_type and coverage_level.
    - Sparse coverage-level groups fall back to an "all" coverage threshold for the same task_type.
    - Sparse task_type groups set anomaly metadata unavailable for that task_type rather than guessing.
  </behavior>
  <action>Add anomaly/OOD threshold computation to train.py. Use the scalar anomaly_distance formula `max(latency_p90_ms - latency_p50_ms, 0) / max(latency_p50_ms, 1) + timeout_rate + max(1 - success_rate, 0) + max(1 - coverage_ratio, 0)` for each successful row. Group by `request_kind` and `coverage_level`, with missing request_kind mapped to `unknown` and missing coverage_level mapped to `none`. For each group with at least `ANOMALY_MIN_SAMPLES=20`, compute `mean`, population `stddev`, `mean_stddev_threshold = mean + ANOMALY_STDDEV_K * stddev` with `ANOMALY_STDDEV_K=3.0`, `percentile_threshold` using q=0.95, and publish `threshold = max(mean_stddev_threshold, percentile_threshold)`. Also compute an `all` coverage-level threshold per task_type from all successful rows for that task_type. Return anomaly_thresholds and anomaly_evidence counts from train_p70/train_file.</action>
  <verify>
    <automated>uv run pytest tools/scheduler_training/tests/test_train_publish.py tools/scheduler_training/tests/test_export_schema.py</automated>
  </verify>
  <acceptance_criteria>
    - tools/scheduler_training/tests/test_train_publish.py asserts successful samples produce anomaly_thresholds[task_type][coverage_level] entries with threshold, sample_count, mean, and stddev.
    - A test proves failure and timeout rows change anomaly_evidence counts but not the computed threshold for the same successful rows.
    - A test proves a sparse coverage_level omits that level while preserving the task_type "all" fallback when the task_type has at least 20 successful samples.
    - A test proves a task_type with fewer than 20 successful samples has no guessed threshold entry.
  </acceptance_criteria>
  <done>Training produces deterministic anomaly thresholds from safe successful samples and comparison evidence for non-success outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish anomaly metadata in the runtime manifest</name>
  <files>tools/scheduler_training/scheduler_training/artifacts.py, tools/scheduler_training/tests/test_train_publish.py</files>
  <read_first>
    - tools/scheduler_training/scheduler_training/artifacts.py
    - tools/scheduler_training/scheduler_training/train.py
    - tools/scheduler_training/tests/test_train_publish.py
    - .planning/phases/15-training-feedback-and-onnx-path/15-CONTEXT.md
  </read_first>
  <behavior>
    - manifest.json includes anomaly_thresholds as nested JSON keyed by task_type and coverage_level.
    - manifest.json includes anomaly_evidence counts for success, failure, timeout, and unavailable_threshold task types.
    - Runtime artifact directories still contain only model.onnx, manifest.json, checksum/metadata, and no raw exported datasets or bulky training logs.
  </behavior>
  <action>Extend artifact publish/build logic to copy `anomaly_thresholds` and `anomaly_evidence` from the training result into manifest.json. Preserve existing feature_schema_version, semantic_aggregate_features, semantic_aggregates_supported, model_parameters, model_sha256, and onnx_parity behavior. Do not create a sidecar anomaly file.</action>
  <verify>
    <automated>uv run pytest tools/scheduler_training/tests/test_train_publish.py</automated>
  </verify>
  <acceptance_criteria>
    - Published manifest JSON contains `anomaly_thresholds.simple_qa.tenant.threshold`, `sample_count`, `mean`, and `stddev` for a seeded successful sample set.
    - Published manifest JSON contains task_type-level fallback at `anomaly_thresholds.simple_qa.all`.
    - Published runtime artifact output does not include raw JSONL exports, prompts, embeddings, authorization headers, API keys, provider secrets, or original request payloads.
  </acceptance_criteria>
  <done>Versioned ONNX artifacts carry D-04/D-05 anomaly metadata in manifest.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Go manifest structs for anomaly metadata</name>
  <files>internal/scheduler/onnx/artifact.go, internal/scheduler/onnx/artifact_test.go</files>
  <read_first>
    - internal/scheduler/onnx/artifact.go
    - internal/scheduler/onnx/artifact_test.go
    - .planning/phases/18-anomaly-and-ood-conservative-scoring/18-CONTEXT.md
  </read_first>
  <behavior>
    - Manifest unmarshalling preserves anomaly_thresholds[task_type][coverage_level].
    - ThresholdEntry exposes Threshold, SampleCount, Mean, and Stddev fields.
    - This plan does not make missing anomaly metadata fail startup; validation and degradation are handled in 18-02.
  </behavior>
  <action>Add Manifest.AnomalyThresholds with type `map[string]map[string]AnomalyThreshold`, Manifest.AnomalyEvidence with a small struct or map for evidence counts, and AnomalyThreshold fields Threshold, SampleCount, Mean, and Stddev with matching JSON tags. Add artifact tests that load a valid manifest containing anomaly_thresholds while preserving current checksum, feature schema, target, ONNX parity, and semantic feature validation behavior.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/onnx</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/onnx/artifact_test.go asserts nested threshold metadata is available after LoadArtifact.
    - Existing invalid schema, target, parity, checksum, and semantic feature tests still pass.
    - Missing anomaly_thresholds still loads as a valid artifact in this plan.
  </acceptance_criteria>
  <done>Go runtime can read anomaly threshold metadata without changing startup behavior yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Control-state samples -> offline tooling | Only safe SchedulerTrainingSample fields enter threshold computation. |
| Offline artifact -> scheduler runtime | Manifest metadata crosses from Python tooling into Go runtime validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information Disclosure | tools/scheduler_training | mitigate | Use exported safe scheduler fields only; do not publish raw prompts, embeddings, semantic-cache payloads, auth headers, API keys, provider secrets, tenant/API-key identity, or original payloads in manifest or tests. |
| T-18-01-02 | Tampering | manifest.json | mitigate | Keep checksum/parity validation unchanged and add typed Go fields so later validation can reject structurally invalid anomaly metadata. |
| T-18-01-03 | Denial of Service | sparse threshold groups | mitigate | Use ANOMALY_MIN_SAMPLES=20 and mark sparse groups unavailable rather than publishing guessed thresholds. |
</threat_model>

<verification>
Run:
- `uv run pytest tools/scheduler_training/tests/test_train_publish.py tools/scheduler_training/tests/test_export_schema.py`
- `go test -timeout 60s ./internal/scheduler/onnx`
- `go build ./...`
</verification>

<success_criteria>
- ANOM-01 is satisfied: offline tooling computes and publishes anomaly/OOD thresholds in versioned scheduler artifacts.
- D-01 through D-06 are represented in tests and artifact shape.
- Runtime artifact output remains secret-safe and does not include raw datasets or sensitive request content.
</success_criteria>

## Artifacts this phase produces

- Python constants: ANOMALY_MIN_SAMPLES, ANOMALY_STDDEV_K, ANOMALY_PERCENTILE.
- Python functions: anomaly_distance, compute_anomaly_thresholds.
- Manifest fields: anomaly_thresholds, anomaly_evidence.
- Go types/fields: Manifest.AnomalyThresholds, Manifest.AnomalyEvidence, AnomalyThreshold.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP success 1 | Offline tooling computes anomaly/OOD thresholds and publishes versioned artifacts | Covered by Tasks 1 and 2. |
| ANOM-01 | Tooling computes thresholds from safe scheduler samples and publishes artifacts | Covered by all tasks. |
| CONTEXT D-01/D-04 | Thresholds by task_type + coverage_level and nested manifest shape | Covered by Tasks 1, 2, and 3. |
| CONTEXT D-02 | Successful samples only, failure/timeout comparison evidence separate | Covered by Task 1. |
| CONTEXT D-03 | Sparse coverage fallback to task_type threshold, otherwise unavailable | Covered by Task 1. |
| CONTEXT D-05/D-06 | threshold/sample_count/mean/stddev and conservative max(mean+k*stddev, percentile) | Covered by Task 1. |
| Out of scope | Runtime scoring changes and quality rollups | Deferred to 18-02 and 18-03. |

<output>
Create `.planning/phases/18-anomaly-and-ood-conservative-scoring/18-01-SUMMARY.md` when done.
</output>

