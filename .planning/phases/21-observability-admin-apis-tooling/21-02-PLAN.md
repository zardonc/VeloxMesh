---
phase: 21-observability-admin-apis-tooling
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/http/router.go
  - internal/http/handlers/admin_scheduler.go
  - internal/http/handlers/admin_scheduler_test.go
  - internal/scheduler/admin_scheduler_service.go
  - internal/controlstate/repository.go
  - internal/controlstate/sqlite/scheduler_training_samples.go
  - internal/controlstate/postgres/scheduler_training_samples.go
  - internal/controlstate/sqlite/scheduler_training_samples_test.go
  - internal/controlstate/postgres/scheduler_training_samples_test.go
  - internal/scheduler/semantic_neighbors.go
  - internal/scheduler/semantic_neighbors_test.go
autonomous: true
requirements: ["OBS-04", "QDR-07"]
must_haves:
  truths:
    - "D-11 training export supports JSON and NDJSON; JSON is default."
    - "D-12 export filters are optional: start, end, task_type, and limit; no filters returns a recent capped export."
    - "D-13 exported samples use split features and labels objects."
    - "D-14 exported samples include low-cardinality safe fields only and exclude tenant IDs, user IDs, raw text, prompts, embeddings, provider payloads, auth headers, API keys, and secrets."
    - "D-15 export limit defaults to 1000 and maxes at 10000."
    - "D-16 support NDJSON through the smallest existing handler pattern, accepting format=ndjson or Accept: application/x-ndjson."
    - "D-18 SchedulerTrainingSampleRepository.ListByIDs returns found samples only and preserves requested ID order."
  artifacts:
    - path: "internal/controlstate/repository.go"
      provides: "ListByIDs repository contract"
    - path: "internal/http/handlers/admin_scheduler.go"
      provides: "training-sample export handler"
    - path: "internal/scheduler/semantic_neighbors.go"
      provides: "precise semantic-neighbor sample hydration"
  key_links:
    - from: "internal/scheduler/semantic_neighbors.go"
      to: "internal/controlstate/repository.go"
      via: "semantic neighbor hydration calls ListByIDs instead of broad ListByWindow"
---

<objective>
Add safe training-sample export and precise completed-sample hydration by ID.

Purpose: OBS-04 gives operators safe export tooling, and QDR-07 removes semantic-neighbor hydration's broad time-window scan.
Output: JSON/NDJSON export endpoint, safe projection, repository `ListByIDs` in SQLite/PostgreSQL, semantic-neighbor hydration update, and tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
@internal/controlstate/repository.go
@internal/controlstate/sqlite/scheduler_training_samples.go
@internal/controlstate/postgres/scheduler_training_samples.go
@internal/scheduler/training.go
@internal/scheduler/semantic_neighbors.go
@internal/http/handlers/admin_scheduler.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ListByIDs repository support with order preservation</name>
  <files>internal/controlstate/repository.go, internal/controlstate/sqlite/scheduler_training_samples.go, internal/controlstate/postgres/scheduler_training_samples.go, internal/controlstate/sqlite/scheduler_training_samples_test.go, internal/controlstate/postgres/scheduler_training_samples_test.go</files>
  <read_first>
    - internal/controlstate/repository.go
    - internal/controlstate/sqlite/scheduler_training_samples.go
    - internal/controlstate/postgres/scheduler_training_samples.go
    - internal/controlstate/sqlite/scheduler_training_samples_test.go
    - internal/controlstate/postgres/scheduler_training_samples_test.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Add `ListByIDs(ctx context.Context, ids []string) ([]*SchedulerTrainingSample, error)` to `SchedulerTrainingSampleRepository`. Implement SQLite and PostgreSQL with a bounded query for the requested IDs, then reorder the found rows in memory to match the input ID order. Missing IDs are omitted without error. Empty ID input returns an empty slice.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate/sqlite ./internal/controlstate/postgres</automated>
  </verify>
  <acceptance_criteria>
    - SQLite and PostgreSQL tests prove requested order is preserved.
    - SQLite and PostgreSQL tests prove missing IDs are omitted without error.
    - Empty ID input returns `[]` and does not query with invalid SQL.
  </acceptance_criteria>
  <done>Repositories can hydrate exact completed samples efficiently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use ListByIDs for semantic-neighbor hydration</name>
  <files>internal/scheduler/semantic_neighbors.go, internal/scheduler/semantic_neighbors_test.go</files>
  <read_first>
    - internal/scheduler/semantic_neighbors.go
    - internal/scheduler/semantic_neighbors_test.go
    - internal/controlstate/repository.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Replace semantic-neighbor hydration's broad `ListByWindow` call with `Repo.ListByIDs(ctx, ids)` where `ids` are extracted from vector search payloads. Preserve result ordering from the vector search payload by mapping found samples by ID and iterating the original result list. Missing samples reduce coverage without returning an error.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - A semantic-neighbor test proves hydration calls `ListByIDs` with exact vector result IDs.
    - A semantic-neighbor test proves missing IDs do not error and reduce neighbor coverage.
    - A semantic-neighbor test proves output order follows vector result order for found IDs.
  </acceptance_criteria>
  <done>Semantic-neighbor hydration no longer scans a full time window.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add safe training-sample export endpoint</name>
  <files>internal/http/router.go, internal/http/handlers/admin_scheduler.go, internal/http/handlers/admin_scheduler_test.go, internal/scheduler/admin_scheduler_service.go</files>
  <read_first>
    - internal/http/router.go
    - internal/http/handlers/admin_scheduler.go
    - internal/http/handlers/admin_scheduler_test.go
    - internal/scheduler/admin_scheduler_service.go
    - internal/scheduler/training.go
    - internal/controlstate/repository.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Add an admin route such as `GET /admin/v1/scheduler/training-samples/export`. Parse optional `start`, `end`, `task_type`, `limit`, and `format` query parameters. Default limit to 1000, cap at 10000, default format to JSON, and support NDJSON via `format=ndjson` or `Accept: application/x-ndjson`. Project each sample into `{features:{...}, labels:{...}}` with only safe low-cardinality fields from the existing training sample shape.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/http/handlers ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - Handler tests prove JSON is default and NDJSON returns one JSON object per line with content type `application/x-ndjson`.
    - Handler tests prove default limit is 1000 and requested limit above 10000 is rejected or clamped consistently.
    - Export includes safe fields such as model_class, request_kind, priority, semantic coverage, scheduler version/type, outcome, latency/token labels, and provider class.
    - Export excludes tenant IDs, user IDs, raw task text, raw prompts, embeddings, semantic-cache payloads, provider payloads, auth headers, API keys, and secrets.
  </acceptance_criteria>
  <done>Operators can export safe scheduler training data for tooling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> training sample repository | Operators can export selected safe training data. |
| Vector search payload -> repository hydration | Search result IDs drive exact sample lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | training export | mitigate | Use an explicit safe projection and tests proving excluded sensitive fields stay out. |
| T-21-02-02 | Denial of Service | export endpoint | mitigate | Default limit to 1000 and cap at 10000. |
| T-21-02-03 | Tampering | semantic hydration order | mitigate | Preserve vector result order and omit missing IDs without fabricating data. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/controlstate/sqlite ./internal/controlstate/postgres ./internal/scheduler ./internal/http/handlers`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- OBS-04 and QDR-07 are covered.
- Training export is safe and bounded.
- Semantic-neighbor hydration uses precise ID lookup.
</success_criteria>

## Artifacts this phase produces

- Repository method: `SchedulerTrainingSampleRepository.ListByIDs`.
- Route: `GET /admin/v1/scheduler/training-samples/export`.
- Export projection types for split `features` and `labels`.
- Semantic-neighbor hydration path using exact IDs.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| OBS-04 | Safe training-sample export | Task 3. |
| QDR-07 | Precise ID hydration and ListByIDs | Tasks 1 and 2. |
| CONTEXT D-11..D-15, D-18 | Export and hydration decisions | Covered by must_haves and Tasks 1-3. |

<output>
Create `.planning/phases/21-observability-admin-apis-tooling/21-02-SUMMARY.md` when done.
</output>
