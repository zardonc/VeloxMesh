---
phase: 21-observability-admin-apis-tooling
plan: "01"
type: execute
wave: 1
depends_on: ["20-02"]
files_modified:
  - internal/http/router.go
  - internal/http/handlers/admin_scheduler.go
  - internal/http/handlers/admin_scheduler_test.go
  - internal/scheduler/admin_scheduler_service.go
  - internal/scheduler/sla_promotion.go
  - internal/scheduler/executor.go
  - internal/scheduler/intake.go
autonomous: true
requirements: ["SCH-08", "OBS-03"]
must_haves:
  truths:
    - "D-01 add GET /admin/v1/scheduler/status and keep rollout endpoints compatible."
    - "D-02 status responses are partial with warnings when a component is unavailable."
    - "D-03 quality rollups use limit query parameter default 100."
    - "D-04 status exposes queue depth, executor slots, circuit-breaker state, and quality rollups when available."
    - "D-05 status responses never include prompts, embeddings, provider payloads, auth headers, API keys, or secrets."
    - "D-06 runtime SLA-rule updates replace the in-memory rule set atomically."
    - "D-07 runtime SLA changes are in-memory only."
    - "D-08 invalid submitted rule rejects the whole replacement and keeps the prior active rules."
    - "D-09 audit successful replacements with old/new counts and safe rule keys only."
    - "D-10 use existing admin scheduler namespace and auth/writeability protections."
  artifacts:
    - path: "internal/http/handlers/admin_scheduler.go"
      provides: "admin scheduler status and SLA-rule handlers"
    - path: "internal/scheduler/admin_scheduler_service.go"
      provides: "status aggregation and in-memory SLA replacement service"
    - path: "internal/http/router.go"
      provides: "admin scheduler routes under existing protections"
  key_links:
    - from: "internal/http/router.go"
      to: "internal/http/handlers/admin_scheduler.go"
      via: "router registers admin scheduler routes with admin auth and writable middleware"
---

<objective>
Add operator-facing scheduler status and runtime SLA-rule admin APIs.

Purpose: SCH-08 and OBS-03 expose safe scheduler runtime state and let operators replace in-memory SLA promotion rules without restart or persistence.
Output: status endpoint, SLA rules GET/PUT or PATCH endpoint, service methods, route wiring, auth/audit tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
@.planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
@internal/http/router.go
@internal/http/handlers/admin_scheduler.go
@internal/http/handlers/admin_scheduler_test.go
@internal/scheduler/admin_scheduler_service.go
@internal/scheduler/sla_promotion.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduler status endpoint</name>
  <files>internal/http/router.go, internal/http/handlers/admin_scheduler.go, internal/http/handlers/admin_scheduler_test.go, internal/scheduler/admin_scheduler_service.go, internal/scheduler/executor.go, internal/scheduler/intake.go</files>
  <read_first>
    - internal/http/router.go
    - internal/http/handlers/admin_scheduler.go
    - internal/http/handlers/admin_scheduler_test.go
    - internal/scheduler/admin_scheduler_service.go
    - internal/scheduler/executor.go
    - internal/scheduler/intake.go
    - internal/controlstate/repository.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Add `GET /admin/v1/scheduler/status`. Extend `AdminSchedulerService` with a status method that returns queue depth when queue access is available, executor `slots_used` and `slots_total` when executor slot telemetry exists, rollout/circuit-breaker state from the existing controller snapshot when available, and last N quality rollups from `SchedulerQualityRollups().ListByWindow` with query `limit` default 100. Build a `warnings []string` response field for missing repo, missing queue, missing executor slots, or rollup query errors instead of failing the whole response.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/http/handlers ./internal/http ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - Handler tests prove admin auth is required for `GET /admin/v1/scheduler/status`.
    - A handler/service test proves `?limit=5` passes limit 5 to quality rollups and no limit uses 100.
    - A service test proves unavailable rollups produce a warnings entry and still return available status fields.
    - The JSON response contains no raw prompts, embeddings, provider payloads, auth headers, API keys, or secrets.
  </acceptance_criteria>
  <done>Operators can inspect scheduler runtime state from one safe admin response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add runtime SLA rules read and replace API</name>
  <files>internal/http/router.go, internal/http/handlers/admin_scheduler.go, internal/http/handlers/admin_scheduler_test.go, internal/scheduler/admin_scheduler_service.go, internal/scheduler/sla_promotion.go</files>
  <read_first>
    - internal/http/router.go
    - internal/http/handlers/admin_scheduler.go
    - internal/http/handlers/admin_scheduler_test.go
    - internal/scheduler/admin_scheduler_service.go
    - internal/scheduler/sla_promotion.go
    - internal/config/config_validation.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Add an admin scheduler SLA-rules route such as `GET /admin/v1/scheduler/sla-rules` and `PUT /admin/v1/scheduler/sla-rules`. The GET returns the current in-memory safe rule set. The PUT decodes with `DisallowUnknownFields`, validates every rule using the same rule constraints as config validation, builds a new immutable slice, and atomically swaps the active `SLAPromoter` rule set only after all rules pass. Do not persist the replacement to config or control state.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/http/handlers ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - A handler test proves writeability/admin middleware protects the replacement route.
    - A service test proves invalid rule replacement returns 400 and leaves the old rule set unchanged.
    - A service test proves successful replacement swaps all rules atomically.
    - GET returns safe rule keys only: policy_id, tenant_class or tenant selector, model_class, request_kind, and wait_threshold.
  </acceptance_criteria>
  <done>Operators can inspect and replace runtime SLA rules without restart or persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Audit successful runtime rule replacements safely</name>
  <files>internal/scheduler/admin_scheduler_service.go, internal/http/handlers/admin_scheduler_test.go</files>
  <read_first>
    - internal/scheduler/admin_scheduler_service.go
    - internal/controlstate/repository.go
    - internal/controlstate/audit.go
    - internal/http/handlers/admin_scheduler_test.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>On successful SLA-rule replacement, write an audit event with action `scheduler.sla_rules.replace`, old_count, new_count, and safe rule keys only: policy IDs, tenant class where present, model class, and request kind. Do not audit full rule bodies, tenant/user identifiers, prompts, provider payloads, embeddings, auth headers, API keys, or secrets.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/http/handlers ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - A SQLite-backed handler test proves a successful replacement writes one audit event.
    - The audit metadata includes old_count and new_count.
    - The audit metadata excludes full submitted rule bodies, tenant IDs, user IDs, prompts, provider payloads, embeddings, auth headers, API keys, and secrets.
  </acceptance_criteria>
  <done>Runtime tuning changes are traceable without leaking sensitive data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> scheduler runtime | Authenticated operators can read status and replace in-memory rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | status endpoint | mitigate | Return only low-cardinality status, warnings, and quality rollups; exclude raw payloads and secrets. |
| T-21-01-02 | Tampering | SLA rule replacement | mitigate | Validate the whole submitted set and swap atomically only after all rules pass. |
| T-21-01-03 | Repudiation | SLA rule audit | mitigate | Audit successful replacement counts and safe rule keys through existing audit repository. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/http ./internal/http/handlers ./internal/scheduler`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- SCH-08 and OBS-03 are covered.
- Rollout endpoints remain backward-compatible.
- Status and rule APIs use existing admin protections and sanitized data.
</success_criteria>

## Artifacts this phase produces

- Route: `GET /admin/v1/scheduler/status`.
- Routes: `GET /admin/v1/scheduler/sla-rules`, `PUT /admin/v1/scheduler/sla-rules`.
- Service methods for scheduler status and SLA rule replacement.
- Audit action: `scheduler.sla_rules.replace`.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| SCH-08 | Scheduler status endpoint | Task 1. |
| OBS-03 | SLA rules admin API and audit | Tasks 2 and 3. |
| CONTEXT D-01..D-10 | Status and SLA decisions | Covered by must_haves and Tasks 1-3. |

<output>
Create `.planning/phases/21-observability-admin-apis-tooling/21-01-SUMMARY.md` when done.
</output>
