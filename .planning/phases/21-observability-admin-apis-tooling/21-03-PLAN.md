---
phase: 21-observability-admin-apis-tooling
plan: "03"
type: execute
wave: 2
depends_on: ["20-01", "21-02"]
files_modified:
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
  - internal/app/semantic_neighbors.go
  - internal/scheduler/semantic_neighbors.go
  - internal/scheduler/client.go
  - internal/scheduler/types.go
  - internal/scheduler/heuristic/config.go
  - internal/scheduler/heuristic/score.go
  - internal/scheduler/quality.go
  - internal/scheduler/semantic_neighbors_test.go
  - internal/scheduler/client_test.go
  - internal/scheduler/heuristic/score_test.go
  - internal/scheduler/quality_test.go
  - config.heuristic.example.json
autonomous: true
requirements: ["QDR-08", "OBS-05", "OBS-06"]
must_haves:
  truths:
    - "D-17 add scheduler.semantic_neighbors_embedding_model and SCHEDULER_SEMANTIC_NEIGHBORS_EMBEDDING_MODEL defaulting to the current hardcoded model."
    - "D-19 heuristic_config_file supports only base_latency and model-family multiplier overrides in Phase 21."
    - "D-20 provide a small heuristic config template file."
    - "D-21 ScoreResult.SchedulerType is populated everywhere ScoreResult is produced before quality evidence is recorded."
  artifacts:
    - path: "internal/config/config.go"
      provides: "semantic-neighbor embedding model and heuristic config file loading"
    - path: "internal/scheduler/semantic_neighbors.go"
      provides: "configurable embedding model usage"
    - path: "internal/scheduler/client.go"
      provides: "complete SchedulerType attribution across scoring paths"
    - path: "internal/scheduler/heuristic/config.go"
      provides: "narrow heuristic override file loader"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/app/semantic_neighbors.go"
      via: "scheduler.semantic_neighbors_embedding_model flows into SemanticNeighborService"
---

<objective>
Add remaining operator tuning controls and fix SchedulerType attribution.

Purpose: QDR-08, OBS-05, and OBS-06 make semantic-neighbor embedding model selection configurable, let operators tune the heuristic scorer narrowly, and ensure quality rollups attribute scheduler type correctly.
Output: config/env field, semantic-neighbor model wiring, heuristic override loader/template, SchedulerType tests across scoring paths.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
@.planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
@internal/config/config.go
@internal/app/semantic_neighbors.go
@internal/scheduler/semantic_neighbors.go
@internal/scheduler/client.go
@internal/scheduler/types.go
@internal/scheduler/heuristic/config.go
@internal/scheduler/heuristic/score.go
@internal/scheduler/quality.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make semantic-neighbor embedding model configurable</name>
  <files>internal/config/config.go, internal/config/config_validation.go, internal/config/config_test.go, internal/app/semantic_neighbors.go, internal/scheduler/semantic_neighbors.go, internal/scheduler/semantic_neighbors_test.go</files>
  <read_first>
    - internal/config/config.go
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - internal/app/semantic_neighbors.go
    - internal/scheduler/semantic_neighbors.go
    - internal/scheduler/semantic_neighbors_test.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Add `SemanticNeighborsEmbeddingModel string` to `SchedulerConfig` with JSON key `semantic_neighbors_embedding_model` and ENV key `SCHEDULER_SEMANTIC_NEIGHBORS_EMBEDDING_MODEL`. Default it to the current hardcoded model `text-embedding-3-small`. Add a matching field to `SemanticNeighborConfig` and use it in `SemanticNeighborService.embed` instead of the package constant.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config ./internal/scheduler ./internal/app</automated>
  </verify>
  <acceptance_criteria>
    - Config tests prove default model is `text-embedding-3-small`.
    - Config tests prove ENV and JSON override the model.
    - Semantic-neighbor tests prove `EmbeddingRequest.Model` uses the configured value.
    - Empty configured model falls back to `text-embedding-3-small`.
  </acceptance_criteria>
  <done>Operators can select the semantic-neighbor embedding model without code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load narrow heuristic override config</name>
  <files>internal/config/config.go, internal/scheduler/heuristic/config.go, internal/scheduler/heuristic/score.go, internal/scheduler/heuristic/score_test.go, config.heuristic.example.json</files>
  <read_first>
    - internal/config/config.go
    - internal/scheduler/heuristic/config.go
    - internal/scheduler/heuristic/score.go
    - internal/scheduler/heuristic/score_test.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Use the existing `scheduler.heuristic_config_file` value to load a narrow JSON file containing only `base_latency` and `model_multipliers`. Merge those overrides into the heuristic scorer config while preserving defaults for omitted entries. Reject unknown top-level fields with `DisallowUnknownFields`. Add `config.heuristic.example.json` with a tiny safe template.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler/heuristic ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - A heuristic test proves base latency override changes scoring for a known request kind.
    - A heuristic test proves model-family multiplier override changes scoring for a matching model class.
    - A heuristic test proves omitted override entries keep defaults.
    - A config/heuristic test proves unknown fields in the heuristic file fail with an explicit error.
    - `config.heuristic.example.json` contains no secrets, prompts, provider payloads, embeddings, auth headers, API keys, or raw task text.
  </acceptance_criteria>
  <done>Operators can tune the two approved heuristic tables only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate SchedulerType across all scoring paths</name>
  <files>internal/scheduler/client.go, internal/scheduler/types.go, internal/scheduler/quality.go, internal/scheduler/client_test.go, internal/scheduler/quality_test.go</files>
  <read_first>
    - internal/scheduler/client.go
    - internal/scheduler/types.go
    - internal/scheduler/intake.go
    - internal/scheduler/quality.go
    - internal/scheduler/client_test.go
    - internal/scheduler/quality_test.go
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
  </read_first>
  <action>Audit every `ScoreResult` construction path in `internal/scheduler/client.go` and adjacent scorers: FIFO, heuristic, ONNX, predictive, fallback, gRPC/proto decode, weighted/merged paths, and error fallback. Ensure `SchedulerType` is set before `scoreMetadata` records quality evidence. Add a defaulting guard only at the shared score-normalization point if one exists; otherwise set the field at each producer with the smallest diff.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove FIFO scores use `SchedulerTypeFIFO`.
    - Tests prove heuristic scores use `SchedulerTypeHeuristic`.
    - Tests prove ONNX/predictive scores use the ONNX or predictive scheduler type already defined in `types.go`.
    - Tests prove fallback/error paths do not record an empty scheduler_type in `scoreMetadata`.
    - Quality rollup tests prove `qualityRollup` receives a non-empty `SchedulerType` for representative paths.
  </acceptance_criteria>
  <done>Quality rollups attribute MAPE and wait-time metrics by scheduler type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config -> embedding provider | Config selects model identifier sent to embedding provider. |
| Operator config -> heuristic scorer | Heuristic override file changes local scoring tables. |
| Scoring path -> quality rollups | SchedulerType metadata is recorded for observability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | heuristic override file | mitigate | Accept only base_latency and model_multipliers with unknown fields rejected. |
| T-21-03-02 | Information Disclosure | embedding model config and examples | mitigate | Store model identifier only; examples contain no secrets or payloads. |
| T-21-03-03 | Repudiation | scheduler quality attribution | mitigate | Ensure all scoring paths set SchedulerType before quality evidence is recorded. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/config ./internal/scheduler ./internal/scheduler/heuristic ./internal/app`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- QDR-08, OBS-05, and OBS-06 are covered.
- Semantic-neighbor embedding model is configurable.
- Heuristic override scope stays intentionally narrow.
- SchedulerType is non-empty in quality evidence.
</success_criteria>

## Artifacts this phase produces

- Config field/env: `SchedulerConfig.SemanticNeighborsEmbeddingModel`, `SCHEDULER_SEMANTIC_NEIGHBORS_EMBEDDING_MODEL`.
- Heuristic config template: `config.heuristic.example.json`.
- Heuristic override loader for `base_latency` and `model_multipliers`.
- Tests proving non-empty `ScoreResult.SchedulerType` across scoring paths.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| QDR-08 | Configurable embedding model | Task 1. |
| OBS-06 | Heuristic config file and template | Task 2. |
| OBS-05 | SchedulerType rollup attribution | Task 3. |
| CONTEXT D-17, D-19, D-20, D-21 | Operator tuning and attribution decisions | Covered by must_haves and Tasks 1-3. |

<output>
Create `.planning/phases/21-observability-admin-apis-tooling/21-03-SUMMARY.md` when done.
</output>
