---
phase: 24-plan-3-vector-compatibility
plan: "01"
type: execute
wave: 1
depends_on: ["08-01", "09-01", "13-01", "21-01"]
files_modified:
  - internal/app/semantic_cache.go
  - internal/app/semantic_cache_test.go
  - internal/app/semantic_neighbors.go
  - internal/app/app_test.go
  - docs/scheduler-1.0-runbook.md
  - README.md
  - .env.example
autonomous: true
requirements: ["PLAN3-01", "PLAN3-02", "PLAN3-03", "PLAN3-04"]
must_haves:
  truths:
    - "Plan 3 stays single-node and does not change the Plan 1 deployment shape."
    - "Plan 3 defaults to LanceDB when vector store config is absent."
    - "Qdrant is used for Plan 3 only when explicitly configured."
    - "LanceDB and Qdrant have no v7.7 data interop or migration path."
    - "Qdrant semantic cache and semantic-neighbor collection setup remain compatible when configured."
  artifacts:
    - path: "internal/app/semantic_cache.go"
      provides: "LanceDB default and explicit Qdrant vector adapter selection"
    - path: "internal/app/semantic_neighbors.go"
      provides: "semantic-neighbor startup guard around vector collection setup"
---

<objective>
Retroactively record the shipped Phase 24 plan for Plan 3 vector compatibility.

Purpose: keep Plan 3 single-node while documenting and preserving LanceDB-default or explicit-Qdrant vector behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Preserve Plan 3 vector-store selection</name>
  <files>internal/app/semantic_cache.go, internal/app/semantic_cache_test.go</files>
  <read_first>
    - internal/app/semantic_cache.go
    - internal/app/semantic_cache_test.go
    - internal/storage/vector_lancedb.go
    - internal/storage/vector_qdrant.go
  </read_first>
  <action>Keep absent `cache.vector_store` on the LanceDB/default path, keep explicit `qdrant` on the Qdrant adapter path, and degrade safely when the active vector backend is unavailable in the local build/runtime.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/app -run TestNewVectorAdapter</automated>
  </verify>
  <acceptance_criteria>
    - `TestNewVectorAdapterDefaultsToNoopWhenLanceDBUnavailable` passes.
    - `TestNewVectorAdapterExplicitLanceDBDegradesWhenUnavailable` passes.
    - `TestNewQdrantVectorAdapterDegradesWithoutFallbacks` passes.
  </acceptance_criteria>
  <done>Plan 3 vector backend selection remains build-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Keep semantic-neighbor Qdrant setup compatible</name>
  <files>internal/app/semantic_neighbors.go, internal/app/app_test.go</files>
  <read_first>
    - internal/app/semantic_neighbors.go
    - internal/app/app_test.go
    - internal/scheduler/semantic_neighbors.go
  </read_first>
  <action>Ensure semantic-neighbor startup remains disabled unless durable samples, embedding provider, and vector collection setup are available. Preserve Qdrant collection setup behavior when Qdrant is explicitly configured.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/app ./internal/scheduler -run 'SemanticNeighbor|VectorAdapter'</automated>
  </verify>
  <acceptance_criteria>
    - Semantic neighbors stay disabled without durable/vector/embed dependencies.
    - Qdrant semantic-neighbor startup succeeds when real Qdrant config is supplied.
    - Failed Qdrant collection ensure disables semantic neighbors instead of failing gateway startup.
  </acceptance_criteria>
  <done>Qdrant semantic-neighbor compatibility is retained.</done>
</task>

<task type="auto">
  <name>Task 3: Document Plan 3 limits and backend choices</name>
  <files>README.md, docs/scheduler-1.0-runbook.md, .env.example</files>
  <read_first>
    - README.md
    - docs/scheduler-1.0-runbook.md
    - .env.example
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Document Plan 3 as single-node `App + SQLite + LanceDB/Qdrant`, with LanceDB as default when no vector store is configured and Qdrant only when explicitly configured. State that LanceDB and Qdrant data are mutually exclusive in v7.7, with no migration or shared-read path.</action>
  <verify>
    <automated>rg -n "Plan 3|LanceDB|Qdrant|vector_store|no migration" README.md docs/scheduler-1.0-runbook.md .env.example</automated>
  </verify>
  <acceptance_criteria>
    - README lists Plan 3 as single-node.
    - Docs state LanceDB is the Plan 3 default when no vector store is configured.
    - Docs state Qdrant is explicit.
    - Docs state LanceDB/Qdrant interop and migration are out of scope for v7.7.
  </acceptance_criteria>
  <done>Operators can choose Plan 3 vector behavior without assuming data interop.</done>
</task>

</tasks>

<verification>
Run:
- `go test -timeout 60s ./internal/app`
- `go test -timeout 60s ./internal/scheduler`
- `go test -timeout 60s ./tests/integration -run TestSemanticCache -count=1`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- PLAN3-01 through PLAN3-04 are implemented and documented.
- Plan 3 remains single-node.
- LanceDB is the default vector path when no vector store is configured.
- Explicit Qdrant remains compatible with semantic cache and semantic-neighbor setup.
</success_criteria>

## Artifacts this phase produces

- `newLanceDBVectorAdapter`
- `newQdrantVectorAdapter`
- `TestNewVectorAdapterDefaultsToNoopWhenLanceDBUnavailable`
- `TestNewVectorAdapterExplicitLanceDBDegradesWhenUnavailable`
- `TestNewQdrantVectorAdapterDegradesWithoutFallbacks`

