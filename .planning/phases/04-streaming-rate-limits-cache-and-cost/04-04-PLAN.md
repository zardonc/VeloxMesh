---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - internal/llm/types.go
  - internal/providers/adapter.go
  - internal/providers/capabilities.go
  - internal/providers/openai/adapter.go
  - internal/providers/openai/adapter_test.go
  - internal/providers/anthropic/adapter.go
  - internal/providers/anthropic/adapter_test.go
  - internal/providers/gemini/adapter.go
  - internal/providers/gemini/adapter_test.go
autonomous: true
requirements:
  - STRM-01
user_setup: []
must_haves:
  truths:
    - "D-19/D-23: provider-neutral stream contract exists and provider-native parsing stays inside adapter packages."
    - "STRM-01: OpenAI-compatible adapter can emit provider-neutral streaming events."
    - "D-18: provider capabilities report actual streaming support, not roadmap intent."
  artifacts:
    - path: "internal/llm/types.go"
      provides: "provider-neutral stream event and optional usage metadata"
    - path: "internal/providers/adapter.go"
      provides: "optional streaming adapter interface"
    - path: "internal/providers/openai/adapter.go"
      provides: "OpenAI-compatible SSE upstream parser"
  key_links:
    - from: "internal/providers/openai/adapter.go"
      to: "internal/llm/types.go"
      via: "normalized stream events"
      pattern: "StreamEvent|Usage"
---

<objective>
Start the second D-02 theme: streaming, with the provider-neutral contract and first adapter implementation.

Purpose: give gateway orchestration a clean streaming boundary without moving provider-native SSE into handlers.
Output: stream event types, optional adapter interface, OpenAI-compatible streaming support, and capability tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-03-SUMMARY.md
@.planning/phases/02-health-aware-routing/02-10-CONTEXT.md
@internal/llm/types.go
@internal/providers/adapter.go
@internal/providers/openai/adapter.go
@internal/providers/anthropic/adapter.go
@internal/providers/gemini/adapter.go
</context>

## Decision Coverage
D-02 D-19 D-20 D-21 D-22 D-23 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 STRM-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Provider-Neutral Stream Contract</name>
  <files>internal/llm/types.go, internal/providers/adapter.go, internal/providers/capabilities.go, internal/providers/anthropic/adapter.go, internal/providers/anthropic/adapter_test.go, internal/providers/gemini/adapter.go, internal/providers/gemini/adapter_test.go</files>
  <behavior>
    - Stream event can represent content delta, finish reason, done marker, provider/model, and optional usage.
    - Anthropic/Gemini do not claim streaming support until their mapping exists.
    - Unsupported streaming returns structured provider/gateway errors before any response bytes.
    - Stream event/errors do not expose API keys, auth headers, raw prompts, or raw upstream payloads.
  </behavior>
  <action>Add a narrow optional streaming interface next to `ProviderAdapter` per D-23. Keep `Complete` unchanged. Add provider-neutral stream event fields needed by 04-10 settlement: content delta, finish reason, done, usage, provider, and model. Update capability metadata to report actual streaming support per D-18. Do not implement native Anthropic/Gemini streaming in this plan unless existing adapters already have a safe mapped stream path.</action>
  <verify>
    <automated>go test ./internal/providers -run "Capability|Stream"</automated>
    <automated>go test ./internal/providers/anthropic ./internal/providers/gemini -run "Stream|Capability|Unsupported|SecretSafe"</automated>
  </verify>
  <done>Gateway code can depend on a provider-neutral stream contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OpenAI-Compatible Upstream Stream Parser</name>
  <files>internal/providers/openai/adapter.go, internal/providers/openai/adapter_test.go</files>
  <behavior>
    - Adapter sends `stream:true` upstream.
    - SSE `delta.content`, finish reason, `[DONE]`, and usage-if-present map to stream events.
    - Malformed upstream stream returns a structured provider error without raw payload dumps.
    - Request context cancellation stops upstream reads.
  </behavior>
  <action>Implement streaming for `internal/providers/openai` using stdlib `net/http` and bounded buffered reads. Provider-native SSE parsing stays in the adapter per D-23. Do not add a new dependency. Keep usage metadata optional so 04-10 can settle only when providers supply actual usage per D-37.</action>
  <verify>
    <automated>go test ./internal/providers/openai -run "Stream|Capability|SecretSafe|Cancel"</automated>
  </verify>
  <done>OpenAI-compatible upstream streams become provider-neutral events.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: stream-capable adapters can stream with SQLite durable provider config.
Full mode: stream-capable adapters can stream with PostgreSQL/Redis runtime coordination; Redis does not carry stream payloads.
Disabled mode: static local providers can stream if their adapter supports streaming.
</mode_behavior>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream SSE -> adapter | Provider-native event data is parsed and normalized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | stream parser errors | mitigate | Return normalized errors without raw upstream event dumps or secrets per D-42. |
| T-04-04-02 | Denial of Service | stream reads | mitigate | Use request context cancellation and bounded line/event reads. |
| T-04-04-SC | Tampering | package installs | accept | Streaming parser uses Go stdlib only. |
</threat_model>

<verification>
Run `go test ./internal/providers ./internal/providers/openai ./internal/providers/anthropic ./internal/providers/gemini`.
</verification>

<success_criteria>
STRM-01 has a provider-neutral adapter contract and OpenAI-compatible upstream streaming parser.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-04-SUMMARY.md` when done.
</output>
