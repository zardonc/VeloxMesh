---
phase: 04-streaming-rate-limits-cache-and-cost
plan: 12
type: execute
wave: 12
depends_on:
  - 04-11
files_modified:
  - internal/cache/semantic.go
  - internal/cache/semantic_test.go
  - internal/gateway/service.go
  - internal/gateway/service_test.go
  - internal/http/handlers/chat.go
  - internal/http/handlers/chat_test.go
  - internal/app/app.go
  - internal/app/app_test.go
  - tests/integration/chat_test.go
autonomous: true
requirements:
  - CACHE-01
user_setup: []
must_haves:
  truths:
    - "CACHE-01: semantic cache is opt-in and observable with real hit/miss behavior."
    - "D-39/D-40: prior explicit miss headers become true cache hit/miss headers only when semantic cache is intentionally scoped."
    - "D-33/D-37: cache hits are recorded without guessing provider token usage."
  artifacts:
    - path: "internal/cache/semantic.go"
      provides: "bounded cosine-similarity semantic cache service"
    - path: "internal/gateway/service.go"
      provides: "opt-in semantic cache lookup/store around chat completions"
    - path: "internal/http/handlers/chat.go"
      provides: "observable cache headers"
  key_links:
    - from: "internal/gateway/service.go"
      to: "internal/cache/semantic.go"
      via: "lookup before provider call and store after settled success"
      pattern: "SemanticCache|Lookup|Store"
---

<objective>
Deliver opt-in observable semantic cache behavior after usage/cost recording exists.

Purpose: complete CACHE-01 with a minimal real cache that avoids new infrastructure and keeps privacy boundaries explicit.
Output: semantic cache service, mode/config gate, gateway lookup/store integration, cache headers, and tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-CONTEXT.md
@.planning/phases/04-streaming-rate-limits-cache-and-cost/04-11-SUMMARY.md
@internal/gateway/service.go
@internal/http/handlers/chat.go
@internal/app/app.go
</context>

## Decision Coverage
D-01 D-02 D-03 D-33 D-34 D-35 D-37 D-39 D-40 D-41 D-42 D-43 D-44 D-45 D-46 D-47 D-48 D-49 CACHE-01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Minimal Semantic Cache Service</name>
  <files>internal/cache/semantic.go, internal/cache/semantic_test.go</files>
  <behavior>
    - Disabled config always misses and does not call embeddings provider.
    - Enabled config embeds normalized text, loads bounded candidates for same safe scope/model, computes cosine similarity, and returns hit above threshold.
    - Expired/disabled entries and different API-key/model scopes never hit.
    - Service stores successful response entries after provider success when response is cacheable.
  </behavior>
  <action>Create a small `internal/cache` package for semantic cache lookup/store. Use the embeddings interface from 04-11, stdlib math for cosine similarity, bounded candidate count, threshold, TTL, and safe scope/model filtering. Do not add vector DB, Redis cache storage, broad invalidation framework, or exact-cache mode. Keep raw prompt text out of durable storage; normalize only in memory for embedding.</action>
  <verify>
    <automated>go test ./internal/cache -run "Semantic|Hit|Miss|Scope|TTL|SecretSafe"</automated>
  </verify>
  <done>Semantic cache service produces real scoped hit/miss decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Cache Into Gateway and Headers</name>
  <files>internal/gateway/service.go, internal/gateway/service_test.go, internal/http/handlers/chat.go, internal/http/handlers/chat_test.go, internal/app/app.go, internal/app/app_test.go, tests/integration/chat_test.go</files>
  <behavior>
    - With semantic cache disabled, responses keep `X-Cache-Hit:false` and `X-Cache-Level:none`.
    - With semantic cache enabled and a similarity hit, gateway returns cached OpenAI-compatible response without calling provider.
    - Cache hit response includes `X-Cache-Hit:true` and `X-Cache-Level:semantic`.
    - Cache miss calls provider, settles usage per 04-10, then stores cache entry when usage/response is safe.
    - Cache hit records observable usage/cache event without guessing provider token usage or deducting provider credits.
  </behavior>
  <action>Add a mode/config gate in `app.New` and gateway service wiring for semantic cache. The feature is opt-in, requires durable control state and an embedding-capable provider, and fails closed to miss if embeddings/cache repository are unavailable. Apply cache lookup before provider execution for non-streaming chat only; do not cache streaming responses in this phase. Store cache entries only after successful provider response and settlement path has run. Preserve secret-safe logging and headers.</action>
  <verify>
    <automated>go test ./internal/gateway -run "SemanticCache|CacheHit|CacheMiss|Settlement"</automated>
    <automated>go test ./internal/http/handlers -run "Chat|Cache"</automated>
    <automated>go test ./internal/app -run "SemanticCache|Mode"</automated>
    <automated>go test ./tests/integration -run "ChatCompletions|SemanticCache|CacheHeaders"</automated>
    <automated>go test ./...</automated>
  </verify>
  <done>Semantic cache is opt-in, real, observable, and scoped safely.</done>
</task>

</tasks>

<mode_behavior>
Lite mode: semantic cache can run with SQLite durable storage and a configured embedding-capable provider when explicitly enabled.
Full mode: semantic cache can run with PostgreSQL durable storage; concurrent gateway instances share durable entries through PostgreSQL. Redis is not required for cache correctness.
Disabled mode: semantic cache is disabled and responses show explicit miss headers.
</mode_behavior>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 04 | Add streaming, rate limits, cache, cost, and circuit breaker after stable routing/adapters | 04-01..04-12 | COVERED | Split into small ordered slices.
REQ | STRM-01 | SSE streaming proxy | 04-04,04-05 | COVERED | Adapter contract plus HTTP SSE behavior.
REQ | RATE-01 | Rate limits | 04-06,04-07 | COVERED | Credit quota admission, not fixed-window counts per D-24.
REQ | CACHE-01 | Semantic cache | 04-11,04-12 | COVERED | Real opt-in embedding-backed semantic cache after settlement exists.
REQ | COST-01 | Usage and cost tracking | 04-08,04-09,04-10 | COVERED | Rates, Admin management, and settlement.
REQ | CB-01 | Circuit breaker and fallback-chain behavior | 04-03 | COVERED | Closed/open/half-open breaker and fallback-chain tests.
RESEARCH | none | No 04-RESEARCH.md present | N/A | COVERED | No research artifact to plan.
CONTEXT | D-01 | Split Phase 04 into smaller slices | 04-01..04-12 | COVERED | All plans are small and sequential.
CONTEXT | D-02 | Initial order durable, streaming, credit, usage/rates | 04-01..04-10 | COVERED | Order preserved visibly; cache follows usage/cost.
CONTEXT | D-03 | Cache deferred until usage/cost exists | 04-11,04-12 | COVERED | Cache plans depend on 04-10.
CONTEXT | D-04..D-18 | Durable provider hardening | 04-01,04-02 | COVERED | Repos, runtime, readiness, probing.
CONTEXT | D-19..D-23 | Streaming contract | 04-04,04-05 | COVERED | Provider-neutral stream and HTTP SSE.
CONTEXT | D-24..D-32 | Credit quota/admission | 04-06,04-07 | COVERED | API-key credits and 429 admission.
CONTEXT | D-33..D-38 | Usage/cost settlement | 04-08,04-10 | COVERED | Actual usage settlement and unsettled records.
CONTEXT | D-39..D-40 | Cache boundary | 04-11,04-12 | COVERED | Real cache after usage/cost; headers become true hit/miss.
CONTEXT | D-41..D-43 | Secret-safe fake-first tests | 04-01..04-12 | COVERED | Fakes default, local checks opt-in.
CONTEXT | D-44..D-49 | Lite/full modes | 04-01..04-12 | COVERED | Each plan states mode behavior.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user request -> embeddings | Request text is embedded only when semantic cache is enabled. |
| cache hit -> client response | Stored response may bypass provider call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12-01 | Information Disclosure | cache hit scope | mitigate | Scope cache by safe API-key identity and model; no cross-key hits. |
| T-04-12-02 | Tampering | cached response | mitigate | Store only after successful provider response and settlement path; include expiry/enabled checks. |
| T-04-12-03 | Repudiation | cache hits | mitigate | Record observable cache hit/miss metadata without guessing provider token usage. |
| T-04-12-04 | Denial of Service | similarity search | mitigate | Bound candidate count and use in-process cosine over scoped candidates. |
| T-04-12-SC | Tampering | package installs | accept | No package-manager dependencies are added. |
</threat_model>

<verification>
Run `go test ./internal/cache ./internal/gateway ./internal/http/handlers ./internal/app`.
Run `go test ./tests/integration -run "ChatCompletions|SemanticCache|CacheHeaders"`.
Run `go test ./...`.
Run `rg -n "raw prompt|Authorization|Bearer|sk-|api_key" internal/cache internal/gateway internal/http tests` and confirm matches are safe fixtures, redaction, or negative assertions only.
</verification>

<success_criteria>
CACHE-01 is satisfied by an opt-in embedding-backed semantic cache with observable hit/miss behavior and privacy-safe durable storage.
</success_criteria>

<output>
Create `.planning/phases/04-streaming-rate-limits-cache-and-cost/04-12-SUMMARY.md` when done.
</output>
