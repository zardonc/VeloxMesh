---
status: complete
phase: 02-06-active-provider-health-probing-and-recovery
source:
  - .planning/phases/02-health-aware-routing/02-06-CONTEXT.md
  - .planning/phases/02-health-aware-routing/02-06-PLAN.md
started: 2026-06-16T00:00:00-07:00
updated: 2026-06-16T00:00:00-07:00
---

## Current Test

[testing complete]

## Tests

### 1. Health Check Config Shape
expected: |
  Health-check parameters are represented as structured config and JSON fields suitable for future Admin Console/Admin API reuse.
result: pass
evidence: |
  `internal/config/config.go` defines `HealthCheckConfig` and `ProviderHealthCheckConfig`, parses top-level `health_check`, and supports provider-level `health_check` overrides.

### 2. Health Check Config Defaults and Validation
expected: |
  Health-check config has conservative defaults, validates durations/thresholds, and preserves existing config compatibility.
result: pass
evidence: |
  `LoadConfig` defaults `enabled`, `interval`, `timeout`, `initial_delay`, `failure_threshold`, `success_threshold`, `stale_after`, and `max_concurrency`. `internal/config/config_test.go` covers valid config, negative duration, bad thresholds, and invalid provider override duration. Existing config paths still pass `go test ./...`.

### 3. Provider Override Runtime Behavior
expected: |
  Provider-level health-check overrides affect the relevant runtime probe behavior where implemented.
result: pass
evidence: |
  `internal/health/prober.go` resolves provider-specific `timeout` in `getTimeout`. `Start(ctx)` now applies provider-specific `enabled`, `interval`, and `initial_delay`. `internal/app/app.go` applies provider-specific `failure_threshold` and `success_threshold` when ensuring providers in the health store.

### 4. ProbeProvider Boundary
expected: |
  `ProbeProvider(ctx, providerID)` is available as an internal boundary for future Admin Console/Admin API use and returns sanitized structured result data.
result: pass
evidence: |
  `internal/health/prober.go` defines `ProbeProvider`, returning `ProbeResult` with provider id, availability, message, duration, and timestamp. `internal/health/prober_test.go` covers success, failure, and unknown provider.

### 5. ProbeOnce Boundary
expected: |
  `ProbeOnce(ctx)` probes configured providers without calling provider completion/generation APIs.
result: pass
evidence: |
  `internal/health/prober.go` iterates `registry.List()` and calls `ProbeProvider`, which calls only `adapter.HealthCheck(ctx)`. `internal/health/prober_test.go` covers successful and failed provider probes through `ProbeOnce`.

### 6. Probe Timeout and Cancellation
expected: |
  Probe calls honor timeout/cancellation and the background loop exits cleanly on context cancellation.
result: pass
evidence: |
  `ProbeProvider` wraps health checks in `context.WithTimeout`. `TestProber_StartStop` starts `Prober.Start(ctx)`, cancels the context, and asserts the loop returns.

### 7. Health Store Probe Metadata
expected: |
  Health snapshots include secret-safe probe metadata.
result: pass
evidence: |
  `ProviderSnapshot` includes `LastProbeAt`, `LastProbeSuccess`, `LastProbeError`, and `LastProbeDuration`. `RecordProbe` populates them, and `internal/health/store_test.go` asserts probe success/failure metadata.

### 8. Probe-Driven Failure and Recovery
expected: |
  Probe failures can degrade/unhealthy providers, and probe successes can recover unhealthy providers.
result: pass
evidence: |
  `RecordProbe` increments consecutive failures on failed probes and resets failures on successful probes. `TestInMemoryStore_RecordProbe` covers degraded/unhealthy transition and successful recovery.

### 9. Configured Threshold Runtime Wiring
expected: |
  Configured `health_check.failure_threshold` and `health_check.success_threshold` influence runtime health transitions.
result: pass
evidence: |
  `internal/app/app.go` now constructs `health.NewInMemoryStore()` and calls `EnsureProvider(p.ID, failureThreshold, successThreshold)` using top-level config plus provider-level overrides. `internal/health/store.go` stores per-provider `failureThreshold` and `successThreshold`, uses failure threshold for unhealthy status, and requires configured consecutive successes before clearing failures.

### 10. App Lifecycle Wiring
expected: |
  The app constructs and starts the prober under runtime context without making tests hang.
result: pass
evidence: |
  `internal/app/app.go` constructs `health.NewProber` and `Run(ctx)` starts `a.Prober.Start(ctx)`. Existing tests construct `app.New()` without starting uncontrolled background work, and `go test ./...` passes.

### 11. Readiness Probe Visibility
expected: |
  `/readyz` exposes secret-safe probe state while preserving routeability semantics.
result: pass
evidence: |
  `internal/http/handlers/health.go` includes `probe_enabled` and per-provider probe metadata only from health snapshots. It preserves 200 when at least one provider is healthy/degraded and 503 when none are routeable.

### 12. End-to-End Recovery Test
expected: |
  Tests prove an unhealthy provider can become routeable again after a successful probe.
result: pass
evidence: |
  `tests/integration/health_test.go` includes `TestHealthRecovery`, which makes p1 unhealthy through failures, calls `application.Prober.ProbeProvider(context.Background(), "p1")`, asserts health returns to `healthy`, and confirms the gateway routes to p1 again.

### 13. README Config Documentation
expected: |
  README documents the health-check config example without unrelated documentation regressions.
result: pass
evidence: |
  `README.md` documents `health_check.enabled`, `interval`, and `timeout`, and the previously duplicated Go stack line, install step, and curl example headings have been removed.

### 14. Scope Boundary
expected: |
  Phase 2.6 does not introduce deferred systems.
result: pass
evidence: |
  Code inspection found no Admin Console/Admin API endpoint, Redis/PostgreSQL state, runtime CRUD/hot reload, full circuit breaker state machine, synthetic generation probes, Prometheus/OpenTelemetry exporter, streaming retry, hedged requests, rate limiting, semantic cache, or cost governance.

### 15. Full Verification Commands
expected: |
  `gofmt -l .`, `go vet ./...`, and `go test ./...` pass.
result: pass
evidence: |
  `go vet ./...` passed with no output. `go test ./...` passed for all packages. `gofmt -l .` returns no output, confirming all code is properly formatted.

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "`gofmt -l .`, `go vet ./...`, and `go test ./...` pass."
  status: fixed
  reason: "User reported: `gofmt -l .` returned `internal\\health\\store.go`; `go vet` and `go test` passed."
  severity: minor
  test: 15
  root_cause: "`internal/health/store.go` contains formatting that does not match gofmt output."
  artifacts:
    - path: "internal/health/store.go"
      issue: "Needs gofmt."
  missing:
    - "Run `gofmt -w internal/health/store.go` and rerun `gofmt -l .`."
  debug_session: "inline-verify-02-06"
