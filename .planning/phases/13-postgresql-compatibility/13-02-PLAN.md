---
phase: "13-postgresql-compatibility"
plan: "13-02"
type: "standard"
wave: 2
depends_on: ["13-01"]
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03]
files_modified:
  - "internal/controlstate/postgres/repository.go"
  - "internal/controlstate/postgres/limits.go"
  - "internal/controlstate/postgres/session_blacklist.go"
  - "internal/controlstate/migrations/postgres/0003_limits_sessions.sql"
  - "internal/controlstate/postgres/migrations.go"
  - "internal/controlstate/capabilities.go"
  - "internal/controlstate/postgres/repository_test.go"
  - "internal/controlstate/controlstate_test.go"
must_haves:
  truths:
    - "D-07: PostgreSQL repositories pursue SQLite full parity, including LimitRules and SessionBlacklist."
    - "D-08: Unsupported PostgreSQL repository capabilities fail closed with explicit unsupported errors, never nil repositories or fake success."
    - "D-09: Any remaining parity gap is a blocking task or a tested explicit unsupported path."
    - "D-10: PostgreSQLCapabilityProfile reports true only for implemented and verified PostgreSQL capabilities."
  artifacts:
    - path: "internal/controlstate/postgres/limits.go"
      provides: "PostgreSQL LimitRuleRepository parity with SQLite behavior"
    - path: "internal/controlstate/postgres/session_blacklist.go"
      provides: "PostgreSQL SessionBlacklistRepository parity"
    - path: "internal/controlstate/capabilities.go"
      provides: "Truthful PostgreSQL capability flags"
  key_links:
    - from: "internal/admission/controller.go"
      to: "postgres.Repository.LimitRules"
      via: "admission reads API-key and provider limit rules without nil checks"
    - from: "internal/controlstate/replication"
      to: "postgres.Repository.SessionBlacklist"
      via: "replication publishes blacklist mutations through the shared Repository interface"
user_setup:
  - "Integration tests require POSTGRES_TEST_DSN or an equivalent externally supplied PostgreSQL DSN."
---

<objective>
Bring PostgreSQL control-state repositories to the SQLite parity needed for Plan 4 and make capability reporting truthful. This plan covers CTRL-01, CTRL-02, CTRL-03 and implements D-07 through D-10.
</objective>

<context>
Read first:
- .planning/phases/13-postgresql-compatibility/13-CONTEXT.md
- internal/controlstate/repository.go
- internal/controlstate/postgres/repository.go
- internal/controlstate/sqlite/repository.go
- internal/controlstate/sqlite/limits.go
- internal/controlstate/sqlite/session_blacklist.go
- internal/controlstate/capabilities.go
- internal/admission/controller.go
- internal/controlstate/replication/repository.go

Existing patterns:
- Repository callers expect non-nil subrepositories.
- SQLite is the parity reference for limit rules and session blacklist behavior.
- PostgreSQL integration tests must be skipped unless an external test DSN is supplied.
</context>

<artifacts_this_phase_produces>
- PostgreSQL LimitRuleRepository
- PostgreSQL SessionBlacklistRepository
- Explicit unsupported-error path for any deliberately unimplemented repository surface
- PostgreSQL migration for missing parity tables
- Truthful PostgreSQL capability profile tests
</artifacts_this_phase_produces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Replace nil PostgreSQL repositories with concrete or fail-closed implementations</name>
  <files>internal/controlstate/postgres/repository.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - Repository.LimitRules, Repository.SessionBlacklist, and Repository.SemanticRules never return nil.
    - Unsupported behavior returns a named unsupported error that callers can surface clearly.
    - Existing implemented repositories keep current behavior.
  </behavior>
  <action>Add a small shared unsupported repository error if any method remains intentionally unsupported. Prefer implementing the missing repositories in this phase. Add tests that fail if PostgreSQL repository accessors return nil.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate/postgres ./internal/controlstate</automated>
  </verify>
  <done>PostgreSQL repository accessors are safe to call through the shared Repository interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PostgreSQL limit rule parity</name>
  <files>internal/controlstate/postgres/limits.go, internal/controlstate/migrations/postgres/0003_limits_sessions.sql, internal/controlstate/postgres/migrations.go, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - Limit rules can be saved, listed by scope/target, deleted, and used by admission code.
    - Schema preserves SQLite field semantics and timestamps.
    - PostgreSQL tests match the existing SQLite behavior cases.
  </behavior>
  <action>Port the SQLite limit-rule repository behavior to pgx using PostgreSQL placeholders and upserts. Add the missing migration table/indexes and wire it into the PostgreSQL migrator list. Keep the schema minimal and compatible with existing controlstate.LimitRule types.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate/postgres -run TestPostgresLimitRules -count=1</automated>
  </verify>
  <done>Admission can use PostgreSQL-backed LimitRules without special-case branching.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement PostgreSQL session blacklist parity</name>
  <files>internal/controlstate/postgres/session_blacklist.go, internal/controlstate/migrations/postgres/0003_limits_sessions.sql, internal/controlstate/postgres/repository_test.go</files>
  <behavior>
    - Sessions can be blacklisted, checked, and expired rows purged.
    - Expired blacklist records do not block sessions.
    - Replication consumers can call the repository through the shared interface.
  </behavior>
  <action>Port SQLite session blacklist behavior to PostgreSQL with a unique session_hash key, expires_at filtering, and purge query. Reuse the same migration as Task 2 if it keeps the diff small.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate/postgres -run TestPostgresSessionBlacklist -count=1</automated>
  </verify>
  <done>PostgreSQL matches SQLite session blacklist behavior for active, expired, and purge paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update capability profile only after tests prove support</name>
  <files>internal/controlstate/capabilities.go, internal/controlstate/controlstate_test.go</files>
  <behavior>
    - CapabilityProfile fields are true only for implemented and tested PostgreSQL support.
    - Tests document why any field remains false.
  </behavior>
  <action>Update PostgreSQLCapabilityProfile after Tasks 1-3 pass. Keep RedisHotState separate. Set SemanticCache, RateLimits, and CostGovernance only if the current PostgreSQL repository tests prove those paths work. Do not mark target-state capabilities true ahead of implementation.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/controlstate</automated>
  </verify>
  <done>CTRL-02 is satisfied by profile tests that match real PostgreSQL repository behavior.</done>
</task>
</tasks>

<threat_model>
STRIDE register:
- Denial of service: Nil repositories can panic or bypass admission checks. Disposition: non-nil repository accessors and tests.
- Tampering: Limit rules and blacklist mutations must use parameterized SQL only. Disposition: pgx placeholders and repository tests.
- Repudiation: Capability flags must not overclaim unsupported governance behavior. Disposition: tests assert the final profile.
</threat_model>

<verification>
Run:
- go test -timeout 60s ./internal/controlstate
- go test -timeout 60s ./internal/controlstate/postgres
- POSTGRES_TEST_DSN=<external test dsn> go test -timeout 60s ./internal/controlstate/postgres -count=1
</verification>

<success_criteria>
- CTRL-01 active gateway repository surfaces are implemented or explicitly fail closed.
- CTRL-02 capability profile matches verified PostgreSQL behavior.
- CTRL-03 has focused PostgreSQL integration tests gated by external DSN.
- No caller needs nil checks around PostgreSQL repository accessors.
</success_criteria>
