---
phase: "13-postgresql-compatibility"
plan: "13-01"
type: "standard"
wave: 1
depends_on: []
autonomous: true
requirements: [PG-01, PG-02, PG-03, TEST-01]
files_modified:
  - "docker-compose.postgres.yml"
  - ".env.example"
  - "Agent-gateway/gateway-postgres.md"
  - "internal/config/config.go"
  - "internal/config/config_test.go"
  - "internal/app/app.go"
  - "internal/app/app_test.go"
must_haves:
  truths:
    - "D-01: Plan 4 uses a separate Docker Compose override for PostgreSQL plus pgvector; the base compose remains the Plan 1/2 stack."
    - "D-02: Example configuration is secret-safe and uses placeholders only."
    - "D-03: CONTROL_STATE_BACKEND=postgres fails startup when PostgreSQL is unavailable or migrations fail."
    - "D-04: pgvector/vector initialization may degrade semantic-cache vector capability without stopping core forwarding when relational PostgreSQL is healthy."
    - "D-05: Public /readyz remains coarse; dependency names and topology detail stay in admin/internal diagnostics or logs."
    - "D-06: Plan 4 smoke verification is prepared to prove compose, migrations, gateway startup, and one chat request."
  artifacts:
    - path: "docker-compose.postgres.yml"
      provides: "Plan 4 opt-in compose override with PostgreSQL/pgvector service wiring"
    - path: ".env.example"
      provides: "Placeholder-only Plan 4 environment variables"
    - path: "Agent-gateway/gateway-postgres.md"
      provides: "Operator runbook for Plan 4 startup, readiness, and smoke prerequisites"
  key_links:
    - from: "CONTROL_STATE_BACKEND=postgres"
      to: "internal/app/app.go"
      via: "startup selects PostgreSQL and runs required migrations fail-closed"
    - from: "SEMANTIC_CACHE_VECTOR_STORE=pgvector"
      to: "later pgvector adapter"
      via: "configuration is visible before adapter implementation"
user_setup:
  - "Operators must provide real DSNs, passwords, encryption keys, and provider keys outside git-managed source."
---

<objective>
Add the Plan 4 deployment and configuration foundation: a PostgreSQL/pgvector compose override, placeholder-only example settings, startup/readiness behavior, and operator docs. This plan covers PG-01, PG-02, PG-03, TEST-01 and locks D-01 through D-06 without changing the OpenAI-compatible data-plane contract.
</objective>

<context>
Read first:
- .planning/phases/13-postgresql-compatibility/13-CONTEXT.md
- .planning/REQUIREMENTS.md
- docker-compose.yml
- .env.example
- internal/config/config.go
- internal/app/app.go
- internal/http/handlers/health.go
- Agent-gateway/gateway-multiNode.md

Existing patterns:
- Configuration is env-first and must not include real secrets.
- Public readiness avoids topology/detail leakage from Phase 12.
- PostgreSQL control state already exists as a backend option but needs Plan 4 operator packaging.
</context>

<artifacts_this_phase_produces>
- Plan 4 compose override for PostgreSQL plus pgvector
- Placeholder-only Plan 4 environment examples
- PostgreSQL deployment runbook
- Fail-closed startup behavior for required relational state
- Coarse public readiness and detailed operator diagnostics
</artifacts_this_phase_produces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Add Plan 4 compose override and secret-safe examples</name>
  <files>docker-compose.postgres.yml, .env.example, Agent-gateway/gateway-postgres.md</files>
  <behavior>
    - Base docker-compose.yml remains focused on Redis Stack and Qdrant for Plans 1/2.
    - Plan 4 PostgreSQL/pgvector services are opt-in through the override.
    - Examples contain placeholders only.
  </behavior>
  <action>Create docker-compose.postgres.yml with a pgvector-capable PostgreSQL service and environment placeholders. Extend .env.example with Plan 4 placeholders for CONTROL_STATE_BACKEND, CONTROL_STATE_DSN, CONTROL_STATE_MIGRATE_ON_STARTUP, CONTROL_STATE_ENCRYPTION_KEY, SEMANTIC_CACHE_VECTOR_STORE, and pgvector settings. Document the exact compose command and keep real secrets out of source-controlled files.</action>
  <verify>
    <manual>Review docker-compose.postgres.yml and .env.example and confirm no real DSNs, passwords, API keys, provider secrets, or encryption keys are present.</manual>
  </verify>
  <done>Operators can see how to start Plan 4 locally without weakening the existing Plans 1/2 compose path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate Plan 4 configuration defaults</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <behavior>
    - PostgreSQL settings are explicit when Plan 4 is enabled.
    - pgvector settings are visible and have documented defaults.
    - Invalid vector dimensions and unsupported vector stores fail config validation.
  </behavior>
  <action>Add the smallest config fields needed for Plan 4: pgvector vector store selection, vector dimension with a reasonable default, and index/search knobs needed by 13-03. Reject missing PostgreSQL DSN when CONTROL_STATE_BACKEND=postgres. Do not add config for values the implementation will not use in Phase 13.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <done>Config tests prove placeholder defaults, explicit PostgreSQL DSN validation, vector dimension validation, and no hardcoded secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce startup and readiness boundaries</name>
  <files>internal/app/app.go, internal/app/app_test.go, internal/http/handlers/health.go</files>
  <behavior>
    - PostgreSQL control-state failures stop gateway startup when selected.
    - pgvector initialization failure degrades semantic vector capability only.
    - Public /readyz stays coarse and does not name PostgreSQL, pgvector, primary, replica, leader, follower, node, or topology.
  </behavior>
  <action>Wire startup so PostgreSQL connection/migration failures return clear startup errors. Add a pgvector-degraded state placeholder that later plans can fill, while core forwarding remains possible if relational state is healthy. Keep dependency names in logs/admin/internal diagnostics only, not public readiness bodies.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/app ./internal/http/handlers</automated>
  </verify>
  <done>Startup behavior is fail-closed for relational PostgreSQL and graceful only for semantic vector degradation.</done>
</task>
</tasks>

<threat_model>
STRIDE register:
- Information disclosure: Example files and readiness responses must not leak DSNs, passwords, provider secrets, API keys, or backend topology. Disposition: placeholder-only examples and coarse public readiness.
- Denial of service: Starting with CONTROL_STATE_BACKEND=postgres but no database must fail fast instead of running against fake state. Disposition: validate config and startup connection/migration.
- Tampering: Operator-provided DSNs and secrets must remain external configuration, not committed defaults. Disposition: examples use placeholders only.
</threat_model>

<verification>
Run:
- go test -timeout 60s ./internal/config
- go test -timeout 60s ./internal/app ./internal/http/handlers
- docker compose -f docker-compose.yml -f docker-compose.postgres.yml config
</verification>

<success_criteria>
- PG-01 and PG-02 have concrete compose/docs/example support.
- PG-03 fail-closed relational startup behavior is tested.
- TEST-01 has documented Plan 4 smoke prerequisites for 13-04.
- Plans 1/2 remain SQLite + Redis Stack + Qdrant by default.
</success_criteria>
