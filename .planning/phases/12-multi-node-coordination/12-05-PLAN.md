---
phase: "12-multi-node-coordination"
plan: "12-05"
type: "standard"
wave: 5
depends_on: ["12-01", "12-02", "12-03", "12-04"]
autonomous: true
requirements: [COORD-01, FENCE-01, FENCE-02, HLTH-01, TEST-01]
files_modified:
  - "Agent-gateway/gateway-multiNode.md"
  - "tests/integration/multinode_lb_routing_test.go"
must_haves:
  truths:
    - "Normal admin write traffic is routed to the current writable node before calling mutative /admin/v1/* endpoints."
    - "Leader-aware routing is an operational optimization; RequireWritable and repository fencing remain authoritative."
    - "Failover windows may still return a generic retryable 503, then refresh topology and retry once or with bounded backoff."
    - "Ordinary user responses never expose leader, follower, primary, replica, node identity, lag, failover, writable, or topology details."
  artifacts:
    - path: "Agent-gateway/gateway-multiNode.md"
      provides: "Deployment guidance for LB/admin write routing and bounded retry behavior"
    - path: "tests/integration/multinode_lb_routing_test.go"
      provides: "Real multi-node tests for leader-aware admin write routing and failover retry"
  key_links:
    - from: "/admin/v1/topology"
      to: "leader-aware LB or admin client"
      via: "admin/internal-only discovery of writable node"
    - from: "mutative /admin/v1/* requests"
      to: "RequireWritable"
      via: "backend write fencing as final safety check"
    - from: "ordinary client responses"
      to: "security tests"
      via: "forbidden topology term scan"
user_setup: []
---

<objective>
Add the Phase 12 deployment-level optimization that prevents normal admin write traffic from being sprayed across followers. The system should document and test a leader-aware LB/admin client pattern that discovers the writable node through admin/internal topology, routes mutative admin requests there, and uses bounded refresh-and-retry on stale routing while preserving generic ordinary-user 503 responses.
</objective>

<context>
Read first:
- .planning/phases/12-multi-node-coordination/12-CONTEXT.md
- Agent-gateway/gateway-multiNode.md
- internal/http/router.go
- internal/http/middleware/writable.go
- internal/http/handlers/health.go
- tests/integration/multinode_harness_test.go
- tests/integration/multinode_security_test.go
- tests/integration/multinode_coordination_test.go

Existing patterns:
- `/admin/v1/topology` is already admin-authenticated and exposes `writable`/role details for internal/admin callers.
- `RequireWritable` already rejects non-writable mutative admin routes with a generic 503.
- Multi-node integration tests must use real app/router/middleware components and a hard `go test -timeout 60s`.
</context>

<artifacts_this_phase_produces>
- LB/admin write routing documentation for Plan 2 multi-node deployments
- Real multi-node integration tests for leader-aware admin write selection
- Real multi-node integration tests for stale-leader failover refresh and bounded retry
</artifacts_this_phase_produces>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Document leader-aware LB/admin write routing</name>
  <files>Agent-gateway/gateway-multiNode.md</files>
  <behavior>
    - Data-plane `/v1/*` requests can still go to all ready nodes.
    - Mutative admin requests `POST`, `PUT`, `PATCH`, and `DELETE` under `/admin/v1/*` should route to the current writable node.
    - The LB or admin client discovers the writable node only through admin/internal topology.
    - Ordinary users never receive topology, role, node, lag, writable, or failover details.
  </behavior>
  <action>Add a deployment section that specifies: route `/v1/*` to all ready nodes; route mutative `/admin/v1/*` to the node whose `/admin/v1/topology` reports `writable: true`; keep admin topology behind existing admin/internal auth; on generic 503 from an admin write, refresh topology and retry with bounded jitter/backoff; keep backend `RequireWritable` and repository fencing as the final safety layer. Include a short Nginx/HAProxy-style pseudocode example only if it stays configuration-agnostic and contains no secrets.</action>
  <verify>
    <manual>Agent-gateway/gateway-multiNode.md contains a section titled "LB 写请求路由优化" and explicitly states that ordinary responses must not expose leader/follower/topology details.</manual>
  </verify>
  <done>The multi-node deployment doc explains how normal admin writes avoid follower 503s without weakening backend fencing or leaking topology to ordinary users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test leader-aware admin write selection with real nodes</name>
  <files>tests/integration/multinode_lb_routing_test.go, tests/integration/multinode_harness_test.go</files>
  <behavior>
    - A test helper queries real `/admin/v1/topology` on each node using admin auth.
    - The helper selects the node with `writable: true`.
    - A mutative admin request sent through that selection succeeds without first hitting a follower 503.
  </behavior>
  <action>Add a focused integration test that starts the existing 3-node harness, discovers the writable node through authenticated topology calls, sends a real `POST /admin/v1/providers` request to that node, and asserts success. Do not mock `RequireWritable`, router auth, topology handler, or repository writes. Keep any helper private to the test file.</action>
  <verify>
    <automated>go test -timeout 60s ./tests/integration -run TestMultiNodeLeaderAwareAdminWriteRouting -count=1</automated>
  </verify>
  <done>The test proves a leader-aware routing decision uses real topology and avoids normal follower 503s for admin writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test stale route refresh and generic 503 security</name>
  <files>tests/integration/multinode_lb_routing_test.go, tests/integration/multinode_security_test.go</files>
  <behavior>
    - A stale admin write target may return one generic 503 during failover or stale LB state.
    - The simulated LB/admin client refreshes topology and retries against the new writable node.
    - The 503 body does not reveal leader, follower, primary, replica, node, lag, failover, writable, or topology terms.
  </behavior>
  <action>Add a failover scenario using the real multi-node harness: discover the current writable node, stop it or make the cached target stale using existing harness capabilities, attempt a mutative admin write against a non-writable/stale node and assert a generic 503, refresh topology across surviving nodes, retry the same write against the new writable node, and assert success. Reuse the forbidden-term scan list from the security test instead of duplicating a divergent list.</action>
  <verify>
    <automated>go test -timeout 60s ./tests/integration -run "TestMultiNodeLeaderAwareAdminWriteRouting|TestMultiNodeLeaderAwareRetryAfterFailover|TestMultiNodeSecurity" -count=1</automated>
  </verify>
  <done>The test proves stale LB state causes bounded generic 503 behavior, not frequent normal-state errors or topology leakage.</done>
</task>
</tasks>

<threat_model>
STRIDE register:
- Spoofing: LB/admin topology discovery must use existing admin/internal auth, not unauthenticated client input. Disposition: mitigate through authenticated topology calls in tests.
- Tampering: A wrong LB decision must still be blocked by RequireWritable and repository fencing. Disposition: keep backend fencing authoritative.
- Information disclosure: Ordinary 503 responses must not expose leader/follower/topology details even when LB routing is stale. Disposition: reuse forbidden-term tests.
- Denial of service: Normal admin writes should not repeatedly hit followers; stale routes use bounded retry only. Disposition: document bounded refresh-and-retry and test one failover path.
</threat_model>

<verification>
Run:
- go test -timeout 60s ./tests/integration -run TestMultiNodeLeaderAwareAdminWriteRouting -count=1
- go test -timeout 60s ./tests/integration -run "TestMultiNodeLeaderAwareAdminWriteRouting|TestMultiNodeLeaderAwareRetryAfterFailover|TestMultiNodeSecurity" -count=1
</verification>

<success_criteria>
- Normal mutative admin traffic has a documented leader-aware LB/admin-client route.
- Stale routing during failover is handled by generic 503 plus bounded topology refresh and retry.
- Backend write fencing remains the final guard; the LB optimization is not treated as a security boundary.
- Ordinary user surfaces still do not expose any main/replica or topology information.
</success_criteria>
