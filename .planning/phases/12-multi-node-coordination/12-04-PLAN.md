---
phase: "12-multi-node-coordination"
plan: "12-04"
type: "standard"
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
autonomous: true
requirements: [COORD-01, COORD-02, COORD-03, REPL-01, REPL-02, REPL-03, FENCE-01, FENCE-02, RECOV-01, RECOV-02, HLTH-01, TEST-01]
files_modified:
  - "tests/integration/multinode_harness_test.go"
  - "tests/integration/multinode_coordination_test.go"
  - "tests/integration/multinode_security_test.go"
  - "internal/coordination/fake.go"
  - "internal/controlstate/replication/recovery_test.go"
must_haves:
  truths:
    - "A 2-3 node in-process harness can start independent SQLite nodes against shared coordination state."
    - "Leader loss, graceful shutdown, Redis outage/recovery, replica lag/degraded, and non-writable write rejection are tested."
    - "SQLite relational state does not diverge or corrupt across covered abnormal scenarios."
    - "Ordinary user responses never expose coordination topology details."
  artifacts:
    - path: "tests/integration/multinode_harness_test.go"
      provides: "Reusable in-process multi-node test harness"
    - path: "tests/integration/multinode_coordination_test.go"
      provides: "Main and abnormal multi-node scenario coverage"
    - path: "tests/integration/multinode_security_test.go"
      provides: "Topology leak regression tests"
  key_links:
    - from: "FakeCoordinator"
      to: "multi-node harness"
      via: "shared in-memory coordination state"
    - from: "independent SQLite DSNs"
      to: "replication consumer"
      via: "per-node repository and stream apply"
    - from: "ordinary HTTP responses"
      to: "security assertions"
      via: "forbidden-term scans"
user_setup: []
---

<objective>
Add focused multi-node verification for Phase 12. The harness uses multiple in-process gateway/server instances, independent SQLite DSNs, and shared Redis test state or fake state, covering the main scenarios and likely abnormal scenarios required by D-13 through D-15.
</objective>

<context>
Read first:
- .planning/phases/12-multi-node-coordination/12-CONTEXT.md
- tests/integration/health_test.go
- tests/integration/admin_provider_actions_test.go
- internal/controlstate/sqlite/repository.go
- internal/coordination/fake.go
- internal/controlstate/replication/recovery.go

Existing patterns:
- Integration tests already use httptest style gateway instances.
- DBForTest supports focused SQLite assertions.
- Backend unit and integration tests must use a hard 60 second timeout.
</context>

<artifacts_this_phase_produces>
- Multi-node test harness with 2-3 nodes
- Scenario tests for startup, failover, graceful shutdown, Redis outage/recovery, lag/degraded, and write rejection
- Security regression tests for ordinary topology secrecy
</artifacts_this_phase_produces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the in-process multi-node harness</name>
  <files>tests/integration/multinode_harness_test.go, internal/coordination/fake.go</files>
  <behavior>
    - Harness starts 2-3 nodes in one process.
    - Each node has an independent SQLite DSN.
    - Nodes share a Redis test instance or FakeCoordinator/FakeStream state.
  </behavior>
  <action>Create reusable test helpers that construct isolated gateway services with independent SQLite repositories and shared coordination/stream fakes per D-15. Keep helpers private to tests. Include cleanup that closes repositories, stops coordinators, and removes temp SQLite files without touching non-test paths.</action>
  <verify>
    <automated>go test -timeout 60s ./tests/integration -run TestMultiNodeHarness</automated>
  </verify>
  <done>Harness test proves 3 nodes start, elect one writable node, expose distinct SQLite databases, and clean up all started services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover required multi-node coordination and recovery scenarios</name>
  <files>tests/integration/multinode_coordination_test.go, internal/controlstate/replication/recovery_test.go</files>
  <behavior>
    - Startup elects exactly one writable node.
    - Leader loss and graceful shutdown promote another node.
    - Redis outage/recovery makes writes fail safely and resumes coordination.
    - Replica lag/degraded affects readiness according to role.
    - Non-writable write attempts are rejected without SQLite divergence.
  </behavior>
  <action>Add focused scenario tests per D-13 and D-14. Use deterministic fake clocks or short test-only intervals through injected options while keeping production constants at 10 second TTL and 3 second heartbeat. Verify relational state by reading each node's SQLite DB after replication/recovery paths settle. Do not add Docker Compose or a full chaos matrix.</action>
  <verify>
    <automated>go test -timeout 60s ./tests/integration -run TestMultiNode</automated>
  </verify>
  <done>Tests cover startup, leader loss, graceful shutdown, Redis outage/recovery, replica lag/degraded, non-writable write rejection, and no SQLite corruption in covered scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add topology secrecy regression coverage</name>
  <files>tests/integration/multinode_security_test.go, tests/integration/health_test.go</files>
  <behavior>
    - Ordinary /healthz and /readyz responses do not reveal topology details.
    - Ordinary data-plane write error responses do not reveal topology details.
    - Admin/internal topology remains visible only to admin/internal callers.
  </behavior>
  <action>Add regression tests that scan ordinary response bodies for forbidden terms: leader, follower, primary, replica, node_id, leader_id, wal_lag, writable, degraded_reason, failover, and topology. Confirm the internal/admin endpoint returns the detailed fields only under existing admin auth. This directly enforces the user's security clarification and D-09 through D-11.</action>
  <verify>
    <automated>go test -timeout 60s ./tests/integration -run 'TestMultiNodeSecurity|TestHealth'</automated>
  </verify>
  <done>Forbidden-term tests fail if any ordinary user surface leaks coordination details, while admin/internal topology remains testable.</done>
</task>
</tasks>

<threat_model>
STRIDE register:
- Spoofing: Harness must test admin-only topology access with valid and invalid identities. Disposition: mitigate in security tests.
- Tampering: Tests assert non-writable writes do not mutate local SQLite. Disposition: mitigate with DB assertions.
- Information disclosure: Forbidden-term scans cover ordinary health, readiness, and data-plane write errors. Disposition: mitigate in multinode_security_test.go.
- Denial of service: Redis outage/recovery tests prove safe failure and recovery without local queues. Disposition: mitigate through scenario coverage.
</threat_model>

<verification>
Run:
- go test -timeout 60s ./tests/integration -run TestMultiNode
- go test -timeout 60s ./tests/integration -run 'TestMultiNodeSecurity|TestHealth'
- go test -timeout 60s ./internal/coordination ./internal/controlstate/replication
</verification>

<success_criteria>
- TEST-01 is covered by focused in-process scenarios.
- All Phase 12 decisions D-13, D-14, and D-15 are implemented without Docker Compose or a full chaos matrix.
- Requirement coverage is end-to-end across coordination, replication, fencing, recovery, health, and security secrecy.
- The test suite gives developers a practical multi-node simulator for later Phase 12 implementation work.
</success_criteria>
