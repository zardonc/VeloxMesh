---
phase: 20-config-unification-scheduler-core-hardening
plan: "03"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - internal/app/semantic_neighbors.go
  - internal/scheduler/semantic_neighbors.go
  - internal/storage/interfaces.go
  - internal/storage/qdrant.go
  - internal/storage/adapters.go
  - internal/scheduler/semantic_neighbors_test.go
  - internal/storage/qdrant_test.go
  - internal/app/app_test.go
autonomous: true
requirements: ["QDR-05", "QDR-06"]
must_haves:
  truths:
    - "D-16 requestText enforces a configurable/defaulted character cap before embedding."
    - "D-17 default semantic-neighbor embedding input cap is 16000 characters."
    - "D-18 truncation evidence is sanitized and never logs raw prompt text."
    - "D-19 semantic neighbors initialize the scheduler_training_samples Qdrant collection at startup using cache.vector_dimension."
    - "D-20 Qdrant initialization failure disables semantic neighbors fail-open without blocking startup."
    - "D-07 cache.vector_dimension is the shared configured vector dimension for semantic cache and semantic neighbors."
  artifacts:
    - path: "internal/scheduler/semantic_neighbors.go"
      provides: "embedding input cap and safe truncation behavior"
    - path: "internal/storage/qdrant.go"
      provides: "explicit Qdrant collection initialization"
    - path: "internal/app/semantic_neighbors.go"
      provides: "startup fail-open wiring"
  key_links:
    - from: "internal/app/semantic_neighbors.go"
      to: "internal/storage/qdrant.go"
      via: "semantic-neighbor startup calls collection ensure with cache vector dimension"
---

<objective>
Add semantic-neighbor input and Qdrant startup safeguards.

Purpose: QDR-05 and QDR-06 prevent embedding token-limit failures and avoid first-write collection surprises while preserving fail-open gateway startup.
Output: configurable request text cap, sanitized truncation evidence, explicit Qdrant ensure path, and startup fail-open tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
@internal/app/semantic_neighbors.go
@internal/scheduler/semantic_neighbors.go
@internal/storage/interfaces.go
@internal/storage/qdrant.go
@internal/storage/adapters.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap semantic-neighbor embedding input</name>
  <files>internal/scheduler/semantic_neighbors.go, internal/scheduler/semantic_neighbors_test.go</files>
  <read_first>
    - internal/scheduler/semantic_neighbors.go
    - internal/scheduler/semantic_neighbors_test.go
    - internal/observability/metrics.go
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Add a named default constant `defaultSemanticNeighborInputMaxChars = 16000` and a field on `SemanticNeighborConfig` for the max input characters. Change `requestText` usage so the string passed to the embedder is truncated to the configured cap before `EmbeddingRequest` is built. If truncation occurs, record a sanitized metric/error reason such as `input_truncated` without logging or storing the raw text.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - A semantic-neighbor test proves an input longer than 16000 characters sends exactly 16000 characters to the embedder by default.
    - A semantic-neighbor test proves a custom cap changes the sent input length.
    - A semantic-neighbor test proves nil/empty request behavior still fails open or returns current defaults.
    - No log, metric label, or test fixture contains raw prompt text from a truncated input.
  </acceptance_criteria>
  <done>Long prompts cannot reach the embedding provider unbounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit Qdrant collection ensure capability</name>
  <files>internal/storage/interfaces.go, internal/storage/qdrant.go, internal/storage/adapters.go, internal/storage/qdrant_test.go</files>
  <read_first>
    - internal/storage/interfaces.go
    - internal/storage/qdrant.go
    - internal/storage/adapters.go
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Add the smallest collection-ensure surface needed for Qdrant, for example an optional `EnsureCollection(ctx, collection string, dimension int) error` method implemented by `QdrantVectorAdapter`. Move the existing lazy create logic in `Insert` behind the same helper so behavior stays consistent. Validate dimension is greater than zero before creating the collection.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/storage</automated>
  </verify>
  <acceptance_criteria>
    - `QdrantVectorAdapter.EnsureCollection` checks `CollectionExists` and creates `scheduler_training_samples` with the requested dimension when absent.
    - `Insert` reuses the ensure helper and still creates a collection lazily from the vector length.
    - Dimension less than 1 returns an error before calling Qdrant create.
    - Non-Qdrant adapters do not need a new broad interface unless compilation requires it.
  </acceptance_criteria>
  <done>Qdrant collection creation can run at startup with a configured dimension.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ensure semantic-neighbor collection at startup fail-open</name>
  <files>internal/app/semantic_neighbors.go, internal/app/app_test.go, internal/scheduler/semantic_neighbors.go</files>
  <read_first>
    - internal/app/semantic_neighbors.go
    - internal/app/app.go
    - internal/storage/qdrant.go
    - internal/scheduler/semantic_neighbors.go
    - internal/app/app_test.go
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>When semantic neighbors are enabled and the vector adapter supports explicit collection ensure, call it during `newSemanticNeighborService` using collection `scheduler_training_samples` and `cfg.Cache.VectorDimension` after Phase 20 config unification. If ensure fails, log a sanitized warning, increment semantic-neighbor fallback/error observability if available, return nil for the semantic-neighbor service, and continue app startup.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/app ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - An app test proves successful ensure keeps semantic neighbors enabled.
    - An app test proves ensure failure disables semantic neighbors and `app.New` still succeeds.
    - The ensure call uses the configured cache vector dimension, not the first indexed vector length.
    - Startup warning text does not include prompts, provider payloads, embeddings, auth headers, API keys, semantic-cache payloads, or raw task text.
  </acceptance_criteria>
  <done>Semantic-neighbor startup prepares Qdrant when possible and fails open when not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User request -> embedding provider | Request messages are transformed into embedding input. |
| Gateway startup -> Qdrant | Startup initializes vector collection state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Denial of Service | semantic-neighbor embedding | mitigate | Truncate request text to a named configurable cap before embedding. |
| T-20-03-02 | Information Disclosure | truncation evidence | mitigate | Record only sanitized truncation reason/counts, never raw prompt text. |
| T-20-03-03 | Denial of Service | app startup | mitigate | Disable semantic neighbors fail-open when Qdrant ensure fails. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/scheduler ./internal/storage ./internal/app`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- QDR-05 and QDR-06 are covered.
- Embedding input is capped before provider calls.
- Qdrant collection initialization is explicit and fail-open.
</success_criteria>

## Artifacts this phase produces

- Constant: `defaultSemanticNeighborInputMaxChars`.
- Config field: `SemanticNeighborConfig.InputMaxChars`.
- Method: `QdrantVectorAdapter.EnsureCollection`.
- Startup behavior in `newSemanticNeighborService` for Qdrant collection ensure.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| QDR-05 | `requestText()` max input length before embedding | Task 1. |
| QDR-06 | Qdrant collection startup initialization | Tasks 2 and 3. |
| CONTEXT D-16..D-20 | Semantic-neighbor safeguards | Covered by must_haves and Tasks 1-3. |

<output>
Create `.planning/phases/20-config-unification-scheduler-core-hardening/20-03-SUMMARY.md` when done.
</output>
