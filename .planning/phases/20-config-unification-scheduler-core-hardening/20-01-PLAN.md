---
phase: 20-config-unification-scheduler-core-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
  - config.json.example
  - .env.example
  - config.scheduler.example.json
  - config.cache.example.json
autonomous: true
requirements: ["CFG-01", "CFG-02", "CFG-03", "CFG-04"]
must_haves:
  truths:
    - "D-01 canonical nested ControlState, Redis, and Cache structs are the source of truth."
    - "D-02 existing ENV variable names remain backward-compatible."
    - "D-03 legacy flat JSON keys normalize into nested structs instead of staying a second live source."
    - "D-04 nested JSON keys win when both nested and flat keys are present."
    - "D-05 ENV seeds defaults, main config JSON overrides ENV, component config files override their component block last."
    - "D-06 Qdrant settings live under Cache."
    - "D-07 cache.vector_dimension is shared by semantic cache and semantic neighbors."
    - "D-08 scheduler_config_file overrides Scheduler and cache_config_file overrides Cache only."
    - "D-09 disabled optional subsystems receive structural/default validation only."
    - "D-10 add minimal config examples only; full runbook remains Phase 22."
  artifacts:
    - path: "internal/config/config.go"
      provides: "nested config structs, legacy JSON normalization, component config file loading"
    - path: "internal/config/config_validation.go"
      provides: "validation keyed to enabled optional subsystems"
    - path: "config.json.example"
      provides: "minimal structured config example"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/config/config_validation.go"
      via: "LoadConfig normalizes into nested structs before Validate consumes them"
---

<objective>
Unify ControlState, Redis, and Cache/Qdrant config into named nested structs while preserving existing environment variables and flat JSON compatibility.

Purpose: CFG-01 through CFG-04 require a single nested runtime shape, component-scoped config overrides, minimal examples, and no deployment breakage when Scheduler, Redis, or semantic cache remain disabled.
Output: nested config types, load/merge/default helpers, validation updates, compatibility tests, and small example files.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
@internal/config/config.go
@internal/config/config_validation.go
@internal/config/config_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical nested config blocks with legacy aliases</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go
    - internal/config/config_test.go
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Add named nested structs `ControlStateConfig`, `RedisConfig`, and `CacheConfig` to `Config` alongside existing `SchedulerConfig`. Load existing ENV keys into those nested blocks. Parse JSON using a temporary file config that accepts both nested keys (`control_state`, `redis`, `cache`) and legacy flat keys (`control_state_backend`, `redis_enabled`, `semantic_cache_enabled`, `qdrant_addr`, etc.), then normalize into the nested blocks. If both forms set the same value, keep the nested value.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - `internal/config/config.go` defines `ControlStateConfig`, `RedisConfig`, and `CacheConfig`.
    - `LoadConfig` seeds nested blocks from the current ENV names including `CONTROL_STATE_BACKEND`, `REDIS_ENABLED`, `SEMANTIC_CACHE_ENABLED`, `SEMANTIC_CACHE_VECTOR_DIMENSION`, and `QDRANT_ADDR`.
    - A config test proves legacy flat JSON loads into nested blocks.
    - A config test proves nested JSON wins over conflicting flat JSON for the same field.
    - Existing tests for provider loading and scheduler config still pass.
  </acceptance_criteria>
  <done>Runtime config has one canonical nested source while existing deployments still load.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply component-scoped config file overrides</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go
    - internal/config/config_test.go
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Add `SchedulerConfigFile string` and `CacheConfigFile string` JSON fields named `scheduler_config_file` and `cache_config_file`. After the main config file is merged and before defaults/validation, read each non-empty component file. Merge `scheduler_config_file` into `Config.Scheduler` using the existing scheduler merge behavior, and merge `cache_config_file` into `Config.Cache` only. Do not let a component file modify providers, Redis, ControlState, or other blocks.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - A test proves `scheduler_config_file` overrides `scheduler.executor_concurrency` from the main config.
    - A test proves `cache_config_file` overrides `cache.qdrant.addr` and `cache.vector_dimension`.
    - A test proves component files do not alter unrelated root fields.
    - Missing component file errors include the path and are not swallowed.
  </acceptance_criteria>
  <done>Operators can split scheduler and cache config without changing the main config shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update validation and minimal examples</name>
  <files>internal/config/config_validation.go, internal/config/config_test.go, config.json.example, .env.example, config.scheduler.example.json, config.cache.example.json</files>
  <read_first>
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - config.json.example
    - .env.example
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Update validation to consume nested blocks. Require ControlState DSN/encryption only when the backend is sqlite or postgres, Redis address only when Redis is enabled, and Qdrant address only when semantic cache or semantic neighbors require Qdrant. Add minimal `config.json.example`, `.env.example`, `config.scheduler.example.json`, and `config.cache.example.json` entries showing disabled defaults and optional nested blocks without any real secrets.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - A validation test proves disabled Scheduler, Redis, and semantic cache do not require endpoint/address values.
    - A validation test proves enabled Redis requires `redis.addr`.
    - A validation test proves enabled Qdrant-backed cache or semantic neighbors requires `cache.qdrant.addr`.
    - `.env.example` keeps existing ENV variable names and does not include passwords, API keys, auth headers, provider payloads, prompts, embeddings, semantic-cache payloads, or raw task text.
    - `config.json.example` shows `control_state`, `redis`, `cache`, and `scheduler` as separate named blocks.
  </acceptance_criteria>
  <done>Config unification is documented minimally and optional subsystems remain disabled by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config -> gateway runtime | ENV, main JSON, and component JSON files affect runtime subsystem wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | LoadConfig merge order | mitigate | Enforce ENV -> main JSON -> component file precedence and test nested-over-flat conflict handling. |
| T-20-01-02 | Information Disclosure | examples and config structs | mitigate | Keep examples secret-free and never add prompt, provider payload, auth header, embedding, or raw task text fields. |
| T-20-01-03 | Denial of Service | validation | mitigate | Require connection details only for enabled optional subsystems so disabled deployments continue starting. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/config`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- CFG-01 through CFG-04 are covered.
- Existing ENV variable deployments remain compatible.
- Nested config is the only live runtime source after loading.
</success_criteria>

## Artifacts this phase produces

- Types: `ControlStateConfig`, `RedisConfig`, `CacheConfig`, `QdrantConfig`.
- Config fields: `Config.ControlState`, `Config.Redis`, `Config.Cache`, `Config.SchedulerConfigFile`, `Config.CacheConfigFile`.
- JSON keys: `control_state`, `redis`, `cache`, `scheduler_config_file`, `cache_config_file`.
- Example files: `config.json.example`, `config.scheduler.example.json`, `config.cache.example.json`.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| CFG-01 | Nested subsystem structs | Tasks 1 and 3. |
| CFG-02 | Component-scoped config files | Task 2. |
| CFG-03 | Example config docs | Task 3. |
| CFG-04 | Optional subsystems disabled by default | Tasks 1 and 3. |
| CONTEXT D-01..D-10 | Phase 20 config decisions | Covered by must_haves and Tasks 1-3. |

<output>
Create `.planning/phases/20-config-unification-scheduler-core-hardening/20-01-SUMMARY.md` when done.
</output>
