---
phase: "09-redis-stack-qdrant-fallback-integration"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "internal/hotstate/hotstate.go"
  - "internal/hotstate/redis.go"
  - "internal/hotstate/local.go"
  - "internal/hotstate/hotstate_test.go"
  - "tests/integration/redis_hotstate_test.go"
autonomous: true
requirements:
  - "Phase 9: Redis Stack Integration"
must_haves:
  truths:
    - "Redis is hot-path acceleration, while SQLite remains authoritative per D-04 through D-06"
    - "Periodic windows, RPM, and short-lived counters use Redis atomic execution per D-12"
    - "Config Pub/Sub routes by event type instead of blanket reload semantics per D-14"
    - "Redis keys remain namespaced and never include secrets or raw prompts"
  artifacts:
    - path: "internal/hotstate/hotstate.go"
      provides: "Typed hot-state interfaces for atomic counters, cache bytes, blacklist, and typed config changes"
    - path: "internal/hotstate/redis.go"
      provides: "Redis implementations using Lua/check-and-increment primitives and namespaced keys"
    - path: "internal/hotstate/local.go"
      provides: "Local fallback implementations for tests and Redis-degraded mode"
  key_links:
    - from: "internal/hotstate/redis.go"
      to: "internal/hotstate/hotstate.go"
      via: "RedisClient implements the expanded hot-state interfaces"
      pattern: "Atomic"
---

<objective>
Add the Redis hot-state primitives Phase 9 needs before wiring behavior into gateway traffic.

Purpose: later plans need one narrow Redis surface for cache bytes, atomic limit counters, session blacklist checks, and typed config events.
Output: expanded `hotstate.Client` capabilities with Redis and local implementations plus focused tests.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-redis-stack-qdrant-fallback-integration/09-CONTEXT.md
@.planning/phases/07-adapter-interfaces-sqlite-foundation/07-CONTEXT.md
@internal/hotstate/hotstate.go
@internal/hotstate/redis.go
@internal/hotstate/local.go
@tests/integration/redis_hotstate_test.go
@C:/Users/inthe/IdeaProjects/Notes-sur-l-IA/Projects/Agent-gateway/gateway-ratelimiting.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define hot-state primitives without widening business logic</name>
  <files>internal/hotstate/hotstate.go, internal/hotstate/hotstate_test.go</files>
  <behavior>
    - Test 1: namespaced keys are stable and never accept an empty namespace for Redis construction.
    - Test 2: config change messages carry an event type and target ID without secret-bearing fields.
    - Test 3: atomic limit requests model check-and-increment inputs without storing raw prompts, tokens, or Authorization values.
  </behavior>
  <action>Extend `hotstate` with narrow interfaces: `ByteCache` for `GetBytes/SetBytes/Delete`, `AtomicLimiter` for fixed-window check-and-increment plus optional adjustment, `SessionBlacklist` for blacklist add/check, and typed `ConfigChangeMessage` fields `Type` and `TargetID` while preserving backward compatibility for existing provider fields if needed. Keep the API small: no generic Redis command escape hatch and no config structs for values not used in Phase 9. Add constants for event types such as `provider`, `combo`, `semantic_rules`, `api_key`, `limit_rule`, and `vector_policy`. Tests must prove messages remain secret-safe by reflection.</action>
  <verify>
    <automated>go test ./internal/hotstate -run "Test.*HotState|Test.*ConfigChange|Test.*Namespaced" -timeout 60s</automated>
  </verify>
  <done>Hot-state interfaces express the minimum Redis behavior needed by Phase 9 and keep secrets/raw content out of messages and keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Redis and local hot-state primitives</name>
  <files>internal/hotstate/redis.go, internal/hotstate/local.go, internal/hotstate/hotstate_test.go, tests/integration/redis_hotstate_test.go</files>
  <behavior>
    - Test 1: Redis fixed-window counter accepts requests below limit, rejects above limit, and sets TTL only on first write.
    - Test 2: local fallback mirrors the same accept/reject behavior for unit tests and degraded mode.
    - Test 3: byte cache and session blacklist use namespaced keys with TTL.
    - Test 4: Pub/Sub preserves typed event fields and remains backward-compatible with existing config-change subscribers.
  </behavior>
  <action>Implement the new interfaces on `RedisClient` using Redis commands already available through `github.com/redis/go-redis/v9`. Use a small Lua script for atomic check-and-increment so concurrent requests cannot pass a limit by racing. Implement local fallback with a mutex-protected map and expiry timestamps. Do not add a new dependency or a Redis abstraction framework. Keep Redis errors visible to callers; only app-level wiring decides whether to degrade to local.</action>
  <verify>
    <automated>go test ./internal/hotstate ./tests/integration -run "TestRedisHotState|TestLocalHotState|TestAtomic" -timeout 60s</automated>
  </verify>
  <done>Redis and local hot state expose cache bytes, atomic counters, blacklist, and typed Pub/Sub behavior with matching semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Redis | Runtime request/account state crosses into hot cache. |
| admin/config -> Pub/Sub | Config events cross process boundaries. |
| Redis -> gateway | Hot-state values affect admission and reload behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | Redis keys/messages | mitigate | Use opaque IDs/hashes only; tests reject secret/prompt/auth fields in config messages. |
| T-09-01-02 | Tampering | limit counters | mitigate | Use Redis Lua for atomic check-and-increment; no read-then-write race. |
| T-09-01-03 | Denial of Service | Redis outage | mitigate | Preserve local fallback implementation; app wiring handles degrade policy explicitly. |
| T-09-01-SC | Tampering | dependencies | accept | Reuse existing `go-redis/v9`; add no new package. |
</threat_model>

<verification>
Run hot-state tests and Redis integration tests with a hard 60-second timeout:

<automated>go test ./internal/hotstate ./tests/integration -run "TestRedisHotState|TestLocalHotState|TestAtomic|TestConfigChange" -timeout 60s</automated>
</verification>

<success_criteria>
- D-04 through D-06, D-12, and D-14 have reusable primitives without gateway rewiring yet.
- Redis atomic counter behavior is tested.
- Pub/Sub messages are typed and secret-safe.
- Local fallback stays available for no-Redis tests and degraded mode.
</success_criteria>

<output>
Create `.planning/phases/09-redis-stack-qdrant-fallback-integration/09-01-SUMMARY.md` when done.
</output>
