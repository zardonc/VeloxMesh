---
phase: "09-redis-stack-qdrant-fallback-integration"
plan: "04"
type: execute
wave: 4
status: completed
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
files_modified:
  - "internal/storage/redis_vss.go"
  - "internal/storage/redis_vss_test.go"
  - "internal/storage/qdrant.go"
  - "internal/cache/semantic.go"
  - "internal/cache/semantic_test.go"
  - "internal/app/app.go"
  - "internal/app/app_test.go"
  - "internal/hotstate/hotstate.go"
  - "internal/controlstate/admin_service.go"
  - "internal/controlstate/admin_combo_service.go"
  - "internal/controlstate/admin_semantic_rules_service.go"
autonomous: true
requirements:
  - "Phase 9: Redis Stack Integration"
must_haves:
  truths:
    - "Qdrant remains primary and Redis VSS is default off per D-01 and D-02"
    - "Redis VSS activates only on hard Qdrant failure or timeout/slow-threshold degradation policy per D-02"
    - "Vector degradation never blocks core LLM proxying/auth/routing/fallback per D-03"
    - "Config hot reload routes by event type per D-14 and reuses existing behavior per D-15"
  artifacts:
    - path: "internal/storage/redis_vss.go"
      provides: "Redis VSS vector adapter fallback behind the existing VectorAdapter seam"
    - path: "internal/cache/semantic.go"
      provides: "Semantic cache fallback ordering: Qdrant primary, Redis VSS fallback, SQLite/noop safe behavior"
    - path: "internal/app/app.go"
      provides: "Qdrant degradation policy and event-type config reload dispatch"
  key_links:
    - from: "internal/app/app.go"
      to: "internal/storage/redis_vss.go"
      via: "vector fallback selected only under degradation policy"
      pattern: "VectorAdapter"
---

<objective>
Wire Redis VSS fallback and event-type hot reload after the durable Redis primitives exist.

Purpose: finish Phase 9 by hardening the vector fallback path and replacing blanket config reloads with typed event dispatch.
Output: Redis VSS adapter, Qdrant degradation policy wiring, semantic-cache fallback behavior, and typed hot-reload routing.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-redis-stack-qdrant-fallback-integration/09-CONTEXT.md
@.planning/phases/07-adapter-interfaces-sqlite-foundation/07-CONTEXT.md
@internal/storage/interfaces.go
@internal/storage/qdrant.go
@internal/cache/semantic.go
@internal/app/app.go
@internal/hotstate/hotstate.go
@internal/controlstate/admin_service.go
@internal/controlstate/admin_combo_service.go
@internal/controlstate/admin_semantic_rules_service.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Redis VSS behind the vector adapter seam</name>
  <files>internal/storage/redis_vss.go, internal/storage/redis_vss_test.go, internal/storage/interfaces.go</files>
  <behavior>
    - Test 1: Redis VSS adapter implements `VectorAdapter`.
    - Test 2: insert/search/delete use namespaced Redis keys and do not store raw prompts outside explicit metadata already accepted by semantic cache.
    - Test 3: adapter returns clear unsupported/degraded errors when Redis Stack VSS capability is unavailable.
  </behavior>
  <action>Add a `RedisVSSVectorAdapter` that satisfies the existing `storage.VectorAdapter` contract. Keep the implementation narrow and compatible with Redis Stack query/index commands exposed through existing go-redis primitives. If go-redis does not expose a typed helper, use explicit Redis commands in this one adapter only; do not add a new dependency. Keep Redis VSS default off and avoid changing semantic cache behavior in this task. Delete must be implemented enough for semantic-cache invalidation by ID/scope metadata; if richer filter deletion would sprawl, return a clear unsupported error for unsupported filters and document it in tests.</action>
  <verify>
    <automated>go test ./internal/storage -run "TestRedisVSS" -timeout 60s</automated>
  </verify>
  <done>Redis VSS is available as a vector adapter, but not yet selected unless app/cache wiring opts into fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select Redis VSS only under Qdrant degradation policy</name>
  <files>internal/app/app.go, internal/app/app_test.go, internal/cache/semantic.go, internal/cache/semantic_test.go, internal/storage/qdrant.go</files>
  <behavior>
    - Test 1: healthy Qdrant is selected as primary vector adapter.
    - Test 2: Qdrant hard initialization failure can select Redis VSS when fallback is enabled and Redis VSS is available.
    - Test 3: Qdrant timeout/slow-threshold policy marks vector degraded and can select Redis VSS fallback.
    - Test 4: when Qdrant and Redis VSS are both unavailable, semantic cache degrades/noops and core chat startup still works.
  </behavior>
  <action>Add a minimal vector fallback selection helper in app/cache wiring. Qdrant remains primary. Redis VSS is selected only when config enables fallback and Qdrant either fails health check or breaches a configured timeout/slow threshold. Keep threshold configuration small and local to vector fallback; avoid a broad policy engine. Semantic cache lookup/store should treat vector errors as cache misses/degraded behavior where safe, while still logging the root cause. Do not let vector initialization failure block core LLM proxying.</action>
  <verify>
    <automated>go test ./internal/app ./internal/cache ./internal/storage -run "Test.*Vector|Test.*Qdrant|Test.*RedisVSS|TestSemanticCache" -timeout 60s</automated>
  </verify>
  <done>Semantic cache uses Qdrant first, Redis VSS only under degradation policy, and no vector outage blocks core gateway traffic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route config hot reload by event type</name>
  <files>internal/hotstate/hotstate.go, internal/app/app.go, internal/app/app_test.go, internal/controlstate/admin_service.go, internal/controlstate/admin_combo_service.go, internal/controlstate/admin_semantic_rules_service.go</files>
  <behavior>
    - Test 1: provider events reload providers/secrets/routing as needed.
    - Test 2: semantic-rule events refresh semantic rule snapshots without unrelated provider secret reload work.
    - Test 3: API-key and limit-rule events invalidate hot caches or reload limit state without blanket provider reload.
    - Test 4: unknown event types log a warning and choose the safest existing reload path without swallowing errors.
  </behavior>
  <action>Update config publishers to populate the typed `ConfigChangeMessage` event from Plan 09-01. Replace `StartConfigChangeSubscriber` blanket reload behavior with a small dispatch function. Reuse existing `ReloadProviders` for provider/combo/routing events. Add focused handlers for semantic rules, API-key cache invalidation, limit rules, and vector policy where the relevant runtime state exists. If a handler cannot update state incrementally yet, make that explicit and call the narrowest existing reload helper rather than reloading everything. Log event type, target ID, action, and revision only; never log secrets, API keys, prompts, or raw rule payloads.</action>
  <verify>
    <automated>go test ./internal/app ./internal/controlstate -run "Test.*ConfigChange|Test.*HotReload" -timeout 60s</automated>
  </verify>
  <done>Config Pub/Sub uses typed event routing and avoids blanket reloads for semantic-rule/API-key/limit-rule changes where a narrower path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Qdrant/Redis -> semantic cache | External vector stores affect cache lookup/store. |
| Pub/Sub -> app reload | Cross-process events trigger runtime state changes. |
| admin services -> hot-state publish | Admin writes publish reload events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Denial of Service | vector fallback | mitigate | Vector outages degrade semantic cache only; core LLM proxy path continues. |
| T-09-04-02 | Tampering | Redis VSS fallback | mitigate | Fallback is config/degradation gated; Qdrant remains primary by default. |
| T-09-04-03 | Information Disclosure | config event logs | mitigate | Log event metadata only; publish no secrets/raw payloads. |
| T-09-04-04 | Spoofing | config event type | mitigate | Unknown event types warn and use safe fallback behavior; no silent no-op on critical reload errors. |
| T-09-04-SC | Scope creep | vector policy engine | accept | Keep degradation policy to hard failure and timeout/slow threshold only. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
ROADMAP | Phase 9 | RedisCacheAdapter implementation | 09-01 | COVERED | Byte cache primitive in hotstate; no separate broad cache framework.
ROADMAP | Phase 9 | Redis VSS fallback for vector data default off | 09-04 | COVERED | Qdrant primary, Redis only on degradation.
ROADMAP | Phase 9 | Atomic rate limiting via Redis INCR | 09-01, 09-02 | COVERED | Lua check-and-increment, admission gate.
ROADMAP | Phase 9 | LimitRule direction; full DB unification deferred | 09-02 | COVERED | Minimal domain/store, deferred full unification.
ROADMAP | Phase 9 | Config Pub/Sub hot-reload | 09-01, 09-04 | COVERED | Typed event publish and dispatch.
ROADMAP | Phase 9 | Token cost aggregation buffer | 09-03 | COVERED | Redis aggregation after SQLite settlement.
ROADMAP | Phase 9 | Session blacklist via Redis SET | 09-03 | COVERED | Redis hot check plus SQLite recoverability.
ROADMAP | Phase 9 | API key hot cache with 5min TTL | 09-03 | COVERED | Safe identity envelope, no raw token.
CONTEXT | D-01 | Qdrant remains primary | 09-04 | COVERED | Fallback selection tests.
CONTEXT | D-02 | Redis VSS only on Qdrant failure/slow threshold | 09-04 | COVERED | Degradation policy.
CONTEXT | D-03 | Vector degradation does not block core proxying | 09-04 | COVERED | Noop/degraded behavior.
CONTEXT | D-04 | Redis is hot-path acceleration, not source of truth | 09-01, 09-02, 09-03 | COVERED | SQLite truth preserved.
CONTEXT | D-05 | Listed Redis state reconciles/flushes to SQLite | 09-02, 09-03 | COVERED | Rules/session/settlement.
CONTEXT | D-06 | SQLite authoritative for rules/totals/audit/recoverable state | 09-02, 09-03 | COVERED | SQLite repos remain final record.
CONTEXT | D-07 | Rate limit and budget separate | 09-02 | COVERED | LimitRule dimensions and SQLite balance.
CONTEXT | D-08 | Minimal LimitRule subset only | 09-02 | COVERED | API/upstream subset.
CONTEXT | D-09 | Full LimitRule unification deferred in roadmap | 09-02 | COVERED | Roadmap note remains.
CONTEXT | D-10 | API and upstream gates both required | 09-02 | COVERED | Admission checks both.
CONTEXT | D-11 | Lifetime budget SQLite-authoritative | 09-02, 09-03 | COVERED | Settlement preserved.
CONTEXT | D-12 | Periodic/RPM counters use Redis atomic execution | 09-01, 09-02 | COVERED | Lua counter.
CONTEXT | D-13 | Provider balance ratio out of scope | 09-02 | COVERED | Validation rejects ratio dimension.
CONTEXT | D-14 | Pub/Sub reload by event type | 09-01, 09-04 | COVERED | Typed dispatch.
CONTEXT | D-15 | Reuse existing event behavior | 09-04 | COVERED | Existing publishers updated.
</source_audit>

<verification>
Run the vector/reload tests, then the full Phase 9 touched surface:

<automated>go test ./internal/storage ./internal/cache ./internal/app ./internal/controlstate -run "TestRedisVSS|Test.*Vector|Test.*Qdrant|Test.*ConfigChange|Test.*HotReload" -timeout 60s</automated>
<automated>go test ./internal/hotstate ./internal/controlstate/sqlite ./internal/admission ./internal/http/middleware ./internal/gateway ./internal/storage ./internal/cache ./internal/app ./tests/integration -timeout 60s</automated>
</verification>

<success_criteria>
- D-01 through D-15 are covered across the four Phase 9 plans.
- Redis VSS is available but not default.
- Qdrant hard failure and slow/timeout degradation can activate Redis VSS when configured.
- Config hot reload is event-type based.
- Core LLM proxying continues when vector systems degrade.
</success_criteria>

<output>
Create `.planning/phases/09-redis-stack-qdrant-fallback-integration/09-04-SUMMARY.md` when done.
</output>
