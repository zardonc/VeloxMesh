---
phase: "09-redis-stack-qdrant-fallback-integration"
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - "internal/controlstate/limits.go"
  - "internal/controlstate/sqlite/limits.go"
  - "internal/controlstate/sqlite/limits_test.go"
  - "internal/controlstate/migrations/sqlite/0004_limit_rules.sql"
  - "internal/admission/controller.go"
  - "internal/admission/controller_test.go"
  - "internal/app/app.go"
autonomous: true
requirements:
  - "Phase 9: Redis Stack Integration"
must_haves:
  truths:
    - "Rate limiting and lifetime budget are separate concepts per D-07"
    - "Phase 9 introduces a LimitRule domain/interface but persists only the smallest necessary subset per D-08"
    - "API-layer limits and upstream-account limits are both required gates with no override arbitration per D-10"
    - "Lifetime budget remains SQLite-authoritative per D-11"
    - "Full LimitRule database unification remains deferred per D-09"
  artifacts:
    - path: "internal/controlstate/limits.go"
      provides: "Minimal LimitRule domain model and repository contract"
    - path: "internal/controlstate/migrations/sqlite/0004_limit_rules.sql"
      provides: "SQLite persistence for Phase 9 API-key and upstream-account limit rules"
    - path: "internal/admission/controller.go"
      provides: "Admission gate that evaluates SQLite budget plus Redis atomic periodic/RPM checks"
  key_links:
    - from: "internal/admission/controller.go"
      to: "internal/hotstate/hotstate.go"
      via: "AtomicLimiter check-and-increment"
      pattern: "LimitRule"
---

<objective>
Implement the minimal Redis-backed rate/budget admission gates.

Purpose: API keys and upstream accounts need concurrency-safe limit checks without turning Phase 9 into a full quota platform rewrite.
Output: a small `LimitRule` model, SQLite rule persistence for the Phase 9 subset, and admission checks using Redis atomic counters where appropriate.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-redis-stack-qdrant-fallback-integration/09-CONTEXT.md
@internal/admission/controller.go
@internal/controlstate/repository.go
@internal/controlstate/types.go
@internal/controlstate/sqlite/repository.go
@internal/controlstate/migrations/sqlite/0001_control_state.sql
@internal/http/middleware/auth.go
@C:/Users/inthe/IdeaProjects/Notes-sur-l-IA/Projects/Agent-gateway/gateway-ratelimiting.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the minimal LimitRule contract and SQLite store</name>
  <files>internal/controlstate/limits.go, internal/controlstate/sqlite/limits.go, internal/controlstate/sqlite/limits_test.go, internal/controlstate/migrations/sqlite/0004_limit_rules.sql, internal/controlstate/sqlite/migrations.go</files>
  <behavior>
    - Test 1: API-key RPM and periodic budget rules save, list by scope, and validate allowed dimensions/windows.
    - Test 2: upstream-account RPM and periodic absolute usage rules save, list by provider scope, and reject provider-balance ratio rules.
    - Test 3: lifetime budget is represented as SQLite-authoritative API key credit balance, not Redis-only state.
    - Test 4: unsupported future scopes are rejected but named as deferred, not silently accepted.
  </behavior>
  <action>Add `LimitRule` types with scope `api_key` and `upstream_account`, dimensions `rpm`, `periodic_budget`, and `periodic_requests`, and windows `1m`, `5h`, `1d`, and `7d`. Do not implement team/model/global scopes yet. Add a narrow `LimitRuleRepository` and expose it from the concrete SQLite repository only if that matches existing semantic-rule pattern; avoid widening PostgreSQL unless required by interfaces. Add migration `0004_limit_rules.sql` for the minimal Phase 9 subset. Update the SQLite migrator file list. Validate that upstream provider balance ratio is not accepted per D-13. Keep full database-wide LimitRule unification deferred as the roadmap already records.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "TestLimitRule" -timeout 60s</automated>
  </verify>
  <done>SQLite stores only the minimal Phase 9 LimitRule subset and rejects unsupported/future dimensions explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Redis atomic checks into admission</name>
  <files>internal/admission/controller.go, internal/admission/controller_test.go, internal/app/app.go</files>
  <behavior>
    - Test 1: missing or disabled auth identity is rejected before Redis calls.
    - Test 2: exhausted SQLite credit balance rejects even if Redis counters would pass.
    - Test 3: API-key RPM and periodic budget limits reject via Redis atomic counters when over limit.
    - Test 4: upstream-account/provider limits are checked after route selection and reject independently from API-key limits.
    - Test 5: Redis unavailable returns a visible error when correctness would be compromised; it does not silently allow over-limit requests.
  </behavior>
  <action>Replace `CreditAdmissionController` with a small `LimitAdmissionController` or extend it only as much as needed. Keep the cheap static checks first: identity, enabled key, SQLite lifetime credit balance. Then evaluate API-key limit rules and upstream-account/provider limit rules using the `hotstate.AtomicLimiter` from Plan 09-01. Use `routing.RoutingDecision.ProviderID` as the upstream-account scope unless planning finds an existing account ID field. Return `429` with `X-RateLimit-Remaining-Tokens` or an equivalent existing header where applicable. Do not deduct final lifetime budget here; continue to let settlement update SQLite after provider usage is known. App startup should inject the limiter only when Redis/hotstate supports it, and fail visibly if configured limits require Redis but no atomic limiter is available.</action>
  <verify>
    <automated>go test ./internal/admission ./internal/app -run "Test.*Limit|Test.*Admission" -timeout 60s</automated>
  </verify>
  <done>Admission enforces SQLite lifetime budget plus Redis atomic API/upstream periodic limits without adding override arbitration between gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API key identity -> admission | Authenticated identity controls which user/app limits apply. |
| route decision -> upstream limits | Selected provider/upstream account controls global/shared limit checks. |
| Redis counters -> admission decision | Hot counters reject or allow request traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation | API/upstream gates | mitigate | Evaluate both gates independently; any failure rejects per D-10. |
| T-09-02-02 | Tampering | lifetime budget | mitigate | Keep lifetime budget SQLite-authoritative; Redis is not final truth. |
| T-09-02-03 | Denial of Service | Redis outage | mitigate | Fail visibly for Redis-required atomic limits instead of silently allowing traffic. |
| T-09-02-04 | Repudiation | usage/budget | mitigate | Preserve SQLite settlement and usage records as audit trail. |
| T-09-02-SC | Tampering | schema scope | accept | Only minimal Phase 9 LimitRule subset is implemented; full unification is deferred by roadmap. |
</threat_model>

<verification>
Run admission and SQLite limit tests first, then touched packages:

<automated>go test ./internal/controlstate/sqlite ./internal/admission ./internal/app -run "TestLimitRule|Test.*Limit|Test.*Admission" -timeout 60s</automated>
</verification>

<success_criteria>
- D-07 through D-13 are represented in tested behavior.
- API-key and upstream-account gates both run and cannot override each other.
- Lifetime budget remains SQLite-authoritative.
- Unsupported full LimitRule scopes remain deferred, not half-implemented.
</success_criteria>

<output>
Create `.planning/phases/09-redis-stack-qdrant-fallback-integration/09-02-SUMMARY.md` when done.
</output>
