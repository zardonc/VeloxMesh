---
phase: 29-semantic-cache-latency-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - internal/config/config_types.go
  - internal/config/config_file.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
  - internal/cache/semantic.go
  - internal/cache/semantic_test.go
  - internal/gateway/service.go
  - internal/gateway/service_tool_test.go
  - internal/app/semantic_cache.go
  - internal/app/app.go
  - internal/observability/metrics.go
  - internal/observability/prometheus.go
  - tests/integration/semantic_cache_test.go
autonomous: false
requirements:
  - CACHE-F01
user_setup: []
must_haves:
  truths:
    - "D-06: embed, vector, repository, timeout, malformed-entry and queue faults cause immediate reason-recorded bypass or write drop, never a cache-caused request 5xx."
    - "D-07: disabled cache has no external I/O; enabled lookup has one measured short budget, and response completion does not wait for embedding/vector writes."
    - "Queued writes capture immutable version/model/answer identity and cannot revive an old-version hit after publication."
    - "Cache fault observability uses a bounded reason vocabulary without prompts, answers, credentials, or high-cardinality IDs."
  prohibitions:
    - statement: "MUST NOT retry a failed cache read by an unbounded SQLite scan after vector/embedding failure."
      status: resolved
      verification: test
    - statement: "MUST NOT delay or fail the primary response because an optional cache write failed or the queue is full."
      status: resolved
      verification: test
  artifacts:
    - path: internal/cache/semantic.go
      provides: "Budgeted lookup and bounded optional write worker with explicit lifecycle."
    - path: internal/gateway/service.go
      provides: "Immediate primary forwarding on cache read miss/fault and nonblocking candidate enqueue."
    - path: internal/observability/prometheus.go
      provides: "Low-cardinality cache outcome/fault counters."
  key_links:
    - from: internal/gateway/service.go
      to: internal/cache/semantic.go
      via: "One cache read attempt and nonblocking enqueue; no cache error returns to the client."
    - from: internal/app/app.go
      to: internal/cache/semantic.go
      via: "Bounded worker shutdown coordinated with the existing app lifecycle."
---

<objective>
Bound optional cache work so a low-hit or failing cache cannot dominate primary-model latency. Keep all existing provider, routing and Usage ownership unchanged.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
@.codex/gsd-core/references/checkpoints.md
@.planning/phases/29-semantic-cache-latency-hardening/29-CONTEXT.md
@.planning/phases/29-semantic-cache-latency-hardening/29-RESEARCH.md
@.planning/phases/29-semantic-cache-latency-hardening/29-01-PLAN.md
</execution_context>

<tasks>
  <task type="checkpoint:decision" gate="blocking">
    <name>Approve measured read and worker bounds</name>
    <decision>Approve measured cache-read budget, worker/queue bounds and graceful shutdown limit under contract F.</decision>
    <context>These numeric limits must be derived from representative primary, embedding and vector-store latency; an invented constant could fail the D-08 release gate.</context>
    <options>
      <option id="measured"><name>Use baseline-derived bounds</name><pros>Quantifiable latency tradeoff.</pros><cons>Requires a controlled pre-implementation baseline.</cons></option>
      <option id="disabled"><name>Keep cache off</name><pros>No added latency risk.</pros><cons>Does not deliver eligible cache hits.</cons></option>
    </options>
    <resume-signal>Record approved values, workload and owner, or defer cache enablement.</resume-signal>
  </task>
  <task type="auto" tdd="true">
    <name>Write fault and concurrency tests before changing the read/write path</name>
    <files>internal/cache/semantic_test.go, internal/gateway/service_tool_test.go, tests/integration/semantic_cache_test.go, internal/config/config_test.go</files>
    <read_first>Read `29-RESEARCH.md` pitfalls, approved budget/queue values, current `Lookup` and `Store`, `Service.HandleChatCompletion`, `App.Close`, vector adapters, and existing observability labels.</read_first>
    <action>List failure modes, then create red tests for embed delay/error; vector search/insert timeout or error; repository failure; malformed response; dimension mismatch; no SQLite fallback after vector fault; queue saturation; write worker cancellation; bounded shutdown; default-off zero I/O; old-version queued write isolation. Use a primary-provider spy to prove one ordinary call and no cache-caused 5xx. Assert returned response is observable before deliberately slow write completion, and logs/metrics contain only enumerated reasons.</action>
    <verify>
      <automated>go test -timeout 60s ./internal/cache ./internal/gateway ./internal/config ./tests/integration -run 'TestSemanticCache|TestCacheConfig'</automated>
      <fails_when>Before production changes, an all-green run with no new red cases means the behavior was not asserted; timeout or hung tests indicate missing boundedness.</fails_when>
    </verify>
    <acceptance_criteria>Offline tests demonstrate each D-06/D-07 fault and at least one slow-write race before implementation.</acceptance_criteria>
    <done>The new tests fail specifically on the current synchronous or silent-fault behavior.</done>
  </task>
  <task type="auto" tdd="true">
    <name>Implement budgeted read, bounded write and reason-only observability</name>
    <files>internal/config/config_types.go, internal/config/config_file.go, internal/config/config_validation.go, internal/cache/semantic.go, internal/gateway/service.go, internal/app/semantic_cache.go, internal/app/app.go, internal/observability/metrics.go, internal/observability/prometheus.go</files>
    <read_first>Read Task 2 red tests and approved values, existing config merge order, cache/vector/repository interfaces, request cancellation and app lifecycle. Preserve D-03's immutable version identity from 29-01.</read_first>
    <action>Give one lookup attempt its own short context deadline and finite concurrency limit. Treat embedding/vector/repository faults as immediate bypass, with no hidden full-scan fallback after a dependency error. Send immutable candidate snapshots to a finite optional worker queue only after a valid primary response; full queue drops work and increments a reason-only counter without waiting or failing the response. Worker uses an independent bounded context, checks vector dimension, keeps old-version identity frozen, and has bounded shutdown. Surface miss, timeout, dependency error and queue-full reasons via existing observability conventions, never raw prompts, answers, IDs or secrets. Keep debug/error signals visible in internal diagnostics even though the client receives normal forwarding.</action>
    <verify>
      <automated>go test -timeout 60s ./internal/cache ./internal/gateway ./internal/config ./internal/app ./internal/observability ./tests/integration -run 'TestSemanticCache|TestCacheConfig|Test.*Tool'</automated>
      <fails_when>Non-zero exit, zero new cache cases selected, a cache-induced 5xx, response waits for a slow write, vector fault triggers a second unbounded scan, or a reason label carries request content.</fails_when>
    </verify>
    <acceptance_criteria>Red tests pass with bounded read, nonblocking write and reason-only diagnostics; the disabled path remains free of cache I/O.</acceptance_criteria>
    <done>Cache work is optional and bounded, and primary forwarding remains available on every injected cache fault.</done>
  </task>
</tasks>

<verification>
`go test -timeout 60s ./internal/cache ./internal/gateway ./internal/config ./internal/app ./internal/observability ./tests/integration` must pass before final phase validation.
</verification>

## Artifacts This Phase Produces

- Provisional `CacheConfig` budget/queue/worker fields, finalized from the approved baseline.
- One bounded cache write lifecycle in `SemanticCacheService`, exposed through existing app shutdown; exact internal symbol names follow repository patterns.
- Low-cardinality cache fault/outcome metrics and fault-injection cases in existing test files.
