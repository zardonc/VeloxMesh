---
phase: 29-semantic-cache-latency-hardening
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - internal/config/config_types.go
  - internal/config/config_file.go
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
  - internal/llm/types.go
  - internal/http/handlers/chat.go
  - internal/gateway/service.go
  - internal/app/semantic_cache.go
  - internal/cache/semantic.go
  - internal/cache/semantic_test.go
  - tests/integration/semantic_cache_test.go
autonomous: false
requirements:
  - CACHE-F01
user_setup: []
must_haves:
  truths:
    - "D-01 and D-02: disabled or non-allowlisted traffic, including tools, stream, multimodal, structured, live, sensitive, exact-answer, and context-dependent requests, makes zero embedding/vector calls."
    - "D-03: tenant/auth scope, approved use case, knowledge version, target model, generation settings, and embedding identity separate candidate reads and writes; a new version cannot hit old entries."
    - "D-04: a valid cache hit returns a normal response with zero-token Usage and no nonexistent upstream charge; hits and estimated savings are distinct and payload-free."
    - "D-05: enabled cache needs a manually supplied embedding provider/model/dimension, neither Lookup nor Store has a hardcoded model, and wrong dimensions never compare or persist."
    - "Malformed stored choices never become successful cache hits."
  prohibitions:
    - statement: "MUST NOT infer allowlisting or authoritative knowledge version from arbitrary client text or headers."
      status: resolved
      verification: test
    - statement: "MUST NOT make excluded traffic call an embedding or vector service."
      status: resolved
      verification: test
  artifacts:
    - path: internal/cache/semantic.go
      provides: "Configured embedding identity and dimension-safe, version-scoped candidate lookup/store."
    - path: internal/gateway/service.go
      provides: "Trusted eligibility gate and valid zero-Usage hit path."
    - path: tests/integration/semantic_cache_test.go
      provides: "Red-first gateway lifecycle and negative isolation fixtures."
  key_links:
    - from: internal/http/handlers/chat.go
      to: internal/gateway/service.go
      via: "Typed request shape and trusted server-side cache context, without a new client-controlled cache selector."
    - from: internal/gateway/service.go
      to: internal/cache/semantic.go
      via: "Identical opaque answer-validity identity on lookup and store."
---

<objective>
Create one safe end-to-end non-streaming FAQ/document cache path on the existing gateway and cache services. This wave freezes who may opt in and what makes an old answer valid; it leaves latency optimization to 29-02 and never authorizes deployment on its own.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
@.codex/gsd-core/references/checkpoints.md
@.planning/phases/29-semantic-cache-latency-hardening/29-CONTEXT.md
@.planning/phases/29-semantic-cache-latency-hardening/29-RESEARCH.md
@.planning/phases/29-semantic-cache-latency-hardening/29-OVERVIEW.md
</execution_context>

<tasks>
  <task type="checkpoint:decision" gate="blocking">
    <name>Confirm trusted cache eligibility and version ownership</name>
    <decision>Confirm `29-OVERVIEW.md` contracts A-E: trusted application/use-case allowlist, authoritative knowledge version publication, auth partition, allowed request profile/settings, and the two embedding model/dimension mappings.</decision>
    <context>The current HTTP request has no trusted corpus/version field. A client-supplied selector would allow stale or cross-scope reuse; the implementation cannot safely infer one.</context>
    <options>
      <option id="server-owned"><name>Server-owned mapping</name><pros>Fail-closed and no new public API.</pros><cons>Requires a configuration or control-state owner for publication.</cons></option>
      <option id="defer-cache"><name>Defer enablement</name><pros>Preserves safety while ownership is unresolved.</pros><cons>Blocks the eligible-hit acceptance gate.</cons></option>
    </options>
    <resume-signal>Approve a concrete trusted mapping and version-publishing contract, or defer Phase 29 implementation.</resume-signal>
  </task>
  <task type="auto" tdd="true">
    <name>Write red gateway and configuration contract tests</name>
    <files>internal/config/config_test.go, internal/cache/semantic_test.go, tests/integration/semantic_cache_test.go</files>
    <read_first>Read the checkpoint outcome, `29-CONTEXT.md` D-01 to D-05, `29-RESEARCH.md`, `internal/http/handlers/chat.go`, `internal/llm/types.go`, `internal/gateway/service.go`, `internal/cache/semantic.go`, the existing integration test helpers, and both relational candidate implementations.</read_first>
    <action>Enumerate the failure modes first, then encode red-first tests: cache disabled; absent allowlist/version; every D-02 excluded shape; changed temperature/max tokens or unknown answer-affecting field; tenant/auth/use-case/version/target-model/embedding-model/dimension partition; near-identical question with different correct answer; malformed cached choices; wrong vector length; nonmatching TTL; normal primary response followed by a permitted paraphrase hit with zero Usage and no extra settlement. Assert embedding/vector call counts and preserve the public response/header contract. Use trusted fixture metadata only, never a free-form client label.</action>
    <verify>
      <automated>go test -timeout 60s ./internal/config ./internal/cache ./tests/integration -run 'TestSemanticCache|TestCacheConfig'</automated>
      <fails_when>Non-zero exit is expected while the required contract is absent; a green run before implementation with no new failing assertions means the test did not capture the gap.</fails_when>
    </verify>
    <acceptance_criteria>Named offline tests expose each D-01 to D-05 failure, observe zero cache I/O for bypassed requests, and fail before production changes for missing version/model isolation.</acceptance_criteria>
    <done>The red tests are deterministic and demonstrate the current unsafe or absent behavior.</done>
  </task>
  <task type="auto" tdd="true">
    <name>Implement the narrow eligible path with existing components</name>
    <files>internal/config/config_types.go, internal/config/config_file.go, internal/config/config.go, internal/config/config_validation.go, internal/llm/types.go, internal/http/handlers/chat.go, internal/gateway/service.go, internal/cache/semantic.go, internal/app/semantic_cache.go</files>
    <read_first>Read Task 2 red tests, the confirmed A-E contract, existing nested/legacy cache config loading, provider adapter construction, Phase 27 Usage ownership, and Phase 28 tool bypass tests.</read_first>
    <action>Keep cache default off. Add explicit embedding model, dimension, TTL, similarity threshold and candidate bound to existing cache config, with legacy ENV compatibility and no model string or corpus-specific bound fixed in business logic. Bind eligibility to confirmed trusted server-side use-case/version information. Construct one canonical opaque answer-validity scope used by both relational candidate queries and vector collection; include auth/use-case/version/target model/generation profile/embedding provider-model-dimension, without exposing raw IDs or payloads in collection names. Reject wrong returned vector dimension, malformed stored choices and stale candidates as misses. For labeled near-matches with different valid answers, tighten the approved profile/key or conservatively bypass ambiguous similarity matches; never tune a global threshold until those negatives pass. Preserve Phase 27 settlement: a hit has zero Usage and separately observed estimated savings, no synthetic upstream charge. Do not add a second cache subsystem, a new public endpoint, or a database migration without an explicit necessity review.</action>
    <verify>
      <automated>go test -timeout 60s ./internal/config ./internal/cache ./internal/gateway ./tests/integration -run 'TestSemanticCache|TestCacheConfig|Test.*Tool'</automated>
      <fails_when>Non-zero exit, zero tests selected for the new cache cases, any wrong-scope/version/model hit, a cache hit with nonzero Usage, or any embedding/vector call on an excluded request.</fails_when>
    </verify>
    <acceptance_criteria>All red tests turn green; `Lookup` and `Store` use the configured model; version and embedding switch immediately isolate old entries; excluded traffic stays on the ordinary route.</acceptance_criteria>
    <done>The safe thin gateway path is behaviorally complete but not yet latency-qualified.</done>
  </task>
</tasks>

<verification>
`go test -timeout 60s ./internal/config ./internal/cache ./internal/gateway ./tests/integration` must pass before starting 29-02. No real-model call or deployment in this wave.
</verification>

## Artifacts This Phase Produces

- Provisional internal cache config fields: embedding model and trusted allowlist/version mapping, finalized only after checkpoint A-E.
- One internal trusted cache context/eligibility function and canonical answer-validity identity in existing gateway/cache packages; exact symbol names follow repository conventions at implementation time.
- Red-first behavior cases in existing config/cache/integration test files. No external API symbols are added.
