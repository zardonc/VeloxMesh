---
phase: 29-semantic-cache-latency-hardening
plan: "03"
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - tests/integration/semantic_cache_test.go
  - tests/integration/semantic_cache_live_test.go
  - internal/cache/semantic_test.go
  - internal/gateway/service_tool_test.go
  - .planning/phases/29-semantic-cache-latency-hardening/29-EVIDENCE.md
autonomous: false
requirements:
  - CACHE-F01
user_setup:
  - "Provide isolated test-environment access to the existing `.env.local` embedding configurations and the configured vector store; never copy values into tracked files."
must_haves:
  truths:
    - "D-08: same-load low-hit non-streaming cache-on P95 complete-response latency is <= 1.05 times cache-off P95, with raw run details retained."
    - "D-09: curated near-match-different-answer, cross-tenant and cross-version negatives produce zero wrong hits; positive paraphrase hit rate is reported without an invented gate."
    - "D-10: embedding, vector and queue faults immediately bypass or drop optional work with recorded reason and no cache-caused 5xx."
    - "D-11 and D-12: both real embedding configurations pass a manually switched full gateway lifecycle against the configured vector store; deterministic tests remain the routine gate and evidence is redacted and repeatable."
  prohibitions:
    - statement: "MUST NOT treat a direct embeddings API call, synthetic-only vector test, or streaming TTFT as the required full-flow/P95 gate."
      status: resolved
      verification: human
    - statement: "MUST NOT publish credentials, `.env.local` values, raw prompts or answers in evidence."
      status: resolved
      verification: review
  artifacts:
    - path: tests/integration/semantic_cache_test.go
      provides: "Curated negative/positive and gateway-fault behavior cases."
    - path: tests/integration/semantic_cache_live_test.go
      provides: "Opt-in, bounded two-model gateway-flow runner for phase-end validation."
    - path: .planning/phases/29-semantic-cache-latency-hardening/29-EVIDENCE.md
      provides: "Redacted commands, environment shape, per-model gateway outcomes, raw latency summary and pass/fail gates."
  key_links:
    - from: tests/integration/semantic_cache_test.go
      to: internal/gateway/service.go
      via: "Full HTTP/gateway response path and fault bypass, not a direct embedding call."
---

<objective>
Close `CACHE-F01` with behavior, latency and two-model production-shape evidence. This plan is a release gate, not permission to deploy.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
@.planning/phases/29-semantic-cache-latency-hardening/29-CONTEXT.md
@.planning/phases/29-semantic-cache-latency-hardening/29-VALIDATION.md
@.planning/phases/29-semantic-cache-latency-hardening/29-OVERVIEW.md
</execution_context>

<tasks>
  <task type="auto" tdd="true">
    <name>Finish deterministic gateway acceptance cases</name>
    <files>tests/integration/semantic_cache_test.go, internal/cache/semantic_test.go, internal/gateway/service_tool_test.go</files>
    <read_first>Read `29-CONTEXT.md` D-08 to D-12, both preceding plan summaries, the approved A-F contracts, and the existing integration harness and cache tests.</read_first>
    <action>Complete and run a human-labeled, reviewable negative corpus: near-identical but different-answer questions, tenant/auth and knowledge-version crossovers, embedding-model/dimension switch, tool/stream/multimodal/structured/freshness bypass, malformed cached data and dependency faults. Add positive paraphrase cases and report hit rate. Keep cases deterministic and offline for routine merge checks. Before adding any implementation needed by an uncovered case, record the failing case and make it red first.</action>
    <verify>
      <automated>go test -timeout 60s ./internal/cache ./internal/gateway ./tests/integration -run 'TestSemanticCache|Test.*Tool'</automated>
      <fails_when>Non-zero exit, zero selected cache cases, any false hit among labeled negatives, an excluded request making a cache dependency call, or a fault causing cache-originated 5xx.</fails_when>
    </verify>
    <acceptance_criteria>Zero erroneous hits in the curated negative set; positive hit rate and failure reasons reported. Existing tool/stream bypass regressions remain green.</acceptance_criteria>
    <done>Deterministic offline acceptance cases pass with labeled fixtures.</done>
  </task>
  <task type="auto">
    <name>Run full backend and matched-load latency gate</name>
    <files>.planning/phases/29-semantic-cache-latency-hardening/29-EVIDENCE.md</files>
    <read_first>Read `29-VALIDATION.md`, the D-08 benchmark contract, the approved read budget, and the existing test/benchmark harness. Confirm the workload is low-hit and non-streaming.</read_first>
    <action>Only after development is complete, run the full backend suite and a repeatable controlled benchmark with cache off/on at the same request mix, concurrency, primary-provider latency distribution and warm-up. Capture full-response raw samples or reproducible summary, hit ratio, P50/P95, run count, environment and ratio; do not use HTTP header latency or streaming TTFT as the gate. Fix defects and rerun before signing off. Record all results in redacted `29-EVIDENCE.md`.</action>
    <verify>
      <automated>go test -timeout 60s ./...</automated>
      <fails_when>Non-zero exit or any package that did not complete within the hard 60-second test timeout.</fails_when>
      <manual>Reject if raw matched-load non-streaming evidence is absent or cache-on P95 / cache-off P95 exceeds 1.05.</manual>
    </verify>
    <acceptance_criteria>Full backend suite passes and D-08 P95 ratio <= 1.05 with recorded raw procedure and low hit ratio.</acceptance_criteria>
    <done>Offline and latency gates are evidenced, not inferred.</done>
  </task>
  <task type="auto">
    <name>Execute both real-model full gateway flows and record redacted evidence</name>
    <files>tests/integration/semantic_cache_live_test.go, .planning/phases/29-semantic-cache-latency-hardening/29-EVIDENCE.md</files>
    <read_first>Read `29-CONTEXT.md` D-11/D-12, approved embedding/provider/dimension mapping, the integration harness, configured vector-store setup and secret-handling policy. Read `.env.local` only in the isolated execution environment and never copy its values to tracked artifacts.</read_first>
    <action>At phase end, add an opt-in, bounded integration runner using the existing gateway test harness; do not put live calls into default CI. Start the test gateway and the actual configured vector store. Select each of the two existing embedding configurations in separate runs. For each, use the gateway chat endpoint for a primary response, wait within a bounded observation window for the asynchronous candidate to persist, send a safe paraphrase that must hit, switch embedding and knowledge versions to confirm old-entry isolation, then inject embedding/vector/queue faults and confirm ordinary primary forwarding with reason-only diagnostics. Capture exact repeatable commands with secret placeholders, model identifiers, environment shape, response status/cache headers/Usage, timings, and pass/fail in `29-EVIDENCE.md`. Stop test services afterward. Do not substitute a direct embeddings API call for any gateway step.</action>
    <verify>
      <automated>go test -timeout 60s ./tests/integration -run '^TestSemanticCacheLiveGateway$' -count=1 -v</automated>
      <fails_when>Non-zero exit, a skipped live test in the phase-end environment, missing gateway hit/isolation/fault assertions for the selected model, or a run exceeding 60 seconds.</fails_when>
      <manual>Run separately with each manually selected embedding configuration and confirm the redacted evidence contains no credentials or raw sensitive payloads.</manual>
    </verify>
    <acceptance_criteria>Every D-11 step passes for both real models against the actual vector-store path and is repeatable from redacted evidence.</acceptance_criteria>
    <done>Two full live gateway flows and a secret-safe artifact exist; any failing step blocks phase completion.</done>
  </task>
  <task type="checkpoint:human-verify" gate="blocking">
    <name>Review final release-gate evidence</name>
    <what-built>Executed two-model gateway-flow, deterministic and P95 evidence in `29-EVIDENCE.md`.</what-built>
    <how-to-verify>Review the redacted run record, confirm both distinct model identifiers and every gateway step passed, P95 ratio and negative-set gates hold, no secrets or request payloads are tracked, and cache-default-off rollback is documented. Do not accept a direct embeddings-only test.</how-to-verify>
    <resume-signal>Approve only when both model flows, isolation, fault bypass, and all release gates are evidenced; otherwise report the failing step.</resume-signal>
  </task>
</tasks>

<verification>
Phase completion requires every checkbox in `29-VALIDATION.md`, both real-model runs and zero negative false hits. No deployment is implied.
</verification>

## Artifacts This Phase Produces

- Curated gateway acceptance fixtures in existing test files.
- `TestSemanticCacheLiveGateway` in `tests/integration/semantic_cache_live_test.go`, opt-in for phase-end runs only.
- `29-EVIDENCE.md` redacted phase-gate record; no new public API or runtime symbol is expected.
