---
phase: 22-documentation-env-example-uat
plan: "01"
type: execute
wave: 1
depends_on: ["20-01", "20-02", "21-01", "21-02", "21-03"]
files_modified:
  - README.md
  - docs/scheduler-1.0-runbook.md
  - .env.example
  - config.json.example
  - config.scheduler.example.json
  - config.cache.example.json
  - config.heuristic.example.json
  - internal/config/config_test.go
  - .planning/phases/22-documentation-env-example-uat/22-UAT.md
autonomous: true
requirements: ["CFG-03", "CFG-04", "SCH-08", "QDR-06", "QDR-08", "OBS-03", "OBS-04", "OBS-05", "OBS-06"]
must_haves:
  truths:
    - "D-01 keep README as the quick path and add one focused Scheduler 1.0 operator runbook."
    - "D-02 optimize the runbook as a balanced operator guide: production-minded first, with local commands only where they prove behavior."
    - "D-03 runbook covers Scheduler down, Redis unavailable, Qdrant unavailable, ONNX predictor unhealthy, and admin API validation failure."
    - "D-04 runbook includes grouped env/JSON config reference plus local, scheduler-enabled, and semantic-neighbor examples."
    - "D-05 UAT guidance is hybrid: manual steps plus references to existing unit/integration tests; add only a tiny helper if it removes repeated command glue."
    - "D-06 keep .env.example curated with essential local and scheduler-facing variables only."
    - "D-07 add one full structured config example and keep focused scheduler, cache, and heuristic component examples."
    - "D-08 examples use placeholder env names such as OPENAI_PRIMARY_API_KEY and never inline secret-shaped credentials."
    - "D-09 document Qdrant and pgvector parity with one concise comparison table plus short examples."
    - "D-10 UAT proves scheduler enable/disable, degradation, semantic neighbors, admin APIs, examples parse/load, disabled-by-default startup, and secret-safe examples."
    - "D-11 produce a separate UAT report file with command, expected result, actual result, and notes."
    - "D-12 full real-provider UAT uses .env.local; prefer non-Gemini provider resources for routine real-provider checks and reserve Gemini resources for Gemini-specific scenarios."
    - "D-13 failed UAT checks include command output, root cause notes, and whether the issue is blocking or non-blocking."
  artifacts:
    - path: "docs/scheduler-1.0-runbook.md"
      provides: "Scheduler 1.0 operator deployment, config, degradation, vector, and UAT guide"
    - path: ".planning/phases/22-documentation-env-example-uat/22-UAT.md"
      provides: "Phase 22 UAT checklist and evidence report"
    - path: "config.json.example"
      provides: "complete nested structured config example"
  key_links:
    - from: "README.md"
      to: "docs/scheduler-1.0-runbook.md"
      via: "README links operators from quick path to detailed Scheduler 1.0 runbook"
---

<objective>
Publish the Scheduler 1.0 operator documentation, safe config examples, and Phase 22 UAT evidence.

Purpose: Phase 22 makes the completed v7.6 scheduler/config/admin behavior understandable, safely configurable, and verifiable without adding runtime features.
Output: README quick-path update, focused Scheduler 1.0 runbook, enriched examples, secret-safety checks, and `22-UAT.md`.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-documentation-env-example-uat/22-CONTEXT.md
@.planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
@.planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
@README.md
@.env.example
@config.json.example
@config.scheduler.example.json
@config.cache.example.json
@config.heuristic.example.json
@internal/config/config_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scheduler 1.0 operator runbook</name>
  <files>README.md, docs/scheduler-1.0-runbook.md</files>
  <read_first>
    - README.md
    - .planning/phases/22-documentation-env-example-uat/22-CONTEXT.md
    - .planning/phases/21-observability-admin-apis-tooling/21-CONTEXT.md
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
    - tools/scheduler_training/README.md
  </read_first>
  <action>Create `docs/scheduler-1.0-runbook.md`. Cover: Scheduler purpose and disabled-by-default fit; deployment prerequisites; local proof commands; enablement via `SCHEDULER_ENABLED=false|true`, `SCHEDULER_MODE=heuristic|onnx`, `SCHEDULER_ONNX_ROLLOUT_PERCENT`, `SCHEDULER_CONFIG_FILE`, `CACHE_CONFIG_FILE`, and `SCHEDULER_HEURISTIC_CONFIG_FILE`; admin endpoints `GET /admin/v1/scheduler/status`, `GET /admin/v1/scheduler/sla-rules`, `PUT /admin/v1/scheduler/sla-rules`, `GET /admin/v1/scheduler/training-samples/export`, and `PATCH /admin/scheduler/rollout`; degradation playbooks for Scheduler down, Redis unavailable, Qdrant unavailable, ONNX predictor unhealthy, and admin API validation failure; and UAT commands with `go test -timeout 60s`. Update README to keep only quick-path Scheduler Training/Rollout text and link to the runbook for operator detail.</action>
  <verify>
    <automated>rg -n "Scheduler 1.0|SCHEDULER_ENABLED|/admin/v1/scheduler/status|Qdrant|pgvector|go test -timeout 60s" README.md docs/scheduler-1.0-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/scheduler-1.0-runbook.md` contains headings for `Purpose`, `Deployment`, `Configuration`, `Degradation Playbooks`, `Vector Backends`, `Admin APIs`, and `UAT`.
    - README links to `docs/scheduler-1.0-runbook.md`.
    - The runbook states Scheduler is optional, disabled by default, and fail-open.
    - The runbook states Gateway owns queueing, task state, execution, semantic lookup, SLA promotion, fallback, and sensitive-payload boundaries.
    - Every backend unit-test command in the runbook uses `go test -timeout 60s`.
  </acceptance_criteria>
  <done>Operators have one focused Scheduler 1.0 guide without bloating the README.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curate examples and enforce secret safety</name>
  <files>.env.example, config.json.example, config.scheduler.example.json, config.cache.example.json, config.heuristic.example.json, internal/config/config_test.go</files>
  <read_first>
    - .env.example
    - config.json.example
    - config.scheduler.example.json
    - config.cache.example.json
    - config.heuristic.example.json
    - internal/config/config_test.go
    - .planning/phases/22-documentation-env-example-uat/22-CONTEXT.md
    - .planning/phases/20-config-unification-scheduler-core-hardening/20-CONTEXT.md
  </read_first>
  <action>Keep `.env.example` curated with local essentials and scheduler-facing toggles. Expand `config.json.example` into one complete nested shape using `control_state`, `redis`, `cache.qdrant`, `cache.pgvector`, and `scheduler` blocks while preserving disabled defaults. Keep component examples focused: `config.scheduler.example.json` for scheduler fields, `config.cache.example.json` for Qdrant/pgvector vector fields, and `config.heuristic.example.json` for only `base_latency` and `model_multipliers`. Extend `internal/config/config_test.go` to assert examples contain required disabled defaults and do not contain `sk-`, `dev_postgres_secret`, `dev_redis_secret`, `vx_qdrant_secret`, `password`, `token`, `api_key": "`, or provider credential values outside env-var placeholder names such as `OPENAI_PRIMARY_API_KEY`.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` still contains `# SCHEDULER_ENABLED=false`.
    - `.env.example` contains `# SCHEDULER_CONFIG_FILE=config.scheduler.example.json` and `# CACHE_CONFIG_FILE=config.cache.example.json`.
    - `config.json.example` parses as JSON and contains disabled `scheduler`, `redis`, and `cache` blocks.
    - `config.cache.example.json` includes both `qdrant` and `pgvector` examples.
    - `go test -timeout 60s ./internal/config` fails if any example contains secret-shaped placeholders listed in the action.
  </acceptance_criteria>
  <done>Operators can copy safe examples without turning on optional systems accidentally.</done>
</task>

<task type="auto">
  <name>Task 3: Document Qdrant and pgvector parity</name>
  <files>docs/scheduler-1.0-runbook.md, README.md, config.cache.example.json</files>
  <read_first>
    - docs/scheduler-1.0-runbook.md
    - README.md
    - config.cache.example.json
    - tests/integration/semantic_cache_test.go
    - tests/integration/plan4_postgres_smoke_test.go
    - scripts/smoke/plan4-postgres.sh
    - .planning/phases/22-documentation-env-example-uat/22-CONTEXT.md
  </read_first>
  <action>Add one concise Qdrant vs pgvector comparison table covering default deployment fit, config location, vector dimension, operational dependency, and smoke/UAT command. Include short examples for `cache.vector_store="qdrant"` with `cache.qdrant.addr` and `cache.vector_store="pgvector"` with the Plan 4 PostgreSQL DSN path. State that semantic-neighbor lookup remains gateway-owned for both backends and Scheduler receives only aggregate scalar features.</action>
  <verify>
    <automated>rg -n "Qdrant|pgvector|vector_store|scheduler receives only aggregate" docs/scheduler-1.0-runbook.md config.cache.example.json README.md</automated>
  </verify>
  <acceptance_criteria>
    - The runbook includes a table row for Qdrant.
    - The runbook includes a table row for pgvector.
    - The runbook names `cache.vector_store`, `cache.qdrant.addr`, and Plan 4 PostgreSQL DSN configuration.
    - The runbook states semantic-neighbor lookup is gateway-owned and Scheduler receives aggregate scalar features only.
  </acceptance_criteria>
  <done>Operators can choose Qdrant or pgvector without confusing Scheduler ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 4: Produce Phase 22 UAT report</name>
  <files>.planning/phases/22-documentation-env-example-uat/22-UAT.md</files>
  <read_first>
    - .planning/phases/22-documentation-env-example-uat/22-CONTEXT.md
    - .planning/phases/21-observability-admin-apis-tooling/21-UAT.md
    - internal/config/config_test.go
    - internal/http/handlers/admin_scheduler_test.go
    - internal/scheduler/semantic_neighbors_test.go
    - internal/scheduler/client_test.go
    - internal/scheduler/queue_fallback_test.go
    - internal/scheduler/quality_test.go
    - internal/scheduler/sla_promotion_test.go
    - tests/integration/semantic_cache_test.go
    - tests/integration/redis_hotstate_test.go
    - tests/integration/plan4_postgres_smoke_test.go
  </read_first>
  <action>Create `.planning/phases/22-documentation-env-example-uat/22-UAT.md` with checklist rows for scheduler enable/disable, disabled-by-default startup, Scheduler down fallback, Redis unavailable fallback, Qdrant unavailable fallback, ONNX predictor unhealthy fallback, semantic-neighbor Qdrant/pgvector behavior, admin status/SLA/export APIs, config/example parse-load, secret-safe examples, and real-provider checks using `.env.local`. Each row must include command, expected result, actual result, notes, and failure classification. Fill actual results from commands the executor runs; for gated external-service or real-provider checks not run locally, mark actual result `not run`, include required env vars, and classify non-blocking unless the command was required by local evidence.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config ./internal/http/handlers ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - `22-UAT.md` has columns or fields for command, expected result, actual result, notes, and failure classification.
    - UAT rows cover scheduler enable/disable, degradation, semantic neighbors, admin APIs, example safety, disabled-by-default startup, and real-provider checks.
    - Failed checks include command output or a note explaining where output is captured, root cause, and blocking/non-blocking status.
    - Routine real-provider checks reference `.env.local` and prefer non-Gemini provider resources unless the scenario is Gemini-specific.
    - Backend unit-test commands in `22-UAT.md` use `go test -timeout 60s`.
  </acceptance_criteria>
  <done>Phase 22 has auditable UAT evidence tied to existing automated and gated manual checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> operators | Operators may copy examples into local or production environments. |
| Example config -> runtime startup | Example files can accidentally enable optional subsystems or include unsafe placeholders. |
| UAT report -> release decision | UAT evidence informs whether v7.6 can close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Information Disclosure | docs and examples | mitigate | Use env-var placeholder names only; extend tests to reject secret-shaped example content. |
| T-22-01-02 | Tampering | copied config examples | mitigate | Keep Scheduler, Redis, and cache disabled by default and document explicit enablement toggles. |
| T-22-01-03 | Repudiation | UAT evidence | mitigate | Record command, expected result, actual result, notes, and failure classification per check. |
| T-22-01-04 | Denial of Service | operator enablement | mitigate | Document fail-open behavior and degradation playbooks for Scheduler, Redis, Qdrant, ONNX, and admin validation failures. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/config`
- `go test -timeout 60s ./internal/http/handlers ./internal/scheduler`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- README stays a quick path and links to the focused Scheduler 1.0 runbook.
- Examples show nested config shape without hardcoded credentials or enabled optional subsystems.
- Qdrant and pgvector semantic-neighbor deployment guidance is clear and bounded.
- Phase 22 UAT evidence covers roadmap paths and classifies any gaps.
</success_criteria>

## Artifacts this phase produces

- Documentation: `docs/scheduler-1.0-runbook.md`.
- Updated examples: `.env.example`, `config.json.example`, `config.scheduler.example.json`, `config.cache.example.json`, `config.heuristic.example.json`.
- UAT report: `.planning/phases/22-documentation-env-example-uat/22-UAT.md`.
- Secret-safety/example coverage in `internal/config/config_test.go`.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP Phase 22 | README, runbook, examples, Qdrant/pgvector parity, UAT | Tasks 1-4. |
| CONTEXT D-01..D-05 | Operator runbook and UAT guidance | Tasks 1 and 4. |
| CONTEXT D-06..D-09 | Curated env/config examples and vector parity | Tasks 2 and 3. |
| CONTEXT D-10..D-13 | UAT evidence and failure classification | Task 4. |
| CFG-03, CFG-04 | Structured examples and disabled defaults | Task 2. |
| SCH-08, OBS-03..OBS-06, QDR-06, QDR-08 | Docs/UAT for implemented v7.6 behavior | Tasks 1, 3, and 4. |

<output>
Create `.planning/phases/22-documentation-env-example-uat/22-01-SUMMARY.md` when done.
</output>
