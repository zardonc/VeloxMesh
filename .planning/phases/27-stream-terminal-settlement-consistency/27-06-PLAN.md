---
phase: 27-stream-terminal-settlement-consistency
plan: 06
type: execute
priority: P1
wave: 4
depends_on: [27-01, 27-02, 27-04]
files_modified:
  - internal/gateway/fusion_terminal_test.go
  - .planning/phases/27-stream-terminal-settlement-consistency/27-06-SUMMARY.md
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-07, TERM-08]
user_setup: []
estimated_duration: 0.5 engineer-day
must_haves:
  truths:
    - Fusion is verified to classify and finalize terminal outcomes through the shared contract.
    - Fusion normal/error/cancellation/duplicate settlement behavior matches ordinary stream terminal rules.
    - No Fusion aggregation, route selection, provider-member execution, or Judge decision behavior is changed in this package.
  artifacts:
    - path: internal/gateway/fusion_terminal_test.go
      provides: Fusion terminal consistency regression tests.
    - path: .planning/phases/27-stream-terminal-settlement-consistency/27-06-SUMMARY.md
      provides: Scope and result evidence that no prohibited Fusion behavior changed.
---

<objective>
Verify, and only verify, that Fusion uses the same terminal classification and exactly-once finalization rules as ordinary streams. This P1 package contains no production Fusion behavior edits.

Deliverable: focused Fusion terminal regressions plus a concise evidence note proving aggregation, routing, and Judge semantics were not changed.
</objective>

<execution_context>
**Preconditions:** `27-01` supplies classification; `27-02` installs the thin Fusion terminal adapter; `27-04` supplies outcome-gated settlement behavior. Existing Fusion normal, aggregation, route, and Judge tests are the non-regression baseline.

**Dependencies:** Requires `27-01`, `27-02`, and `27-04`. `27-05` consumes the resulting evidence. This plan intentionally does not depend on handler/SSE work because it tests gateway Fusion terminal semantics only.

**Duration:** 0.5 engineer-day. No real provider, Fusion configuration change, deployment, or benchmark against external models.

**Test environment:** Local gateway tests with existing Fusion fixtures plus fakes/spies for finalizer callbacks, repository settlement, provider health, breaker, metrics, trace, and admission release.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Establish a test-only Fusion terminal fixture boundary</name>
  <files>internal/gateway/fusion_terminal_test.go</files>
  <action>
Create a new focused Fusion terminal test file using existing test helpers where possible. Add test-only spies that record terminal kind, callback ordering/counts, settlement attempts, and final Usage; do not modify `fusion.go`, Fusion routes, provider member orchestration, or Judge code.

Make fixture inputs explicit: normal Fusion completion with final Usage, typed provider/Judge error, cancellation, policy rejection where already representable, and duplicate/late terminal candidate. Avoid sleep, random ordering, real provider credentials, and request content fixtures.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/gateway -run 'TestFusion.*Terminal'`. Load the Phase baseline SHA and confirm `git diff-index --name-only "$baseline_sha" --` lists only the permitted source/test paths and this work package's test/evidence files.
  </verify>
  <done>
Fusion terminal tests have isolated fakes and require no production Fusion code modification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify classification, exactly-once lifecycle, and settlement consistency</name>
  <files>internal/gateway/fusion_terminal_test.go</files>
  <action>
Assert that Fusion normal completion produces the shared `completed` outcome, runs health/breaker/metrics/trace/release exactly once, and settles only once with final Usage. Assert error/cancellation/policy outcomes use the same classification rules as ordinary streams, do not duplicate lifecycle calls, and never debit non-completed outcomes. Assert a duplicate terminal candidate cannot re-run release, health, or settlement.

For Fusion-specific error fixtures, compare the existing client mapping and health-affecting decision to the ordinary classification expectation. Do not add new failure handling or change how members aggregate, how routes are selected, or how the Judge evaluates a result.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/gateway -run 'TestFusion.*Terminal|TestFusion.*Settlement|TestFusion.*Duplicate'` followed by the existing Fusion suite selected by the repository's current test names.
  </verify>
  <done>
Focused tests prove Fusion has the same terminal contract and outcome-gated settlement as ordinary streams.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record prohibited-scope and regression evidence</name>
  <files>.planning/phases/27-stream-terminal-settlement-consistency/27-06-SUMMARY.md</files>
  <action>
Write an evidence note dated September 20, 2026 with commands, fixture categories, results, and an explicit declaration that this package did not change Fusion aggregation, route selection, member execution, or Judge behavior. List the existing normal Fusion tests rerun as compatibility checks and record any unresolved real-provider soak limitation.
  </action>
  <verify>
Review the summary against `git diff-index --stat "$baseline_sha" --` and rerun one normal and one duplicate-terminal Fusion test. Run `git diff-index --check "$baseline_sha" --`.
  </verify>
  <done>
The P1 result is independently reviewable and its limited verification-only scope is demonstrable from files and test evidence.
  </done>
</task>
</tasks>

<acceptance_criteria>
1. Fusion completed/error/cancelled/policy/duplicate terminal cases use the shared outcome contract in tests.
2. Fusion lifecycle callbacks execute exactly once; only completed final Usage settles once.
3. Non-completed Fusion outcomes never persist a debit, even with partial Usage.
4. Existing Fusion aggregation, routing, member execution, and Judge tests retain their prior expectations.
5. This work package modifies only Fusion terminal tests and its evidence summary.
</acceptance_criteria>

<test_matrix>
| ID | Environment and data | Steps | Expected result |
| --- | --- | --- | --- |
| N-08 | Existing normal Fusion fixture with final Usage | Complete Fusion request | Shared `completed`; all lifecycle counters one; one settlement. |
| E-08 | Typed Fusion provider/Judge failure fixture | Finalize error | Existing mapping/health policy; zero settlement; release once. |
| C-06 | Fusion request context cancelled | Cancel during terminal wait | Shared cancellation; no health penalty/debit; release once. |
| AUTH-06 | Existing representable policy-rejection fixture | Finalize rejection | Health-neutral; zero settlement; no sensitive output. |
| B-06 | Duplicate/late Fusion terminal candidate | Submit both candidates | First wins; no duplicate health/release/settlement. |
| COMP-06 | Existing aggregation, route, and Judge fixtures | Rerun unchanged tests | Previous response/decision expectations remain unchanged. |
| PF-06 | Repeated finite fake Fusion completions | Focused allocation/counter check | No new external I/O, retry, or unbounded callback growth. |
</test_matrix>

<handoff>
`27-05` incorporates these P1 results into the Phase 27 gate. Do not expand this package into Fusion feature work; any aggregation/routing/Judge change requires a separate planned phase.
</handoff>
