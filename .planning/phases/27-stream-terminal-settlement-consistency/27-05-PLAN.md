---
phase: 27-stream-terminal-settlement-consistency
plan: 05
type: execute
priority: P0
wave: 4
depends_on: [27-01, 27-02, 27-03, 27-04]
files_modified:
  - internal/gateway/stream_terminal_test.go
  - internal/gateway/service_stream_test.go
  - internal/gateway/service_settlement_test.go
  - internal/http/handlers/chat_stream_test.go
  - tests/integration/chat_test.go
  - tests/integration/chat_stream_test.go
  - .planning/phases/27-stream-terminal-settlement-consistency/27-05-SUMMARY.md
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-04, TERM-05, TERM-06, TERM-07, TERM-08]
user_setup: []
estimated_duration: 1.5 engineer-days
must_haves:
  truths:
    - The full terminal matrix is tested at unit, service, handler, and integration boundaries without duplicated tests.
    - Tests cover normal, provider error, cancellation, duplicate completion, persistence fault, policy/auth, compatibility, and bounded performance behavior.
    - The recorded verification result is reproducible and distinguishes no-match test selection from passing tests.
  artifacts:
    - path: tests/integration/chat_stream_test.go
      provides: Focused streaming integration terminal tests below the file-size limit.
    - path: .planning/phases/27-stream-terminal-settlement-consistency/27-05-SUMMARY.md
      provides: Executed command evidence, matrix results, and residual risks.
---

<objective>
Complete the Phase 27 behavior-driven regression matrix across unit, gateway service, HTTP handler, and integration boundaries. This is a test/verification package: it must not introduce new production behavior.

Deliverable: deterministic test matrix, split integration stream tests where required by file-size limits, a reproducible validation command set, and a short evidence summary for review/phase execution.
</objective>

<execution_context>
**Preconditions:** `27-01` through `27-04` implementation/tests pass. No production source change is permitted in this package except test-only helper wiring already accepted by repository conventions.

**Dependencies:** Requires the four P0 implementation work packages. It runs in parallel with P1 `27-06`; the P0 phase gate remains independently reviewable when P1 verification is deferred.

**Duration:** 1.5 engineer-days. No deployment or live-provider soak test is required for acceptance; real-provider soak remains a post-phase optional check.

**Test environment:** Local Go test environment. Integration tests use in-process fake providers/repositories, `httptest`, deterministic writers, and static fixtures. All backend commands use a 60-second timeout.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Split oversized integration tests and create a non-overlapping matrix</name>
  <files>tests/integration/chat_test.go, tests/integration/chat_stream_test.go, internal/gateway/stream_terminal_test.go, internal/gateway/service_stream_test.go, internal/gateway/service_settlement_test.go, internal/http/handlers/chat_stream_test.go</files>
  <action>
Before adding coverage, move stream-terminal integration tests out of oversized `tests/integration/chat_test.go` into `chat_stream_test.go`, preserving existing behavior. Keep every touched Go file at or below 500 lines. Build a compact test ownership map: terminal classifier/once guard in unit tests; lifecycle/release in service tests; writer/cancellation bytes in handler tests; end-to-end endpoint contract in integration tests. Do not duplicate the same assertion at every layer.

Name tests by outcome and boundary, use fakes instead of sleep/retry timing, and make a no-match selection failure visible by collecting `go test -json` output and checking for actual `pass` events for selected tests.
  </action>
  <verify>
Run focused package tests and `wc -l` for every touched test file. Confirm each matrix row below has one primary owning layer and no test depends on live provider/network state.
  </verify>
  <done>
Test files meet size limits and the matrix is traceable from requirement to one primary executable test location.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement P0 terminal, SSE, settlement, and security regression coverage</name>
  <files>internal/gateway/stream_terminal_test.go, internal/gateway/service_stream_test.go, internal/gateway/service_settlement_test.go, internal/http/handlers/chat_stream_test.go, tests/integration/chat_stream_test.go</files>
  <action>
Implement cases for normal completion/EOF, retryable and non-retryable provider errors, policy rejection, client cancellation, handler write failure, bare-close behavior, duplicate/late/concurrent completion, missing final Usage, settlement persistence error, Usage-log failure, aggregator failure, and admission release despite any persistence error.

At handler/integration scope assert OpenAI-compatible stream headers/event encoding continue to work for the successful route. At security/auth boundaries assert unauthorized or policy-rejected requests cannot create provider health penalties, client debit, or sensitive log/event data. Include a bounded repeated-stream/benchmark check for no unbounded goroutine count, per-chunk external I/O, or material allocation regression.
  </action>
  <verify>
Run focused commands with `-json` output captured to a temporary path and confirm selected tests emit a `pass` event. Then run `go test -timeout 60s ./internal/gateway ./internal/http/handlers ./tests/integration`.
  </verify>
  <done>
All P0 requirements have normal, exceptional, boundary, cancellation, security/permission, compatibility, and performance-oriented coverage at the appropriate test layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute the phase gate and record reproducible evidence</name>
  <files>.planning/phases/27-stream-terminal-settlement-consistency/27-05-SUMMARY.md</files>
  <action>
Run `gofmt` on changed Go files, `go vet` for affected packages if it is already part of the local toolchain, the focused matrix commands, and `go test -timeout 60s ./...`. Load the recorded baseline SHA from `27-PHASE-BASE.txt`; run `git diff-index --check "$baseline_sha" --` and compare `git diff-index --name-only "$baseline_sha" --` against the planned paths plus Phase 27 evidence. Do not stage, commit, deploy, or alter unrelated files.

Write the summary with exact commands, date, package outcomes, matrix IDs, test fixture type, passing assertion counts where available, and remaining risks. If a command cannot run, record the concrete blocker and do not mark the gate passed.
  </action>
  <verify>
Review `27-05-SUMMARY.md`, rerun one representative normal/error/cancel/write-failure command, and confirm `git diff-index --check "$baseline_sha" --` is clean.
  </verify>
  <done>
The Phase 27 gate has dated, repeatable evidence rather than an unverified statement of coverage.
  </done>
</task>
</tasks>

<acceptance_criteria>
1. TERM-01 through TERM-08 are each mapped to an executable, passing test.
2. Unit, service, handler, and integration tests each own distinct behavior assertions; no live provider is required.
3. Every terminal side effect is asserted once-only under duplicate/late/concurrent input.
4. Cancellation, writer failure, auth/policy rejection, and persistence failure do not debit clients or damage provider health.
5. Valid stream compatibility and bounded performance/no-new-I/O checks pass.
6. All commands complete within the 60-second backend timeout and an evidence summary records the results.
</acceptance_criteria>

<test_matrix>
| ID | Primary layer | Environment/data | Steps | Expected result |
| --- | --- | --- | --- | --- |
| N-07 | Integration | Fake provider clean EOF | POST streaming request | Compatible headers/chunks; one terminal and one `[DONE]`; settlement once with Usage. |
| E-06 | Service | Typed retryable/non-retryable provider failures | Finalize each | Existing mapping/health policy; one release; no duplicate terminal. |
| C-05 | Handler/service | Cancelled request and partial Usage | Cancel while forwarding | Upstream context cancelled; no post-cancel bytes/debit; release once. |
| E-07 | Handler | Fail-on-Nth-write writer | Fail after first chunk and on terminal write | No later writes or synthetic `[DONE]`; cancellation observed. |
| B-05 | Unit/service | Duplicate, late, concurrent candidates | Submit all candidates | One winning finalization; lifecycle/settlement counters remain one or zero as applicable. |
| OBS-05 | Service | Repository/aggregator fakes fail | Complete request | Sanitized warning/metric; response/release intact; no retry/double debit. |
| AUTH-05 | Handler/integration | Unauthorized and policy fixtures | Submit request | Existing denial compatibility; no health penalty/debit/sensitive output. |
| COMP-05 | Integration | Existing OpenAI stream fixture | Compare headers/data schema | Compatibility retained for valid stream. |
| PF-05 | Unit/handler | Repeated finite fake streams | Benchmark/allocation/goroutine check | No external per-chunk I/O, unbounded goroutines, or material regression. |
</test_matrix>

<handoff>
This plan is the P0 exit. On passing evidence, Phase 27 is ready for implementation review and explicit execution authorization; it is not a deployment authorization.
</handoff>
