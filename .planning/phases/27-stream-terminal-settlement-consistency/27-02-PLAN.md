---
phase: 27-stream-terminal-settlement-consistency
plan: 02
type: execute
priority: P0
wave: 2
depends_on: [27-01]
files_modified:
  - internal/gateway/service.go
  - internal/gateway/service_stream.go
  - internal/gateway/service_test.go
  - internal/gateway/service_stream_test.go
  - internal/gateway/fusion.go
  - internal/gateway/fusion_terminal.go
  - internal/gateway/fusion_terminal_test.go
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-06, TERM-08]
user_setup: []
estimated_duration: 1.5 engineer-days
must_haves:
  truths:
    - Ordinary and buffered streaming paths hand every terminal candidate to the shared one-shot finalizer.
    - Health, breaker, metrics, trace closure, and admission release execute once per request terminal outcome.
    - Fusion uses the same terminal finalizer only for lifecycle completion, without changing aggregation, routing, or Judge semantics.
  artifacts:
    - path: internal/gateway/service_stream.go
      provides: Ordinary and buffered stream lifecycle ownership below the file-size limit.
    - path: internal/gateway/fusion_terminal.go
      provides: Thin Fusion terminal adapter over the shared finalizer.
  key_links:
    - from: internal/gateway/service_stream.go
      to: internal/gateway/stream_terminal.go
      via: Terminal candidate classification and Submit call.
      pattern: 'Classify|Submit'
---

<objective>
Wire the shared terminal finalizer into ordinary, buffered, and Fusion request completion paths. Remove duplicate normal-stream cleanup only after the new path proves equivalent. Keep all touched production and test files under the repository's 500-line maximum by splitting first.

Deliverable: lifecycle code organized into focused files; ordinary/buffered/Fusion paths using the same finalizer contract; deterministic service-level tests for release, health, breaker, metric, and trace exactly-once behavior.
</objective>

<execution_context>
**Preconditions:** `27-01` classification/finalizer tests pass. The Phase baseline exists. Capture the current `wc -l` count for affected files before moving code.

**Dependencies:** Requires `27-01`. `27-03`, `27-04`, and `27-06` consume the real lifecycle seam; `27-05` validates the full matrix.

**Duration:** 1.5 engineer-days. No API change, data migration, or deployment.

**Test environment:** Local gateway service unit tests with fake provider streams, fake admission controller, health/breaker/metrics/trace spies, and deterministic channel closure. Do not use sleeps or networked providers.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Split oversized service implementation and tests before behavior changes</name>
  <files>internal/gateway/service.go, internal/gateway/service_stream.go, internal/gateway/service_test.go, internal/gateway/service_stream_test.go</files>
  <action>
Before altering lifecycle behavior, move existing stream-only functions and matching tests from `service.go` and `service_test.go` into `service_stream.go` and `service_stream_test.go`. Preserve package visibility, exported API, tests, and behavior byte-for-byte where possible. Keep each affected Go file at or below 500 lines, each function at or below 50 nonblank lines, maximum three nesting levels, and use options objects rather than new positional parameter sets.

Run the focused original tests immediately after the move. Do not mix semantic cleanup with this extraction; a failing extraction must be resolved before terminal-finalizer changes start.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/gateway`. Run `wc -l internal/gateway/service.go internal/gateway/service_stream.go internal/gateway/service_test.go internal/gateway/service_stream_test.go` and inspect that all touched files are no more than 500 lines.
  </verify>
  <done>
The stream lifecycle has a focused home, no pre-existing behavior changes, and all affected files satisfy the repository size constraint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route ordinary and buffered stream terminal candidates through the shared finalizer</name>
  <files>internal/gateway/service_stream.go, internal/gateway/service_stream_test.go</files>
  <action>
At every ordinary and buffered stream exit, construct one terminal candidate for provider `End`, clean EOF, provider error, policy rejection, request cancellation, and unexpected internal error, then call the `27-01` classifier/finalizer. Eliminate direct duplicate finish branches in the normal stream path only after the finalizer owns their equivalent effects.

Bind callbacks so only an accepted outcome performs provider health update, breaker record, metrics, trace closure, admission release, and terminal-event decision. Preserve all established routing, cache, retry, and provider-selection behavior. Do not perform new per-chunk I/O, lock waits, allocation-heavy logging, or retries.
  </action>
  <verify>
Use table-driven service tests with fakes and event counters. Run `go test -timeout 60s ./internal/gateway -run 'Test.*Stream.*Terminal|Test.*Admission|Test.*Breaker'`.
  </verify>
  <done>
For ordinary and buffered streams, each terminal condition produces one accepted finalization and one admission release; later EOF/error/channel-close paths cannot repeat side effects.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adapt Fusion lifecycle completion without altering Fusion computation</name>
  <files>internal/gateway/fusion.go, internal/gateway/fusion_terminal.go, internal/gateway/fusion_terminal_test.go</files>
  <action>
Extract only the terminal lifecycle hook currently duplicated around Fusion provider completion into `fusion_terminal.go`, and invoke the common terminal finalizer with the same classification contract as ordinary streams. Preserve member execution fan-out, aggregation, route/provider selection, Judge prompts and decision rules, and output composition exactly as they are. Do not add SSE handling here; handler behavior belongs to `27-03`.

Keep the adapter thin: it translates existing Fusion terminal facts into the shared outcome and supplies existing lifecycle dependencies. It must not expose a new Fusion public API or introduce cross-request mutable state.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/gateway -run 'Test.*Fusion.*Terminal|Test.*Fusion.*Provider'`. Compare the pre/post existing Fusion aggregation and Judge test expectations; only lifecycle counters/new terminal assertions may change.
  </verify>
  <done>
Fusion terminal lifecycle uses the shared finalizer while aggregation, routing, and Judge behavior remain unchanged. Detailed compatibility coverage is owned by `27-06`.
  </done>
</task>
</tasks>

<acceptance_criteria>
1. Ordinary, buffered, and Fusion paths reach one shared terminal finalizer contract.
2. Admission release, provider health, breaker, metrics, and trace close run exactly once for the accepted outcome.
3. No direct normal-stream cleanup remains that can duplicate a finalizer-owned side effect.
4. Fusion changes are restricted to a terminal lifecycle adapter; member aggregation, routing, and Judge behavior are unchanged.
5. Every touched Go implementation/test file is at most 500 lines and all focused tests complete within 60 seconds.
</acceptance_criteria>

<test_matrix>
| ID | Environment and data | Steps | Expected result |
| --- | --- | --- | --- |
| N-02 | Ordinary fake stream ending `End` | Consume stream to completion | Health success, breaker record, metrics, trace, release each equal one. |
| N-03 | Buffered fake stream ending clean EOF | Drain buffer | Same one-shot lifecycle result as ordinary flow. |
| E-02 | Provider error then channel close | Trigger error and close source | Error finalizes once; close cannot repeat lifecycle effects. |
| B-02 | Duplicate End/EOF signals | Feed both terminal candidates | First accepted; all counters stay one. |
| C-02 | Context cancelled while buffered result awaits forwarding | Cancel request | Cancellation finalizes once; release occurs once; no health penalty. |
| COMP-02 | Existing Fusion normal/error fixtures | Run unchanged aggregation/Judge fixtures with lifecycle spies | Response/routing/Judge expectations unchanged; only terminal lifecycle is shared. |
| PF-02 | Allocation-aware focused benchmark | Stream representative fake chunks | No new network/repository call and no material per-chunk regression. |
</test_matrix>

<handoff>
`27-03` may now rely on a single gateway terminal decision when handling SSE writes. `27-04` binds settlement through the shared callback. `27-06` adds Fusion-only compatibility tests and must not change `fusion.go`.
</handoff>
