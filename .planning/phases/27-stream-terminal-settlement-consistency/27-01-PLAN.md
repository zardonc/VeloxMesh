---
phase: 27-stream-terminal-settlement-consistency
plan: 01
type: execute
priority: P0
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-stream-terminal-settlement-consistency/27-PHASE-BASE.txt
  - internal/gateway/stream_terminal.go
  - internal/gateway/stream_terminal_test.go
  - internal/errors/errors.go
  - internal/errors/errors_test.go
autonomous: true
requirements: [TERM-01, TERM-02, TERM-03, TERM-08]
user_setup: []
estimated_duration: 1.0 engineer-day
must_haves:
  truths:
    - Every stream terminal input becomes one immutable classified outcome before lifecycle side effects run.
    - A terminal candidate accepted first wins; duplicate, late, or concurrent candidates do not repeat lifecycle effects.
    - Client cancellation and policy rejection are health-neutral, while provider failure preserves existing mapping and health policy.
  artifacts:
    - path: internal/gateway/stream_terminal.go
      provides: Internal terminal outcome model and one-shot finalizer contract.
    - path: internal/gateway/stream_terminal_test.go
      provides: Deterministic terminal classification and duplicate-completion tests.
  key_links:
    - from: internal/gateway/stream_terminal.go
      to: internal/errors/errors.go
      via: Existing error translation and health policy.
      pattern: 'Classify|TranslateError|AffectsProviderHealth'
---

<objective>
Create the internal terminal model that all later work packages call. This plan deliberately changes no stream handler, provider loop, Fusion aggregation, routing, Judge, or settlement behavior.

Deliverable: an immutable terminal outcome, a dependency-injected one-shot finalizer API, a saved Phase 27 baseline, and focused unit tests proving the classification contract before lifecycle wiring begins.
</objective>

<execution_context>
**Preconditions:** Go toolchain and existing gateway unit tests run locally. Record the repository baseline before source edits; do not revert or include unrelated user work.

**Dependencies:** None. Plans `27-02` through `27-06` consume this contract.

**Duration:** 1 engineer-day. No deployment, migration, external service, provider credential, or real provider call.

**Test environment:** Local Go unit tests with deterministic fake callbacks, synthetic `context.Canceled`, typed provider errors, and policy-rejection errors. No network calls or sleeps.
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Freeze baseline and define an immutable terminal vocabulary</name>
  <files>.planning/phases/27-stream-terminal-settlement-consistency/27-PHASE-BASE.txt, internal/gateway/stream_terminal.go</files>
  <action>
Record the execution baseline commit SHA and allowed source/test paths in `27-PHASE-BASE.txt` before editing source. Add a package-private terminal model in a new focused file, not in already oversized `service.go`.

Define only these kinds: `completed`, `provider_error`, `client_cancelled`, `policy_rejected`, and `internal_abnormal`. The outcome carries immutable terminal facts: kind, normalized client finish/error, final Usage snapshot when present, health-affecting classification, and diagnostic cause. It must not contain mutable counters, writers, repositories, or concrete provider dependencies.

Add a narrow classifier using `errors.As` and existing translation/health policy. Preserve mapped provider error categories, map request-context cancellation to `client_cancelled` with 499 semantics, and keep policy rejection health-neutral. Do not introduce a second public error taxonomy or alter existing client payload shapes.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/errors ./internal/gateway`. Confirm the model imports no handler, repository, provider, Fusion, or observability concrete implementation package.
  </verify>
  <done>
The baseline exists, every terminal input has one kind, and the new model has no lifecycle side effects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement a dependency-injected exactly-once finalizer boundary</name>
  <files>internal/gateway/stream_terminal.go, internal/gateway/stream_terminal_test.go</files>
  <action>
Add a small finalizer object/factory accepting callbacks through options or interfaces: admission release, provider health, circuit breaker, metrics, trace close, normalized client terminal emission, and future settlement. Do not construct concrete dependencies inside business logic.

Implement concurrency-safe `Submit(outcome) bool`. Only the winner invokes callbacks; all callbacks receive the immutable outcome. Name and test the callback ordering: classification, provider lifecycle accounting, metrics/trace, settlement decision, admission release, client terminal emission. This plan introduces no hot-path I/O and no state mutation outside private synchronization.
  </action>
  <verify>
Add table-driven fakes for first completion wins, provider error then completion, cancellation then provider error, two concurrent candidates, and callback outcome identity. Run `go test -timeout 60s ./internal/gateway -run 'TestTerminal'`.
  </verify>
  <done>
Duplicate candidates invoke no callback; every registered lifecycle callback runs exactly once for the winning candidate without `time.Sleep`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock error, security, and boundary classification behavior</name>
  <files>internal/errors/errors_test.go, internal/gateway/stream_terminal_test.go</files>
  <action>
Add behavior cases for clean EOF, explicit provider success, retryable/non-retryable provider errors, `context.Canceled`, policy rejection, and unknown internal error. Assert existing mapped status/category, `AffectsProviderHealth` treatment, and final Usage pass-through without persistence.

Use opaque synthetic failures. Assert diagnostics never enter normalized client events; fixtures contain no prompt, credential, authorization header, or provider secret.
  </action>
  <verify>
Run `go test -timeout 60s ./internal/errors ./internal/gateway -run 'Test.*Terminal|Test.*Translate'` and inspect the table for normal, exception, cancellation, policy, duplicate, and boundary coverage.
  </verify>
  <done>
Every terminal kind has executable coverage; cancellation/policy remain health-neutral; no external calls occur.
  </done>
</task>
</tasks>

<acceptance_criteria>
1. One internal terminal outcome type is the sole input to the new finalizer boundary.
2. `Submit` is exactly-once for sequential and concurrent duplicates.
3. Provider mapping stays compatible; cancellation maps to 499; policy rejection never damages health.
4. Final Usage can be represented without settling or debiting it.
5. `service.go` and `service_test.go` remain unchanged: their required split is the first task of `27-02`.
</acceptance_criteria>

<test_matrix>
| ID | Environment and data | Steps | Expected result |
| --- | --- | --- | --- |
| N-01 | Local unit test, clean EOF and final Usage | Classify then submit once | `completed`; callbacks once; Usage visible to callback. |
| E-01 | Typed provider error | Classify and submit | Existing category/status and health policy remain. |
| C-01 | `context.Canceled` | Submit cancellation then late provider error | Cancellation wins; no health penalty; late candidate has no effects. |
| B-01 | Two goroutines, atomic counters | Submit different candidates concurrently | One `true`; each callback counter is one. |
| SEC-01 | Policy rejection with synthetic message | Inspect normalized event | Health-neutral; diagnostic cause excluded. |
| PF-01 | Allocation-aware unit test | Repeatedly classify/submit fake outcome | No repository/network call or unbounded allocation/counter growth. |
</test_matrix>

<handoff>
`27-02` owns the required service-file split and real ordinary/buffered/Fusion lifecycle wiring. `27-03` owns cancellation/SSE. `27-04` owns settlement. Do not add production call sites before those plans.
</handoff>
