---
phase: 25-runbooks-and-verification
plan: "01"
type: execute
wave: 1
depends_on: ["23-01", "24-01"]
files_modified:
  - README.md
  - docs/scheduler-1.0-runbook.md
  - .env.example
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: ["DOC-01", "DOC-02"]
must_haves:
  truths:
    - "Scheduler docs describe default memory queueing, explicit node-scoped Redis queueing, and FallbackQueue recovery behavior."
    - "Deployment docs describe Plan 1 and Plan 3 queue/vector choices, known limits, failure handling, and verification results."
    - "v7.7 closeout records test and build evidence."
  artifacts:
    - path: "docs/scheduler-1.0-runbook.md"
      provides: "scheduler queue, vector, degradation, and verification runbook"
    - path: "README.md"
      provides: "deployment tier summary and quick operator pointers"
---

<objective>
Retroactively record the shipped Phase 25 plan for runbooks and verification.

Purpose: document v7.7 queue/vector behavior and record closeout verification for completed Phases 23-25.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update Scheduler queue and recovery docs</name>
  <files>docs/scheduler-1.0-runbook.md, README.md</files>
  <read_first>
    - docs/scheduler-1.0-runbook.md
    - README.md
    - internal/app/app.go
    - internal/scheduler/queue_fallback.go
  </read_first>
  <action>Document that Scheduler queueing defaults to memory, Redis queueing requires explicit configuration, Redis queues are node-scoped, and FallbackQueue reads memory fallback tasks after primary recovery.</action>
  <verify>
    <automated>rg -n "SCHEDULER_QUEUE_BACKEND|defaults to memory|FallbackQueue|Redis recovers" docs/scheduler-1.0-runbook.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - Runbook states default Scheduler queueing is memory.
    - Runbook states Redis queueing is explicit.
    - Runbook states FallbackQueue recovery reads memory fallback entries.
  </acceptance_criteria>
  <done>Operators can understand queue defaults and recovery behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update Plan 1 and Plan 3 deployment docs</name>
  <files>README.md, docs/scheduler-1.0-runbook.md, .env.example</files>
  <read_first>
    - README.md
    - docs/scheduler-1.0-runbook.md
    - .env.example
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Document Plan 1 as App + SQLite + Redis Stack + Qdrant, and Plan 3 as single-node App + SQLite + LanceDB by default or Qdrant when explicitly configured. State known limits: in-memory queue pending work is not durable across node crash, LanceDB runtime validation is deferred, and LanceDB/Qdrant data migration is out of scope.</action>
  <verify>
    <automated>rg -n "Plan 1|Plan 3|LanceDB|Qdrant|single-node|migration" README.md docs/scheduler-1.0-runbook.md .env.example</automated>
  </verify>
  <acceptance_criteria>
    - README lists Plan 1 and Plan 3 deployment tiers.
    - Docs state Plan 3 is single-node.
    - Docs state LanceDB/Qdrant migration is out of scope.
  </acceptance_criteria>
  <done>Deployment docs reflect v7.7 queue/vector choices.</done>
</task>

<task type="auto">
  <name>Task 3: Record closeout state and verification</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>Mark SCHQ-01..04, PLAN3-01..04, and DOC-01..02 complete. Mark active v7.7 phases 23-25 complete. Record that `go test -timeout 60s ./...` and `go build ./...` passed during closeout.</action>
  <verify>
    <automated>go test -timeout 60s ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` marks all v7.7 requirements complete.
    - `.planning/ROADMAP.md` marks phases 23, 24, and 25 complete.
    - `.planning/STATE.md` records v7.7 implementation verification.
  </acceptance_criteria>
  <done>Planning state matches the completed build.</done>
</task>

</tasks>

<verification>
Run:
- `rg -n "SCHEDULER_QUEUE_BACKEND|FallbackQueue|Plan 3|LanceDB|Qdrant" README.md docs/scheduler-1.0-runbook.md .env.example`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- DOC-01 and DOC-02 are implemented.
- Docs cover queue defaults, Redis node scoping, fallback recovery, Plan 1/Plan 3 choices, known limits, and verification.
- Planning state records Phases 23-25 as complete.
</success_criteria>

## Artifacts this phase produces

- v7.7 requirements completion records
- v7.7 roadmap completion records
- v7.7 state verification record

