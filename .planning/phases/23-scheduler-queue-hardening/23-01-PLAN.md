---
phase: 23-scheduler-queue-hardening
plan: "01"
type: execute
wave: 1
depends_on: ["20-01", "20-02", "21-01"]
files_modified:
  - internal/app/app.go
  - internal/app/app_test.go
  - internal/scheduler/queue_fallback.go
  - internal/scheduler/queue_fallback_test.go
  - internal/scheduler/queue_redis_test.go
  - docs/scheduler-1.0-runbook.md
autonomous: true
requirements: ["SCHQ-01", "SCHQ-02", "SCHQ-03", "SCHQ-04"]
must_haves:
  truths:
    - "Scheduler queue_backend unset, auto, or memory uses the in-memory queue."
    - "Redis scheduler queueing is opt-in and scoped to the current gateway node."
    - "FallbackQueue must keep memory fallback tasks readable after Redis recovery."
    - "Queue tests cover Redis normal, Redis failure, recovery reads, memory default, node-scoped keys, and fallback task retention."
  artifacts:
    - path: "internal/app/app.go"
      provides: "scheduler queue backend selection and node-scoped Redis queue naming"
    - path: "internal/scheduler/queue_fallback.go"
      provides: "merged primary/fallback queue reads after primary recovery"
    - path: "internal/scheduler/queue_fallback_test.go"
      provides: "fallback recovery and merge-order regression coverage"
---

<objective>
Retroactively record the shipped Phase 23 plan for Scheduler queue hardening.

Purpose: make memory the default Scheduler queue, keep Redis queueing explicit and node-scoped, and prevent fallback tasks from becoming unreadable after Redis recovery.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Default Scheduler queueing to memory</name>
  <files>internal/app/app.go, internal/app/app_test.go</files>
  <read_first>
    - internal/app/app.go
    - internal/app/app_test.go
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Update scheduler queue selection so `queue_backend` unset, `auto`, or `memory` returns `scheduler.NewMemoryQueue()` with backend label `memory`. Keep Redis only for explicit `queue_backend=redis`, and name the Redis queue from the configured gateway node ID, falling back to `local`.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/app</automated>
  </verify>
  <acceptance_criteria>
    - `newSchedulerQueue` returns backend `memory` for `Scheduler.QueueBackend == "auto"`.
    - `newSchedulerQueue` returns backend `redis` only for explicit `Scheduler.QueueBackend == "redis"` with usable Redis config.
    - `schedulerRedisQueueName` returns the gateway node ID and falls back to `local`.
  </acceptance_criteria>
  <done>Scheduler queue defaults are local and Redis queueing is explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve fallback queue reads after Redis recovery</name>
  <files>internal/scheduler/queue_fallback.go, internal/scheduler/queue_fallback_test.go</files>
  <read_first>
    - internal/scheduler/queue_fallback.go
    - internal/scheduler/queue_memory.go
    - internal/scheduler/queue_redis.go
  </read_first>
  <action>Make `FallbackQueue.PeekMin`, `PopMin`, and `Len` read both primary and memory fallback queues when primary is available. Remove fallback duplicates after successful primary writes or removes. Preserve score ordering when primary and fallback both contain items.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler -run TestFallbackQueue</automated>
  </verify>
  <acceptance_criteria>
    - `TestFallbackQueuePopMinReadsMemoryWhenPrimaryEmptyAfterRecovery` passes.
    - `TestFallbackQueuePopMinMergesPrimaryAndFallback` passes.
    - `TestFallbackQueuePrimaryRecoveryPushRemovesFallbackDuplicate` passes.
  </acceptance_criteria>
  <done>Fallback tasks remain readable after Redis recovery.</done>
</task>

<task type="auto">
  <name>Task 3: Cover Redis queue behavior and docs</name>
  <files>internal/scheduler/queue_redis_test.go, docs/scheduler-1.0-runbook.md</files>
  <read_first>
    - internal/scheduler/queue_redis.go
    - internal/scheduler/queue_redis_test.go
    - docs/scheduler-1.0-runbook.md
  </read_first>
  <action>Add Redis queue coverage for ZSET ordering, task-ID-only members, peek-without-pop, score replacement, lock TTL, locked-task skip, Redis-unavailable fallback, and runtime Redis failure. Document default memory queueing, explicit Redis queueing, and fallback recovery behavior.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler</automated>
  </verify>
  <acceptance_criteria>
    - `TestRedisQueueRealZSetOperations` passes with real Redis available.
    - `TestRedisQueueStoresOnlyTaskIDMember` passes with real Redis available.
    - `docs/scheduler-1.0-runbook.md` states default queueing is memory.
    - `docs/scheduler-1.0-runbook.md` states Redis queueing is explicit.
  </acceptance_criteria>
  <done>Queue behavior is covered by tests and operator docs.</done>
</task>

</tasks>

<verification>
Run:
- `go test -timeout 60s ./internal/app`
- `go test -timeout 60s ./internal/scheduler`
- `go test -timeout 60s ./...`
- `go build ./...`
</verification>

<success_criteria>
- SCHQ-01 through SCHQ-04 are implemented and tested.
- Scheduler queueing defaults to memory.
- Redis scheduler queueing is explicit and node-scoped.
- FallbackQueue reads memory fallback tasks after primary recovery.
</success_criteria>

## Artifacts this phase produces

- `schedulerRedisQueueName`
- `FallbackQueue.MarkPrimaryAvailable`
- `mergeQueueItems`
- `TestNewSchedulerQueueDefaultsToMemoryWhenRedisIsEnabled`
- `TestNewSchedulerQueueExplicitRedisIsNodeScoped`
- `TestFallbackQueuePopMinReadsMemoryWhenPrimaryEmptyAfterRecovery`
- `TestFallbackQueuePopMinMergesPrimaryAndFallback`

