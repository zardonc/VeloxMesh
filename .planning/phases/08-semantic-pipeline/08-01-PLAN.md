---
phase: "08-semantic-pipeline"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "internal/pipeline/config.go"
  - "internal/pipeline/config_test.go"
  - "internal/config/config.go"
  - "internal/config/config_test.go"
  - "internal/controlstate/semantic_rules.go"
  - "internal/controlstate/sqlite/semantic_rules.go"
  - "internal/controlstate/sqlite/semantic_rules_test.go"
  - "internal/controlstate/migrations/sqlite/0003_semantic_rules.sql"
autonomous: true
requirements:
  - "Phase 8: Semantic Pipeline"
must_haves:
  truths:
    - "All semantic rules exist and default to disabled per D-01 and D-02"
    - "A user rule config overrides only the global default for that user per D-03 and D-04"
    - "Caveman and Ponytail cannot both be enabled for the same resolved config per D-09"
    - "Caveman/Ponytail request text rewriting remains off unless explicitly enabled per D-10 and D-11"
  artifacts:
    - path: "internal/pipeline/config.go"
      provides: "Semantic rule names, default config, YAML loading, validation, and user-over-global resolution"
    - path: "internal/controlstate/semantic_rules.go"
      provides: "Narrow semantic rule store contract without widening the main Repository interface"
    - path: "internal/controlstate/migrations/sqlite/0003_semantic_rules.sql"
      provides: "SQLite tables for global defaults and per-user rule configs"
  key_links:
    - from: "internal/controlstate/sqlite/semantic_rules.go"
      to: "internal/pipeline/config.go"
      via: "store maps rows to validated semantic rule config"
      pattern: "ValidateSemantic"
---

<objective>
Persist the semantic pipeline rule configuration needed by Phase 8.

Purpose: Phase 8 needs a small backend-owned config contract before the executor and admin API can safely resolve rules per user.
Output: Semantic rule config types, all-disabled defaults, validation, YAML file loading, and a SQLite-backed per-user rule store.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-semantic-pipeline/08-CONTEXT.md
@.planning/phases/07-adapter-interfaces-sqlite-foundation/07-CONTEXT.md
@internal/pipeline/pipeline.go
@internal/config/config.go
@internal/controlstate/repository.go
@internal/controlstate/sqlite/repository.go
@internal/controlstate/migrations/sqlite/0001_control_state.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the semantic rule config contract</name>
  <files>internal/pipeline/config.go, internal/pipeline/config_test.go, internal/config/config.go, internal/config/config_test.go</files>
  <behavior>
    - Test 1: default semantic config contains rtk, headroom, pii, rewrite, caveman, ponytail, and filter with Enabled=false.
    - Test 2: user config overrides global defaults and no provider, model, route, or API-key layer is represented.
    - Test 3: validation rejects enabled Caveman and Ponytail together for one resolved config.
    - Test 4: Caveman/Ponytail request text rewrite is false unless option rewrite_request_text is explicitly true.
  </behavior>
  <action>Add semantic rule config types in `internal/pipeline/config.go`: `RuleName` constants for RTK, Headroom, PII, Rewrite, Caveman, Ponytail, and Filter per D-01; `RuleConfig` with `Enabled bool` and validated `Options`; and a resolved config type that contains only global defaults plus per-user overrides per D-03 and D-04. `DefaultSemanticPipelineConfig()` must return all seven rules present and disabled per D-02. `ResolveSemanticRuleConfig(global, user)` must implement only user config greater than global default precedence per D-04. Add validation that rejects unknown rule names, simultaneous enabled Caveman and Ponytail per D-09, and request text rewrite for Caveman/Ponytail unless `rewrite_request_text: true` is explicitly set per D-10 and D-11. Add `LoadSemanticPipelineConfigFile(path)` using the existing `go.yaml.in/yaml/v4` module already present in `go.mod`; an empty path returns all-disabled defaults. Extend `config.Config` with `SemanticPipelineConfigFile`, load it from `SEMANTIC_PIPELINE_CONFIG_FILE` and `semantic_pipeline_config_file`, and do not add committed config files or secret-bearing config content.</action>
  <verify>
    <automated>go test ./internal/config ./internal/pipeline -run "TestSemanticRuleConfig|TestLoadConfig" -timeout 60s</automated>
  </verify>
  <done>Rule config defaults, validation, YAML loading, and config file path loading are covered by tests and expose no extra precedence layer beyond D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store global and per-user semantic rule config in SQLite</name>
  <files>internal/controlstate/semantic_rules.go, internal/controlstate/sqlite/semantic_rules.go, internal/controlstate/sqlite/semantic_rules_test.go, internal/controlstate/migrations/sqlite/0003_semantic_rules.sql</files>
  <behavior>
    - Test 1: an empty store resolves every rule disabled.
    - Test 2: global defaults apply when no user config exists.
    - Test 3: user A overrides do not affect user B.
    - Test 4: persisted configs reject provider/model/route/API-key dimensions.
  </behavior>
  <action>Create a narrow `controlstate.SemanticRuleStore` contract and a SQLite implementation without adding a method to the main `controlstate.Repository` interface. Add `SemanticRules()` only on the concrete SQLite repository so Phase 8 avoids PostgreSQL extension work. Add migration `0003_semantic_rules.sql` with `semantic_rule_global_defaults(rule, enabled, options_json, updated_at)` and `semantic_rule_user_configs(user_id, rule, enabled, options_json, updated_at)`; do not add provider, model, route, or API-key columns per D-04. Store one row per rule and user, validate every save with the config contract from Task 1, and resolve missing rows to all-disabled defaults per D-02. Tests must prove per-user isolation per D-03, user-over-global precedence per D-04, Caveman/Ponytail exclusivity per D-09, and request rewrite opt-in default-off behavior per D-10 and D-11.</action>
  <verify>
    <automated>go test ./internal/controlstate/sqlite -run "TestSemanticRule" -timeout 60s</automated>
  </verify>
  <done>SQLite can persist and resolve global defaults plus per-user overrides, and invalid rule combinations never save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> process | Local YAML content becomes runtime rule config. |
| admin/storage -> SQLite | Backend rule config is persisted and later affects request/response processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | semantic rule config loading | mitigate | Validate rule names, options, and Caveman/Ponytail exclusivity before accepting YAML or stored config. |
| T-08-01-02 | Elevation | rule precedence | mitigate | Model only global defaults and user overrides; no provider/model/route/API-key override layer exists per D-04. |
| T-08-01-03 | Information Disclosure | config persistence | mitigate | Persist only rule names, booleans, and options JSON; no prompts, API keys, provider secrets, or raw request content. |
| T-08-01-SC | Tampering | package dependencies | accept | No new package-manager install; reuse `go.yaml.in/yaml/v4` already present in `go.mod`. |
</threat_model>

<verification>
Run the narrow tests first, then the touched packages:

<automated>go test ./internal/config ./internal/pipeline ./internal/controlstate/sqlite -timeout 60s</automated>
</verification>

<success_criteria>
- D-01 through D-04 and D-09 through D-11 are represented in tested config and storage behavior.
- Every rule defaults disabled.
- Per-user config cannot bleed across users.
- No provider/model/route/API-key override shape exists in code or schema.
</success_criteria>

<output>
Create `.planning/phases/08-semantic-pipeline/08-01-SUMMARY.md` when done.
</output>
