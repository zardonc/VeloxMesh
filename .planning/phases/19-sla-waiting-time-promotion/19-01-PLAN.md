---
phase: 19-sla-waiting-time-promotion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_validation.go
  - internal/config/config_test.go
autonomous: true
requirements: ["SLA-01"]
must_haves:
  truths:
    - "D-01 SLA rules are keyed by trusted tenant, model_class, and request_kind values."
    - "D-02 no matching SLA rule means no SLA promotion."
    - "D-03 promotion is disabled by default; invalid rules fail validation only when promotion is enabled."
    - "D-13 config examples and tests must not hardcode prompts, API keys, auth headers, provider payloads, embeddings, semantic-cache payloads, or raw task text."
  artifacts:
    - path: "internal/config/config.go"
      provides: "disabled-by-default scheduler SLA promotion config and JSON/env loading"
    - path: "internal/config/config_validation.go"
      provides: "promotion rule validation when scheduler.sla_promotion_enabled is true"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/config/config_validation.go"
      via: "SchedulerConfig SLA fields are defaulted before validation consumes them"
---

<objective>
Add the disabled-by-default scheduler SLA promotion configuration surface.

Purpose: SLA-01 and D-01 through D-03 require operator-configured waiting thresholds keyed by trusted tenant, model class, and request kind, while keeping promotion harmless unless explicitly enabled.
Output: Config fields, JSON/env loading, defaults, and validation tests. This plan does not reorder queue entries yet.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
@.planning/phases/14-scheduler-queue-foundation/14-CONTEXT.md
@internal/config/config.go
@internal/config/config_validation.go
@internal/config/config_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduler SLA promotion config fields and defaults</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go
    - internal/config/config_test.go
    - .planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
  </read_first>
  <behavior>
    - Scheduler SLA promotion is disabled by default.
    - Default candidate window is a named constant, not a magic number.
    - JSON config supports scheduler.sla_promotion_enabled, scheduler.sla_promotion_candidate_window, and scheduler.sla_promotion_rules.
    - Env config supports SCHEDULER_SLA_PROMOTION_ENABLED and SCHEDULER_SLA_PROMOTION_CANDIDATE_WINDOW.
  </behavior>
  <action>Add SchedulerConfig fields `SLAPromotionEnabled bool`, `SLAPromotionCandidateWindow int`, and `SLAPromotionRules []SLAPromotionRule`. Add SLAPromotionRule fields `PolicyID`, `TenantID`, `TenantClass`, `ModelClass`, `RequestKind`, and `WaitThreshold` with JSON keys `policy_id`, `tenant_id`, `tenant_class`, `model_class`, `request_kind`, and `wait_threshold`. Load the boolean and window from env, load all fields from JSON config, merge JSON rules by replacing the default empty slice when the source slice is non-empty, and default the window through a named constant `defaultSLAPromotionCandidateWindow` set to 32.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - internal/config/config_test.go asserts SchedulerConfig defaults include SLAPromotionEnabled=false, SLAPromotionCandidateWindow=32, and zero SLA rules.
    - A JSON override test asserts one rule with policy_id, tenant_id, model_class, request_kind, and wait_threshold is loaded.
    - An env override test asserts SCHEDULER_SLA_PROMOTION_ENABLED=true and SCHEDULER_SLA_PROMOTION_CANDIDATE_WINDOW=8 are loaded.
    - No source-controlled config or test fixture contains API keys, authorization headers, provider payloads, prompts, embeddings, semantic-cache payloads, or raw task text.
  </acceptance_criteria>
  <done>Operators can configure SLA promotion without enabling runtime behavior by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate enabled SLA promotion rules</name>
  <files>internal/config/config_validation.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config_validation.go
    - internal/config/config_test.go
    - internal/scheduler/types.go
    - .planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
  </read_first>
  <behavior>
    - Disabled promotion ignores malformed rules so dormant config remains harmless.
    - Enabled promotion rejects non-positive candidate windows.
    - Enabled promotion rejects rules without a policy ID, without tenant_id or tenant_class, without model_class, without request_kind, or with a non-positive wait_threshold.
    - Enabled promotion rejects request_kind values outside the existing scheduler RequestKind enum names.
  </behavior>
  <action>Extend validateSchedulerConfig with SLA promotion validation. When `s.SLAPromotionEnabled` is false, return without validating `SLAPromotionRules`. When enabled, require `SLAPromotionCandidateWindow >= 1`; each rule must have non-empty `PolicyID`, either `TenantID` or `TenantClass`, non-empty `ModelClass`, a request_kind in the current scheduler enum set (`simple_qa`, `code_gen`, `code_review`, `summarization`, `translation`, `structured_output`, `multi_step`, `tool_call`, `rag`, `creative`), and `WaitThreshold` parseable as a duration greater than zero.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/config</automated>
  </verify>
  <acceptance_criteria>
    - TestSchedulerConfigValidation has cases for enabled invalid window, missing policy_id, missing tenant identity/class, missing model_class, invalid request_kind, invalid wait_threshold, and non-positive wait_threshold.
    - A test proves the same malformed rule is accepted when SLAPromotionEnabled=false.
    - Existing scheduler validation cases for timeout, priority, queue backend, ONNX, quality thresholds, and semantic-neighbor settings still pass.
  </acceptance_criteria>
  <done>Invalid SLA rules fail fast only when promotion is actually enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator config -> gateway runtime | SLA rule fields enter SchedulerConfig and must be validated before use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | internal/config/config_validation.go | mitigate | Validate enabled rules for policy ID, trusted tenant selector, model_class, request_kind enum, positive threshold, and bounded window before runtime wiring consumes them. |
| T-19-01-02 | Information Disclosure | internal/config/config.go | mitigate | Keep rule keys to tenant ID/class, model_class, request_kind, threshold, and policy ID; do not add prompt, payload, API-key, auth-header, embedding, semantic-cache, or raw task fields. |
| T-19-01-03 | Denial of Service | SchedulerConfig | mitigate | Keep promotion disabled by default and cap scanning through the candidate window field used by later plans. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/config`
- `go build ./...`
</verification>

<success_criteria>
- SLA-01 is satisfied at the config layer: operators can configure tenant/model/request-kind waiting thresholds.
- Promotion remains disabled by default.
- Enabled invalid rules fail validation; disabled invalid rules remain harmless.
</success_criteria>

## Artifacts this phase produces

- Config fields: SchedulerConfig.SLAPromotionEnabled, SchedulerConfig.SLAPromotionCandidateWindow, SchedulerConfig.SLAPromotionRules.
- Config type: SLAPromotionRule.
- Env keys: SCHEDULER_SLA_PROMOTION_ENABLED, SCHEDULER_SLA_PROMOTION_CANDIDATE_WINDOW.
- JSON keys: scheduler.sla_promotion_enabled, scheduler.sla_promotion_candidate_window, scheduler.sla_promotion_rules, policy_id, tenant_id, tenant_class, model_class, request_kind, wait_threshold.
- Constant: defaultSLAPromotionCandidateWindow.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP success 1 | Operators configure tenant/model/request-kind waiting thresholds disabled by default | Covered by Tasks 1 and 2. |
| SLA-01 | Waiting threshold config disabled by default | Covered by Tasks 1 and 2. |
| CONTEXT D-01 | Rules keyed by tenant + model_class + request_kind | Covered by Task 1 fields and Task 2 validation. |
| CONTEXT D-02 | No matching rule means no promotion | Config supports empty/non-matching rules; runtime match behavior is completed by 19-02. |
| CONTEXT D-03 | Invalid rules fail validation when promotion is enabled; disabled harmless | Covered by Task 2. |

<output>
Create `.planning/phases/19-sla-waiting-time-promotion/19-01-SUMMARY.md` when done.
</output>
