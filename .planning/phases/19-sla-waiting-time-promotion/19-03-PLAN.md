---
phase: 19-sla-waiting-time-promotion
plan: "03"
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - internal/observability/metrics.go
  - internal/observability/prometheus.go
  - internal/observability/prometheus_test.go
  - internal/scheduler/sla_promotion.go
  - internal/scheduler/sla_promotion_test.go
  - internal/app/app.go
  - internal/app/app_test.go
autonomous: true
requirements: ["SLA-04", "SLA-02", "SLA-03"]
must_haves:
  truths:
    - "D-11 durable audit is written only for promoted and blocked_by_priority_or_quota outcomes."
    - "D-12 evidence fields are limited to policy ID, tenant ID/class, model class, request kind, priority, and outcome."
    - "D-13 prompts, API keys, auth headers, provider payloads, embeddings, semantic-cache payloads, and raw task text are excluded from metrics, audit, logs, and policy."
    - "D-14 metrics distinguish promoted, not_eligible, blocked_by_priority_or_quota, disabled, and error."
  artifacts:
    - path: "internal/observability/metrics.go"
      provides: "SLA promotion metrics interface and stub implementation"
    - path: "internal/observability/prometheus.go"
      provides: "sanitized Prometheus counter for SLA promotion outcomes"
    - path: "internal/scheduler/sla_promotion.go"
      provides: "promotion metrics and sanitized durable audit emission"
  key_links:
    - from: "internal/scheduler/sla_promotion.go"
      to: "internal/observability/metrics.go"
      via: "SLAPromoter records one aggregated outcome per pre-pop promotion attempt"
    - from: "internal/scheduler/sla_promotion.go"
      to: "internal/controlstate/audit.go"
      via: "promoted and blocked outcomes use SafeAuditMetadata before durable audit persistence"
---

<objective>
Add sanitized SLA promotion metrics and durable audit evidence.

Purpose: SLA-04 and D-11 through D-14 require operator evidence for promotion behavior without storing raw prompts, secrets, provider payloads, embeddings, semantic-cache payloads, or raw task text.
Output: Metrics interface/prometheus additions, promotion audit/log emission, and tests for sanitized outcome evidence.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/VeloxMesh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
@.planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
@internal/observability/metrics.go
@internal/observability/prometheus.go
@internal/observability/prometheus_test.go
@internal/scheduler/sla_promotion.go
@internal/scheduler/admin_scheduler_service.go
@internal/controlstate/audit.go
@internal/controlstate/repository.go
@internal/app/app.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitized SLA promotion metrics</name>
  <files>internal/observability/metrics.go, internal/observability/prometheus.go, internal/observability/prometheus_test.go</files>
  <read_first>
    - internal/observability/metrics.go
    - internal/observability/prometheus.go
    - internal/observability/prometheus_test.go
    - .planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
    - .planning/phases/16-a-b-rollout-and-prediction-quality/16-CONTEXT.md
  </read_first>
  <behavior>
    - Metrics distinguish outcomes promoted, not_eligible, blocked_by_priority_or_quota, disabled, and error.
    - Prometheus labels are sanitized and bounded.
    - Metrics include policy ID, tenant class, model class, request kind, priority, and outcome.
    - Metrics do not include tenant ID, task ID, prompts, API keys, auth headers, provider payloads, embeddings, semantic-cache payloads, or raw task text.
  </behavior>
  <action>Add `IncSchedulerSLAPromotion(policyID string, tenantClass string, modelClass string, requestKind string, priority string, outcome string)` to observability.Metrics and StubMetrics. Add a Prometheus CounterVec named `gateway_scheduler_sla_promotion_total` with labels `policy`, `tenant_class`, `model_class`, `request_kind`, `priority`, and `outcome`. Add safe label helpers for policy, tenant_class, model_class, request_kind, priority, and outcome; allow outcomes exactly `promoted`, `not_eligible`, `blocked_by_priority_or_quota`, `disabled`, and `error`, with unknown values mapped to `error` or `unknown` as appropriate.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/observability</automated>
  </verify>
  <acceptance_criteria>
    - internal/observability/prometheus_test.go asserts the counter exports all five allowed outcome labels.
    - A test proves an unsafe tenant class, policy ID, model class, request kind, or priority value is normalized to bounded labels.
    - No new metric label contains tenant ID, task ID, prompt, message, api_key, authorization, secret, provider payload, embedding, semantic-cache payload, or raw task text.
  </acceptance_criteria>
  <done>Operators can aggregate SLA promotion outcomes through sanitized metrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit sanitized audit and logs only for meaningful outcomes</name>
  <files>internal/scheduler/sla_promotion.go, internal/scheduler/sla_promotion_test.go, internal/app/app.go, internal/app/app_test.go</files>
  <read_first>
    - internal/scheduler/sla_promotion.go
    - internal/scheduler/admin_scheduler_service.go
    - internal/controlstate/audit.go
    - internal/controlstate/repository.go
    - internal/scheduler/sla_promotion_test.go
    - internal/app/app.go
    - internal/app/app_test.go
    - .planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
  </read_first>
  <behavior>
    - Promoted outcomes write one durable audit event and one sanitized info log.
    - blocked_by_priority_or_quota outcomes write one durable audit event and one sanitized warn log.
    - disabled and not_eligible outcomes do not write durable audit events or logs.
    - error outcomes write one sanitized warn log and no durable audit event.
    - Audit and log fields are limited to policy_id, tenant_id, tenant_class, model_class, request_kind, priority, and outcome.
    - Audit metadata is passed through controlstate.SafeAuditMetadata.
  </behavior>
  <action>Extend SLAPromoter with optional `Audit controlstate.AuditRepository` and `Logger *slog.Logger`. When outcome is promoted or blocked_by_priority_or_quota and Audit is non-nil, write an AuditEvent with Action `scheduler.sla_promotion`, TargetID set to the policy ID, Outcome set to the outcome, Actor `system`, and Metadata passed through controlstate.SafeAuditMetadata with exactly policy_id, tenant_id, tenant_class, model_class, request_kind, priority, and outcome. Emit one structured log for promoted, blocked_by_priority_or_quota, and error outcomes with the same allowed fields only. Update app wiring to pass repo.Audit() when durable control state exists and the existing app logger when present.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler ./internal/app</automated>
  </verify>
  <acceptance_criteria>
    - internal/scheduler/sla_promotion_test.go asserts promoted writes one audit event with Action `scheduler.sla_promotion` and TargetID equal to the policy ID.
    - A test asserts blocked_by_priority_or_quota writes one audit event.
    - A test asserts disabled, not_eligible, and error outcomes write zero audit events.
    - A test asserts promoted, blocked_by_priority_or_quota, and error logs contain only policy_id, tenant_id, tenant_class, model_class, request_kind, priority, and outcome fields.
    - A test asserts audit metadata has no keys other than policy_id, tenant_id, tenant_class, model_class, request_kind, priority, and outcome.
    - A test asserts metadata does not contain prompt, message, api_key, authorization, secret, provider payload, embedding, semantic-cache payload, or raw task text.
  </acceptance_criteria>
  <done>Durable SLA evidence exists only for meaningful promotion/block decisions and remains sanitized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record one promotion outcome per pre-pop attempt</name>
  <files>internal/scheduler/sla_promotion.go, internal/scheduler/sla_promotion_test.go</files>
  <read_first>
    - internal/scheduler/sla_promotion.go
    - internal/scheduler/executor.go
    - internal/observability/metrics.go
    - internal/scheduler/sla_promotion_test.go
    - .planning/phases/19-sla-waiting-time-promotion/19-CONTEXT.md
  </read_first>
  <behavior>
    - Each enabled pre-pop promotion attempt records exactly one metrics outcome.
    - Disabled promotion records disabled once when the promoter is present but disabled.
    - Promotion errors record error once and Executor.RunOne still proceeds to PopMin.
    - Promoted and blocked outcomes record metrics, audit, and sanitized logs.
    - not_eligible records metrics only.
  </behavior>
  <action>Update SLAPromoter.PromoteBeforePop so every call records one IncSchedulerSLAPromotion result with the matched policy ID or `none`, tenant class or `anonymous`, model_class or `unknown`, request_kind or `simple_qa`, priority or `normal`, and final outcome. Keep audit emission restricted to promoted and blocked_by_priority_or_quota. Ensure Executor.RunOne does not return a promotion metrics/audit error when Queue.PopMin can still proceed.</action>
  <verify>
    <automated>go test -timeout 60s ./internal/scheduler ./internal/observability</automated>
  </verify>
  <acceptance_criteria>
    - A scheduler test with a fake metrics collector asserts exactly one metric for promoted.
    - A scheduler test asserts exactly one metric for not_eligible and no audit event.
    - A scheduler test asserts exactly one metric for disabled and no audit event.
    - A scheduler test asserts a promotion queue error records outcome error and then RunOne still pops the original next task.
  </acceptance_criteria>
  <done>SLA promotion produces deterministic evidence without breaking fail-open execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Promotion decision -> metrics | Runtime promotion outcomes become operator-visible Prometheus labels. |
| Promotion decision -> durable audit | Promoted and blocked outcomes become durable control-state audit events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Information Disclosure | metrics/audit/logs | mitigate | Emit only policy ID, tenant ID/class, model class, request kind, priority, and outcome; keep tenant ID out of Prometheus labels and exclude prompts, secrets, provider payloads, embeddings, semantic-cache payloads, and raw text. |
| T-19-03-02 | Tampering | audit metadata | mitigate | Pass metadata through controlstate.SafeAuditMetadata and assert the allowed key set in tests. |
| T-19-03-03 | Denial of Service | metrics cardinality | mitigate | Sanitize/bound Prometheus labels and map unknown outcomes to known buckets. |
| T-19-03-04 | Repudiation | durable audit | mitigate | Persist audit for promoted and blocked_by_priority_or_quota outcomes only, with policy and tenant evidence. |
</threat_model>

<verification>
Run:
- `go test -timeout 60s ./internal/scheduler ./internal/observability ./internal/app`
- `go build ./...`
</verification>

<success_criteria>
- SLA-04 is satisfied: promotions emit sanitized audit and metrics evidence.
- D-11 through D-14 are represented in code and tests.
- Metrics distinguish promoted, not_eligible, blocked_by_priority_or_quota, disabled, and error.
- Durable audit is not written for every eligibility check.
</success_criteria>

## Artifacts this phase produces

- Metrics method: IncSchedulerSLAPromotion.
- Prometheus metric: gateway_scheduler_sla_promotion_total.
- SLA promotion outcomes: promoted, not_eligible, blocked_by_priority_or_quota, disabled, error.
- Audit action: scheduler.sla_promotion.
- Audit metadata keys: policy_id, tenant_id, tenant_class, model_class, request_kind, priority, outcome.
- Structured log keys: policy_id, tenant_id, tenant_class, model_class, request_kind, priority, outcome.

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| ROADMAP success 4 | Sanitized audit, log, and metric records | Covered by Tasks 1, 2, and 3. |
| SLA-04 | Promotions emit sanitized audit/log/metric records with task class, policy, and tenant identifiers only | Covered by all tasks. |
| CONTEXT D-11 | Audit only when promotion happens or is blocked | Covered by Task 2. |
| CONTEXT D-12 | Allowed evidence fields | Covered by Task 2 metadata checks and Task 1 label checks. |
| CONTEXT D-13 | Excluded sensitive fields | Covered by Tasks 1 and 2. |
| CONTEXT D-14 | Outcome buckets | Covered by Tasks 1 and 3. |

<output>
Create `.planning/phases/19-sla-waiting-time-promotion/19-03-SUMMARY.md` when done.
</output>
